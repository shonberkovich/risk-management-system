הנה קובץ המשימות המרוכז שתוכלו לעבוד עליו, אתה ושיי, כדי לחבר את כל ה-APIs האמיתיים לממשק ולסגור את הפער שנוצר בין השרת ללקוח.

הכנתי את זה באותו פורמט עבודה נוח עם צ'ק-ליסטים, כך שתוכלו להעתיק כל בלוק בנפרד לפרומפט של ה-AI או ללוח המשימות שלכם.

---

# השלמת פיתוח צד לקוח (Frontend) – אינטגרציות חיצוניות אמיתיות

**הפרומפט להעתקה (יש להדביק לפני כל משימה):**

> "עליך לממש את המשימה הבאה בצד הלקוח (React) של מערכת ה-RMIS. קרא היטב את תבנית המשימה וודא שאתה פועל בדיוק לפי השלבים המפורטים. המטרה היא לחבר את ה-UI ל-API אמיתי שכבר קיים ועובד בשרת. בסיום, וודא שכל סעיף מסומן ב-'[x]' לאחר שהושלם ונבדק."

---

### [ ] משימה 1: תצוגת נתוני בנק ישראל (BOI Market Data)

* **מהות המשימה**: הוספת כרטיסיה או טבלה המציגה נתוני אינפלציה ושערי חליפין (דולר/אירו) אמיתיים.
* **מיקום במערכת**: מסך הפיננסים / שערוך נכסים, או תחת מסך האינטגרציות.
* **API בשרת**: `GET /api/integrations/economics/boi-market-data`
* **שלבי ביצוע (Checklist)**:
* [ ] שלב 1: הוספת הגדרת קריאה (API call) בקובץ `frontend/src/api/client.ts`.
* [ ] שלב 2: פיתוח קומפוננטת `BoiMarketDataCard` המשתמשת ב-React Query למשיכת הנתונים.
* [ ] שלב 3: עיצוב הקומפוננטה להצגת שערי המטבע ושיעור האינפלציה השנתי (YoY) בצורה ויזואלית.
* [ ] שלב 4: שיבוץ הקומפוננטה במסך המתאים (למשל `Integrations.tsx` או `Financials.tsx`).



---

### [ ] משימה 2: כפתור יזום להדמיית רעידות אדמה (GSI Seismic Trigger)

* **מהות המשימה**: הוספת אפשרות למנהל המערכת לדגום ידנית את נתוני המכון הגיאולוגי ולייצר טיוטות אירוע במידת הצורך (תחליף ל-Cron Job שאינו קיים).
* **מיקום במערכת**: מסך ניהול אירועים (`Incidents`) או מסך האינטגרציות.
* **API בשרת**: `POST /api/incidents/check-seismic-activity`
* **שלבי ביצוע (Checklist)**:
* [ ] שלב 1: הוספת פונקציית POST ב-`client.ts` המפעילה את הראוטר.
* [ ] שלב 2: הוספת כפתור "בצע סריקת רעידות אדמה (GSI)" בסרגל העליון של טבלת האירועים או האינטגרציות.
* [ ] שלב 3: חיבור הכפתור ל-Mutation ב-React Query, כולל חיווי טעינה (Loading spinner) בזמן שהשרת בודק נתונים.
* [ ] שלב 4: הוספת ריענון אוטומטי (`invalidateQueries`) לטבלת האירועים אם השרת החזיר שנוצרו טיוטות חדשות.



---

### [ ] משימה 3: תצוגת קרבה למפעלים וחומרים מסוכנים (Data.gov.il)

* **מהות המשימה**: הנגשת הנתונים המחולצים מהמאגר הממשלתי שמראים אילו נכסים נמצאים ברדיוס סכנה של מפעל.
* **מיקום במערכת**: מסך תיק נכס (לשונית סיכונים) או מסך אינטגרציות מרוכז.
* **API בשרת**: `GET /api/integrations/environmental/property-hazmat-proximity`
* **שלבי ביצוע (Checklist)**:
* [ ] שלב 1: רישום ה-Endpoint ב-`client.ts`.
* [ ] שלב 2: בניית קומפוננטה או הרחבת טבלת סיכוני נכס קיימת, שתציג עמודות: שם המפעל הקרוב, מרחק בק"מ, וסטטוס (בתוך/מחוץ לרדיוס הסכנה).
* [ ] שלב 3: הוספת התראה ויזואלית (דגל אדום או אייקון אזהרה) בתיק הנכס אם השדה `within_hazard_radius` חוזר כ-True.



---

### [ ] משימה 4: באנר התראות בזמן אמת - פיקוד העורף (Home Front Command)

* **מהות המשימה**: יצירת חיווי גלובלי מתפרץ במקרה של אזעקות אמת באזורים שבהם יש לחברה נכסים.
* **מיקום במערכת**: גלובלי – סרגל עליון (Top Bar) ב-`MainLayout.tsx`.
* **API בשרת**: `GET /api/integrations/home-front/alerts`
* **שלבי ביצוע (Checklist)**:
* [ ] שלב 1: הוספת הפונקציה ב-`client.ts`.
* [ ] שלב 2: יצירת קומפוננטת `HomeFrontAlertBanner` המבצעת Polling (למשל כל 30 שניות) לשרת.
* [ ] שלב 3: אם ה-API מחזיר מערך ריק (שגרה), הקומפוננטה מוסתרת.
* [ ] שלב 4: אם יש התראות, הצגת באנר אדום בראש המסך עם רשימת הנכסים המושפעים (`affected_properties`).



---

### [ ] משימה 5: השלמת המרת קואורדינטות בטופס הדיווח (Reverse Geocoding)

* **מהות המשימה**: מילוי אוטומטי של שדה הכתובת כאשר עובד שטח מדווח על אירוע ומשתף את מיקומו.
* **מיקום במערכת**: טופס דיווח אירוע (`IncidentReport` / `IncidentForm`).
* **API בשרת**: `GET /api/integrations/gis/reverse-geocode`
* **שלבי ביצוע (Checklist)**:
* [ ] שלב 1: חיבור הראוטר ל-`client.ts` שמקבל `lat` ו-`lon`.
* [ ] שלב 2: ברגע שהמשתמש לוחץ על כפתור "שתף מיקום" בטופס הדיווח (או כשה-GPS נדגם), שליחת הקואורדינטות לשרת.
* [ ] שלב 3: קבלת הכתובת הטקסטואלית מהשרת ועדכון שדה הכתובת (`resolved_address`) בטופס (State/Form value) כך שהמשתמש יראה את הכתובת בעברית לפני השליחה.
