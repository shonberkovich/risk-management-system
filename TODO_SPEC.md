1. Database & Data Model (בסיס נתונים)
[x] הרחבת Financial_Statements למאזן מלא — כיום קיימים רק נכסים, הכנסות, רווח והוצאות ביטוח. יש להוסיף התחייבויות, הון עצמי ורווח תפעולי (backend/app/models.py + מיגרציית Alembic).
✅ בוצע ב-branch feature/financial-statements-full-balance-sheet: נוספו total_liabilities, total_equity, gross_profit, operating_profit (nullable) ל-FinancialStatement, schema.sql, מיגרציית Alembic 8f2a4c6e1d09, seed.py, schemas.py ו-frontend/src/api/client.ts. services/financials.py חושב כעת gross_margin/operating_margin/equity_ratio, ודוח הרגולציה משתמש ב-total_equity האמיתי (עם נפילה ל-total_assets לשורות ישנות).

[x] טבלת נמעני התראות (Notification_Recipients) — הוצאת נמעני ההתראות מהקוד (Hardcoded) לטבלה דינמית לצורך ניהול עתידי דרך ה-UI.
✅ בוצע ב-branch feature/notification-recipients-table: נוספה טבלת Notification_Recipients (models.py, schema.sql, מיגרציית Alembic c3d9a17f4b62, seed.py — מיגרציה של שני נמעני ברירת המחדל הקודמים). services/notifications.py קורא כעת מה-DB דרך _load_recipients (עם נפילה חזרה ל-DEFAULT_RECIPIENTS אם הטבלה ריקה). נוספו endpoints לניהול: GET/POST/PATCH/DELETE /api/notifications/recipients (כתיבה מוגבלת ל-ADMIN), schemas.py, docs/erd.md, וטסטים ב-test_api_notification_recipients.py.

[x] טבלת Notification_Log — תיעוד התראות שנשלחו בפועל (ערוץ, נמען, סטטוס, חותמת זמן) לצורך Audit Trail.
✅ בוצע ב-branch feature/notification-log-table: נוספה טבלת Notification_Log (models.py, schema.sql עם אינדקס על sent_at, מיגרציית Alembic d4e1b298a715). services/notifications.dispatch_notifications כותבת כעת שורת Audit לכל התראה שנשלחה (בנוסף ל-logger.log הזמני שהיה קיים). נוסף endpoint לקריאה בלבד: GET /api/notifications/log (אותה קבוצת תפקידים כמו preview/dispatch, ללא endpoint כתיבה — האפליקציה היא הכותבת היחידה), schemas.py, docs/erd.md, וטסטים ב-test_api_notification_log.py.

[x] אינדקסים לביצועים — הוספת אינדקס מרחבי (GIST) על קואורדינטות נכסים, FK indexes ואינדקסים מורכבים בטבלאות התביעות והאירועים.
✅ בוצע ב-branch feature/performance-indexes (backend/sql/schema.sql + מיגרציית Alembic e5f7a2c9b3d1). בבדיקה מול הסכימה הקיימת התברר ששני חלקים מהמשימה כבר טופלו בעבר (commit c7cbdcb, "Add Claim_Payments composite index; document existing/impractical index gaps") אך התיעוד שלהם ב-TODO_SPEC.md אבד בעדכון מאוחר יותר של הקובץ (commit 3823186): (1) "אינדקס מרחבי GIST" — GIST הוא מנוע PostgreSQL; SQL Server דורש לשם כך עמודת geography/geometry ייעודית במקום זוג ה-DECIMAL הקיימים (latitude/longitude), שינוי מבנה נתונים החורג מהיקף המשימה — האינדקס המורכב הקיים IX_Properties_Coordinates(latitude, longitude) כבר משרת בפועל את דפוסי השאילתה (מפה, אשכולות MFL), עם הערה מתועדת ב-schema.sql המסבירה את הפער. (2) "אינדקס מורכב על Incidents(status, hazard_type)" ו-"Claims(claim_status, payment_date)" — כבר קיימים כ-IX_Incidents_StatusHazard ו-IX_ClaimPayments_ClaimDate (payment_date קיים בפועל ב-Claim_Payments ולא ב-Claims); תוקנה גם הפניה תלויה (dangling) בהערת schema.sql ל-"אינדקסים חסרים" שכבר לא קיים כסעיף. מה שבאמת נותר וטופל כעת: **FK indexes חסרים** על חמש עמודות שלא היו ממופתחות — Properties.primary_manager_id (IX_Properties_PrimaryManager), Policy_Assets.property_id כעמודה עצמאית מעבר ל-PK המורכב (IX_PolicyAssets_Property), Incidents.reported_by_user_id (IX_Incidents_ReportedBy), Incident_Media.incident_id (IX_IncidentMedia_Incident), Mitigation_Tasks.assigned_to_user_id (IX_Mitigation_AssignedTo). models.py לא עודכן (עקביות עם הדפוס הקיים בקוד — כל האינדקסים מוגדרים רק ב-schema.sql, לא כ-Index() ב-ORM). 58/58 טסטים עוברים.

[x] ניהול מיגרציות — יצירת מיגרציות אינקרמנטליות לכל שינויי הסכימה תחת backend/alembic/versions/.
✅ בוצע ב-branch feature/verify-migration-chain: אומת (`alembic history`) ששרשרת המיגרציות תחת backend/alembic/versions/ שלמה ורציפה, ראש יחיד ללא ענפים — <base> → 5d5363142853 (baseline, תואם schema.sql נכון למועד היווצרותו, ר' c6d2ea1) → 8f2a4c6e1d09 (financial statements) → c3d9a17f4b62 (Notification_Recipients) → d4e1b298a715 (Notification_Log) → e5f7a2c9b3d1 (performance indexes, head). כל שינוי סכימה שבוצע מאז יצירת ה-baseline קיבל בפועל מיגרציה ייעודית משלו במקביל לעדכון schema.sql (התהליך שתואר ב-CLAUDE.md כבר נשמר בפועל ב-4 השינויים האחרונים) — לא נמצא פער. לא בוצע שינוי קוד; זו משימת אימות/תיעוד בלבד, ללא נספח קוד.

2. Backend — APIs חסרים (ליבת הלוגיקה)
[x] CRUD מלא לנכסים — הוספת POST, PUT, DELETE ל-properties.py (קריטי לניהול Asset Inventory).
✅ בוצע ב-branch feature/properties-crud: נוספו POST /api/properties (יצירה, בדיקת ייחודיות property_code, ולידציית קיום primary_manager_id/region_id), PUT /api/properties/{id} (עדכון חלקי, אותן ולידציות) ו-DELETE /api/properties/{id} (soft delete — is_active=False, ולא מחיקה פיזית, כדי לשמר את שרשרת הנתונים התלויה: Asset_Risk_Profiles/Incidents/Claims/Mitigation_Tasks/Policy_Assets — GET /api/properties הקיים כבר מסנן לפי is_active כך שההיסטוריה נשארת נגישה דרך GET /{id}). תפקידי כתיבה: RISK_MANAGER/PROPERTY_MANAGER/ADMIN (כמו mitigation.py). נוספו PropertyCreate/PropertyUpdate ב-schemas.py, עודכן docs/README.md (טבלת ה-routers), וטסטים ב-test_api_properties_crud.py. 68/68 טסטים עוברים (10 חדשים).

[x] API לסקרי סיכונים (Asset_Risk_Profiles) — יצירת Endpoint ליצירה ועדכון של סקר סיכונים, MFL ואמצעי מיגון (risk_profiles.py).
✅ בוצע ב-branch feature/risk-profiles-api: נוסף ראוטר חדש backend/app/routers/risk_profiles.py, רשום ב-main.py. מאחר ול-Property יש לכל היותר פרופיל סיכון אחד (יחס 1:1, models.Property.risk_profile), ה-endpoints מקוננים תחת /api/properties/{property_id}/risk-profile ולא כאוסף שטוח: GET (שליפה), POST (יצירה ראשונה — 409 אם כבר קיים פרופיל, מכוון להשתמש ב-PUT), PUT (עדכון חלקי/סקר חוזר — 404 אם עוד אין פרופיל). ולידציית טווח 1-5 לציוני הסיכון (flood/fire/earthquake) ב-Pydantic (Field(ge=1, le=5)) כדי להחזיר 422 ברור במקום שגיאת CHECK גולמית מה-DB. נוספו RiskProfileCreate/RiskProfileUpdate ב-schemas.py, עודכן docs/README.md (טבלת ה-routers וספירת ה-routers ל-19), וטסטים ב-test_api_risk_profiles.py. 77/77 טסטים עוברים (9 חדשים).

[x] API לעדכון רזרבות (Claim_Reserves) — הוספת דרך להזין ולעדכן רזרבה על תביעה פתוחה ב-claims.py.
✅ בוצע ב-branch feature/claim-reserves-api: נוספו ל-claims.py שלושה endpoints תחת /api/claims/{claim_id}/reserves — GET (רשימה, מהחדש לישן), POST (הזנת רזרבה חדשה) ו-PATCH /{reserve_id} (עדכון). שניהם (POST/PATCH) חסומים לתביעה "פתוחה" בלבד — נבדק מול claim_status לא ב-_TERMINAL_CLAIM_STATUSES (SETTLED/REJECTED), אותו guard הקיים כבר ב-update_claim. נוספו ClaimReserveOut/Create/Update ב-schemas.py, עודכן docs/README.md, וטסטים ב-test_api_claim_reserves.py. 83/83 טסטים עוברים (6 חדשים).

[x] ניהול משתמשים (users.py) — הוספת יכולות כתיבה: יצירת משתמש, עריכת פרטים, שינוי תפקיד והשבתה.
✅ בוצע ב-branch feature/users-write-api: נוספו POST /api/users (יצירה, בדיקת ייחודיות email) ו-PATCH /api/users/{id} (עדכון חלקי — משמש גם לעריכת פרטים, גם לשינוי role, וגם להשבתה דרך is_active=False), שניהם ADMIN בלבד. Users.is_active הוא שדה חדש (models.py, schema.sql, מיגרציית Alembic a1c4d8e2f6b0, ברירת מחדל True) — נאכף לא רק בהתחברות הבאה (routers/auth.py login/refresh) אלא גם על כל טוקן שכבר הונפק (dependencies/permissions.get_current_user בודק is_active בכל בקשה), כך שהשבתה נכנסת לתוקף מיידית. הגנה נוספת: אדמין לא יכול להשבית את עצמו (400). נוספו UserAdminOut/UserCreate/UserUpdate ו-UserRole (Literal) ב-schemas.py; GET /api/users הקיים נשאר פתוח ומינימלי (ל-UI pickers). עודכן docs/README.md (טבלת routers + §9, ההערה הישנה ש"אין ניהול משתמשים" תוקנה לשקף את המצב בפועל). טסטים ב-test_api_users_admin.py. 93/93 טסטים עוברים (10 חדשים).

[x] ניהול Role_Permissions — יצירת API לצפייה ולעריכה של הרשאות לפי תפקיד (החלפת ה-Seed הקבוע).
✅ בוצע ב-branch feature/role-permissions-api: נוסף ראוטר חדש backend/app/routers/role_permissions.py (רשום ב-main.py) — GET /api/role-permissions (פתוח, עם פילטר role אופציונלי), POST (יצירה, 400 אם הצירוף role+permission_key כבר קיים), PATCH /{id} (עריכת description בלבד — role/permission_key יחד מזהים את השורה, שינוי שלהם הוא בפועל מחיקה+יצירה ולא עריכה), DELETE /{id}, כולם ADMIN בלבד לכתיבה. **הערת היקף חשובה שתועדה בקוד:** ה-API מנהל את קטלוג ההרשאות (התיעוד של מה כל role אמור להיות מורשה) ומחליף את רשימת ה-Seed הקבועה כמקור לעריכה — אך **אינו** משנה את אכיפת ה-RBAC בפועל, שממשיכה להתבצע כמו קודם דרך require_roles() מקושח בכל router; חיווט אכיפה חיה שקוראת מהטבלה הזו הוא שינוי גדול משמעותית מ"צפייה ועריכה של הקטלוג" וחורג מהיקף המשימה. אגב כך תוקן פער אמיתי: UniqueConstraint(role, permission_key) שהיה קיים רק ב-schema.sql (UQ_RolePermissions_RoleKey) אך לא ממופה ב-models.py — נוסף ל-RolePermission כך שגם ה-DB הזמני של הטסטים (SQLite, נבנה מ-models.py) אוכף את הייחודיות. נוספו RolePermissionOut/Create/Update ו-UserRole (Literal, נוצר גם עבור users.py הקודם) ב-schemas.py, עודכן docs/README.md (טבלת routers + ספירה ל-20), וטסטים ב-test_api_role_permissions.py. 101/101 טסטים עוברים (8 חדשים).

[x] API פיננסי — הוספת אפשרות הזנה (POST) של דוחות כספיים רב-שנתיים למערכת ב-financials.py.
✅ בוצע ב-branch feature/financial-statements-post-api: נוספו GET /api/financials/statements (רשימת שורות Financial_Statements גולמיות, מהשנה החדשה לישנה) ו-POST /api/financials/statements (הזנת דוח לשנה נתונה — 400 אם כבר קיים דוח לאותה שנה, מגבלת UNIQUE על year). אותה קבוצת תפקידים כמו שאר financials.py (RISK_MANAGER/CFO/ADMIN). הדוח שנכנס דרך ה-POST מוזן מיד ל-services/financials.calculate_multi_year_trends / build_regulatory_report (נבדק בטסט ייעודי). נוספו FinancialStatementOut/Create ב-schemas.py, עודכן docs/README.md, וטסטים חדשים ב-test_api_financial_statements.py. 106/106 טסטים עוברים (5 חדשים).

[x] אוטומציית שטח (ERP & Alerts) — חיבור טריגר למשימת אחזקה ב-ERP ומשלוח התראות Push/SMS בעת הגשת אירוע בסטטוס CRITICAL.
✅ בוצע ב-branch feature/critical-incident-alerts: חלק ה-ERP (משימת מיטיגציה + כרטיס תחזוקה מדומה) כבר היה קיים מקודם (routers/incidents.py::_trigger_critical_incident_ticket). נוסף כעת החלק החסר — משלוח התראות: services/notifications.py קיבל build_critical_incident_alert (בונה alert יחיד מסוג "critical_incident") ו-dispatch_critical_incident_alert (מנתב+"שולח" אותו לנמענים הפעילים ב-Notification_Recipients, אותה לוגיקת ניתוב וסימולציה כמו dispatch_notifications — רפקטור משותף דרך _route_alerts/_send_and_log כדי לא לשכפל קוד). _trigger_critical_incident_ticket קורא לזה בסוף (רק אם settings.notifications_enabled, silent skip ולא 503 — זהו טריגר רקע ולא endpoint). כל התראה שנשלחה נרשמת גם ב-Notification_Log כרגיל (נראית ב-GET /api/notifications/log). alert_type "critical_incident" נוסף ל-3 מקומות Literal ב-schemas.py (AlertOut/NotificationOut/NotificationLogOut). תוקנה גם הפניה תלויה (dangling) בתיעוד services/notifications.py ל-"docs/README.md §8" שכבר לא קיים בפועל. עודכן docs/README.md (שורת incidents.py). טסטים חדשים ב-test_api_incidents.py (2 חדשים: שולח לנמענים על CRITICAL, לא שולח על לא-CRITICAL). 108/108 טסטים עוברים.

3. אבטחה, RBAC ותאימות (Security & Compliance)
[x] אכיפת RBAC על בקשות ה-GET — חסימת צפייה במידע פיננסי (KPIs, פוליסות) לתפקידים כמו FIELD_WORKER על ידי הוספת require_roles גם לקריאות קריאה.
✅ בוצע ב-branch feature/rbac-financial-get-endpoints: נוסף require_roles (חדש: _FINANCIAL_READ_ROLES / _POLICIES_READ_ROLES — כל תפקיד חוץ מ-FIELD_WORKER) לקריאות GET שמובילות בדמויות כספיות: analytics.py (/kpis, /cashflow, /exposure-by-region, /geographic-exposure-clusters, /loss-ratio-trend) ו-policies.py (כל ה-GETs: רשימה, לפי-id, /assets), וגם GET /api/incidents/{id}/eligible-policies (מחזיר PolicyOut, אותה הגנה). endpoints תפעוליים שלא מובילים בסכום כספי (map, risk-matrix, alerts, hazard-distribution) נשארו פתוחים במכוון — נבדק בטסט ייעודי. financials.py/compliance.py/integrations.py כבר היו מוגנים מקודם. עודכן טסט קיים ב-test_api_policies.py (הוספת headers, כי ה-endpoint כבר לא פתוח). claims.py לא נגע — נשאר פתוח במכוון, יש טסט קיים (test_list_claims_is_readable_without_auth) שמתעד זאת במפורש והיקף המשימה נקב רק ב-KPIs/פוליסות. עודכן דוקסטרינג המודול dependencies/permissions.py לשקף את החריגה החדשה, ו-docs/README.md (שורות policies.py/analytics.py). טסטים חדשים ב-test_api_permissions.py (2 חדשים). 110/110 טסטים עוברים.

[x] הצפנת שדות רגישים — הצפנת פרטי שמאי, סכומי כיסוי רגישים ומטא-דאטה מעבר למוצפן כיום (ב-encryption.py).
✅ בוצע ב-branch feature/encrypt-sensitive-fields: הורחבה services/encryption.py (EncryptedString הקיים + EncryptedText חדש ל-NVARCHAR(MAX)) והוחלו על ארבעה שדות חדשים: Claims.adjuster_name ("פרטי שמאי" — הורחב מ-Unicode(100) ל-EncryptedString(255)), Insurance_Policies.per_event_limit ו-Policy_Assets.specific_deductible ("סכומי כיסוי רגישים" — Numeric(18,2) → EncryptedString(64)), ו-Audit_Log.old_value/new_value ("מטא-דאטה" — תמונת המצב לפני/אחרי של גוף הבקשה, עלולה להכיל בעצמה שדות רגישים אחרים; UnicodeText → EncryptedText). היקף מכוון (מתועד בהרחבה ב-docstring של encryption.py): annual_premium/total_limit/deductible_default של Insurance_Policies **לא** הוצפנו — הם מסוכמים על פני שורות רבות בפייתון (kpi.py/financials.py) לצורך loss ratio/cashflow/תקציר מנהלים AI, אותה בעיית architectural כמו ה-Claim_Payments.amount/Claim_Reserves.reserve_amount שכבר תועדו כמחוץ להיקף. per_event_limit/specific_deductible לעומת זאת נקראים תמיד רשומה-בודדת (retention.py, routers/properties.py, routers/policies.py) ולכן בטוחים להצפנה בלי לשבור אגרגציה — כל אתרי הקריאה כבר עוטפים ב-float() כפי שנדרש כש-EncryptedString מחזיר str מפוענח. נוספה מיגרציית Alembic f1a9c7e4b2d3 (ALTER COLUMN ל-3 מהשדות; old_value/new_value נשארים NVARCHAR(MAX) כך שאין ALTER נדרש עבורם) ועודכן schema.sql בהתאם, כולל הערה שערכים ישנים (plaintext, כפי שנשמרו לפני המעבר או דרך seed.py שמכניס ישירות דרך pyodcb כמו reference_number הקיים) ממשיכים להיקרא תקין דרך ה-fallback הקיים ב-EncryptedString.process_result_value בלי מיגרציית backfill. עודכן docs/erd.md (סימון "מוצפן at-rest" על 5 השדות). נוספו טסטים חדשים ב-test_encryption.py (6 חדשים — round-trip הצפנה/פענוח, אימות שהערך הגולמי בעמודה אינו plaintext, ותאימות אחורה לערכים לא-מוצפנים קיימים). 116/116 טסטים עוברים.

[x] מוכנות לפרודקשן (Production Ready) — הפעלת force_https=True מבוסס משתני סביבה ומנגנון רוטציית מפתחות ל-JWT ולשדות מוצפנים.
✅ בוצע ב-branch feature/production-readiness-key-rotation: (1) **force_https מבוסס משתני סביבה** — force_https (main.py, קיים מקודם) כבר נקרא מ-settings שנטען מ-.env/משתני סביבה; נוסף כעת settings.environment ("development"/"staging"/"production") ו-model_validator (`_default_force_https_in_production`) שמדליק אוטומטית force_https=True כש-ENVIRONMENT=production, אלא אם FORCE_HTTPS הוגדר במפורש (נבדק מול Settings.model_fields_set) — כדי שפריסת production לא תישאר בטעות בלי אכיפת HTTPS רק כי מישהו שכח את הדגל, בלי לפגוע באפשרות לכבות אותו במפורש מאחורי proxy שכבר מטפל ב-TLS. (2) **רוטציית מפתחות JWT** — services/auth.py: הוספת settings.jwt_previous_secret_keys (רשימת מפתחות קודמים מופרדת בפסיקים) ו-decode_token מנסה כעת את jwt_secret_key הנוכחי ואז כל מפתח קודם ברשימה, כך שטוקנים שהונפקו לפני רוטציה ממשיכים להיות תקפים עד שפג תוקפם הטבעי (עד jwt_refresh_token_expire_days) במקום להתנתק את כל המשתמשים בבת אחת; חתימת טוקנים חדשים (create_access_token/create_refresh_token) ממשיכה להשתמש רק במפתח הנוכחי. (3) **רוטציית מפתחות הצפנה** — services/encryption.py: `_fernet()` בונה כעת `cryptography.fernet.MultiFernet` מ-[field_encryption_key, *field_encryption_key_previous] (רשימת מפתחות קודמים, אותו פורמט כמו JWT) — הצפנה חדשה תמיד עם המפתח הנוכחי (החוזה המתועד של MultiFernet), פענוח מנסה כל מפתח ברשימה, כך ש-ciphertext שהוצפן תחת מפתח שהוחלף ממשיך להיקרא בלי מיגרציית backfill. תועד תהליך הרוטציה המלא (כולל re-encryption הדרגתי) ב-docstring של config.py/encryption.py וב-.env.example. נוספו 10 טסטים חדשים ב-test_key_rotation.py (רוטציית JWT — מפתח נוכחי/רוטציה/הסרה סופית; רוטציית הצפנה — round-trip/רוטציה/הסרה סופית; ברירת המחדל של force_https לפי environment). 126/126 טסטים עוברים (10 חדשים). לא בוצע: מנגנון re-encryption אוטומטי (סקריפט batch שקורא כל שורה מוצפנת ושומר אותה מחדש תחת המפתח החדש) — תועד כצעד ידני/עתידי בתהליך הרוטציה, מחוץ להיקף המשימה (זהה בגודל למיגרציית backfill שכבר תועדה כחריגה בהצפנת השדות הרגישים הקודמת).

[x] הרחבת דוחות רגולציה — הרחבת מודול ה-Compliance גם לתקני Solvency II והנחיות שוק ההון.
✅ בוצע ב-branch feature/compliance-solvency-ii-cma: הורחב services/compliance.py (build_iso31000_report, שם הפונקציה נשמר לתאימות לאחור עם הראוטר הקיים) כך שה-framework_sections שהוא מחזיר כוללות כעת, לצד 5 סעיפי ISO 31000:2018 הקיימים, גם: **Solvency II — Pillar II (מבנה ממשל תאגידי)** — SII-44 "פונקציית ניהול סיכונים" (בדיקה שקיים משתמש פעיל בתפקיד RISK_MANAGER/RISK_OFFICER, כנדרש בסעיף 44 לדירקטיבה) ו-SII-45 "הערכת סיכונים וסולבנסי עצמית (ORSA)" (נעשה שימוש חוזר במדד עדכניות הסקרים הקיים של ISO 6.6 — אותה ראיה מספקת את שתי הדרישות המקבילות, כפי שממופה תאימות אמיתי בפועל); ו-**רשות שוק ההון, ביטוח וחיסכון — חוזר ניהול סיכונים** — CMA-3 "הקצאת בעלי אחריות" (שימוש חוזר במדד בעלות על נכסי סיכון גבוה/קריטי הקיים של ISO 6.4.4, תחת הניסוח של הרגולטור המקומי) ו-CMA-7 "מוכנות לדיווח הלימות הון" (בדיקה בוליאנית שקיימת לפחות שורת Financial_Statements אחת שממנה services/financials.py::build_regulatory_report יכול להפיק בפועל דוח הלימות הון/סולבנסי). **הערת היקף:** דוח ה-Pillar I הכמותי (יחס סולבנסי מבוסס VaR) כבר קיים ונפרד במכוון ב-financials.py::build_regulatory_report — לא שוכפל כאן, שכן זהו חישוב פיננסי בודד ולא מיפוי בשלות-תהליך; QRTs של Pillar III ופרשנות משפטית מחייבת של חוזרי הרשות הוצאו מהיקף במפורש בדוקסטרינג של המודול, מאותה סיבה שכבר תועדה עבור כלל הדוח — דורשים גורם ציות/אקטוארי אמיתי לחתום, לא סף מחושב. נוסף שדה `standard` ל-ComplianceFrameworkSectionOut (schemas.py) כדי לתייג כל סעיף למסגרת שלו; שדה `standard` ברמת הדוח הפך למחרוזת משולבת ("ISO 31000:2018 + Solvency II (Pillar II) + חוזר ניהול סיכונים (רשות שוק ההון)"). frontend: client.ts (שדה standard חדש), Compliance.tsx (כותרת הכרטיס הוכללה, כל סעיף מציג כעת גם את שם המסגרת שלו, מפתח ה-key ברשימה עודכן ל-standard+clause כדי להישאר ייחודי). נוספו טסטים חדשים ב-test_api_compliance.py (6 חדשים — לא היה קובץ טסטים לראוטר הזה קודם: RBAC, כיסוי שלוש המסגרות, תגובת SII-44 למשתמש RISK_OFFICER פעיל, תגובת CMA-7 לקיום Financial_Statements, ואימות ש-CMA-3 ו-ISO 6.4.4 חולקות אותו מדד בפועל). 132/132 טסטים עוברים (6 חדשים). `npx tsc --noEmit` נקי מלבד שגיאת stylis הקיימת מראש שאינה קשורה (מאומת מול main ללא השינויים).

4. חוויית משטח, AI ומובייל (Field UX & Copilot) — חדש
[x] סייר AI (Copilot Widget) — פיתוח רכיב צף ב-UI לשאילתות שפה טבעית מול מנוע ה-AI (/api/ai/ask) עבור מנהלי הסיכונים.
✅ בוצע ב-branch feature/ai-copilot-widget: נוסף רכיב חדש frontend/src/components/CopilotWidget.tsx — כפתור FAB צף (פינה תחתונה, `insetInlineEnd` כדי להישאר תקין גם ב-RTL) שנפתח לפאנל צ'אט קטן: שאלות מוצעות (TIV, נכסי סיכון גבוה, Loss Ratio) לפתיחה מהירה, שדה טקסט חופשי + שליחה ב-Enter, היסטוריית שאלות/תשובות בתוך המצב המקומי של הרכיב (לא נשמר בין רענוני עמוד — היקף מכוון, אין דרישה לכך במשימה), אינדיקטור טעינה, וטיפול שגיאות ממופה (503 → "שירות ה-AI אינו מוגדר", 429 → יותר מדי בקשות, ברירת מחדל → הודעה כללית). הרכיב מורכב פעם אחת ב-Layout.tsx (לא per-route) כך שהוא זמין בכל מסך מאובטח. משתמש ב-askQuestion הקיים כבר ב-api/client.ts מול POST /api/ai/ask (services/llm.ask_question) — לא נוספה גישת נתונים חדשה, הרכיב הוא מעטפת UI בלבד סביב ה-endpoint המוגבל-בכלים הקיים (ר' CLAUDE.md: "AI tool-use Q&A is deliberately constrained"). **אומת חזותית בדפדפן** (Browser pane, לאחר login אמיתי מול LocalDB): הכפתור מופיע בדשבורד, הפאנל נפתח/נסגר כראוי, שאלה מוצעת נשלחת ומחזירה בהצלחה את הודעת ה-503 המנומסת (שכן ANTHROPIC_API_KEY לא מוגדר בסביבת הפיתוח המקומית) — בדיוק ההתנהגות הרצויה של "degrade gracefully". `npx tsc --noEmit` נקי (מלבד שגיאת stylis הקיימת מראש שאינה קשורה). **תגלית אגבית מחוץ להיקף, לא תוקנה כאן:** האימות דרש להריץ `alembic upgrade heads` על ה-LocalDB המקומי כי מתגלה שרשרת המיגרציות תחת backend/alembic/versions/ התפצלה לשני heads מקבילים (a1c4d8e2f6b0 ו-f1a9c7e4b2d3, שניהם עם down_revision=e5f7a2c9b3d1) — סתירה לתיעוד ב"ניהול מיגרציות" למעלה שמתאר שרשרת ליניארית עם head יחיד; דווח כמשימת רקע נפרדת (spawn_task) לצורך תיקון עם מיגרציית merge, ולא טופל כחלק ממשימה זו.

[x] סיווג אוטומטי בטופס הדיווח — הוספת כפתור "נתח בעזרת AI" בטופס דיווח אירוע שממלא אוטומטית חומרה, סוג והערכת נזק על בסיס הטקסט באמצעות קריאה ל-/api/ai/classify-incident.
✅ כבר היה ממומש בפועל — לא נדרש קוד חדש, רק תיקון תיעוד: frontend/src/pages/IncidentReport.tsx (שלב 3 באשף, "תיאור האירוע") כולל כבר כפתור "נתח עם AI" (AutoAwesomeIcon) שקורא ל-classifyIncident (POST /api/ai/classify-incident) על טקסט התיאור, וב-onSuccess ממלא אוטומטית hazardType/severity/impact/loss וב-business_interruption אם ה-AI מזהה זאת כסביר — בדיוק ההתנהגות שהמשימה מבקשת. מוצג גם קופסת "ניתוח AI" עם רמת ביטחון ונימוק, וטיפול שגיאה מנומס (classifyMutation.isError) שמאפשר להמשיך למלא ידנית אם קריאת ה-AI נכשלת. מומש כבר בקומיט הבסיס המקורי של הפרויקט (8f1398e, "Build RMIS: full-stack risk management system with AI") — פשוט לא סומן ב-TODO_SPEC.md. אומת מול הקוד הקיים בלבד (git log על הקובץ, קריאת המימוש), ללא branch/מיזוג נדרשים.

[x] זיהוי נכס מבוסס GPS — מימוש לחצן "זהה מיקום" שמפעיל Browser Geolocation API ובוחר אוטומטית את הנכס הקרוב ביותר לזירת האירוע.
✅ בוצע ב-branch feature/gps-auto-select-nearest-property: הלחצן "אתר את מיקומי והצע נכסים קרובים" (frontend/src/pages/IncidentReport.tsx, hooks/useGeolocation.ts) כבר היה קיים מקודם ומפעיל את Browser Geolocation API ומציג רשימת נכסים קרובים (עד 5, ברדיוס 15 ק"מ, ממוינים לפי מרחק) — אך "בוחר אוטומטית" מהמשימה לא היה ממומש בפועל: המשתמש היה צריך ללחוץ ידנית על אחד ה-chips. נוסף useEffect חדש (מפתח על geo.coords/properties, לא על nearbyProperties הנגזר-מחדש בכל render כדי למנוע לולאה) שבוחר אוטומטית את הנכס הראשון (הקרוב ביותר) ברשימה לתוך שדה ה-Autocomplete בכל פעם שמתקבל GPS fix חדש. כדי לא לדרוס בחירה ידנית של המשתמש, נוסף ref (`autoSelectedPropertyId`) שעוקב אחרי מה שנבחר אוטומטית — ה-effect מעדכן את הבחירה רק אם עדיין לא נבחר נכס, או אם הבחירה הנוכחית היא עצמה בחירה אוטומטית קודמת (כלומר: fix חדש יכול לעדכן בחירה אוטומטית קודמת, אבל לעולם לא ידרוס בחירה ידנית מפורשת דרך ה-Autocomplete או לחיצה ידנית על chip אחר). **אומת חזותית בדפדפן**: geolocation אמיתי חסום בסביבת ה-sandbox (הרשאה נדחית כברירת מחדל) — אומת ע"י מוק זמני של `navigator.geolocation.getCurrentPosition` (JS ישיר בקונסולה, לא שינוי קוד) עם קואורדינטות זהות לנכס קיים; הנכס הקרוב ביותר (מרחק 0.0 ק"מ) נבחר אוטומטית לתוך שדה ה-Autocomplete ללא כל אינטראקציה נוספת. `npx tsc --noEmit` נקי (מלבד שגיאת stylis הקיימת מראש שאינה קשורה).

[x] סנכרון Offline מלא — הרחבת תור הסנכרון (syncQueue.ts) לתמיכה בעריכת טיוטות, הגשה סופית והעלאת מדיה במצב ללא אינטרנט.
✅ בוצע ב-branch feature/offline-sync-drafts-media: syncQueue.ts תמך עד כה רק בסוג פעולה אחד — "create" (הגשה חדשה מלאה + קבצים מצורפים, כולם ביחד). הורחב ל-4 סוגי פעולה (`QueuedOperation` union): **create** (ללא שינוי — הגשה סופית חדשה, או שמירת טיוטה ראשונה שעדיין אין לה מזהה בשרת), **draft-update** (עריכת טיוטה קיימת שכבר יש לה מזהה — PATCH), **draft-submit** (סיום/הגשת טיוטה קיימת — PATCH+submit), **media** (העלאת קבצים לאירוע שכבר קיים בשרת). **באג אמיתי שתוקן אגב כך:** הקוד הישן ב-submitMutation, כשניסיון ההגשה המקוונת של טיוטה קיימת (draftId לא-null) נכשל בגלל ניתוק, נפל תמיד ל"צור אירוע חדש" — מה שהיה יוצר כפילות (גם הטיוטה המקורית וגם אירוע-הגשה כפול) ברגע הסנכרון. כעת enqueueDraftSubmit שומר את אותו draftId ומסיים את אותה טיוטה בפועל, ללא כפילות. גם saveDraftMutation (שמירת טיוטה) לא הייתה לה בכלל נפילה למצב לא-מקוון קודם — נוספה, עם הבחנה בין טיוטה חדשה (אין עדיין מזהה שרת → נשמרת כ-create) לטיוטה קיימת (יש מזהה → draft-update). וגם: קבצים שנכשלו בהעלאה אחרי שהדיווח עצמו כבר נשלח בהצלחה (למשל ניתוק באמצע ההעלאה) נשמרים כעת ב-enqueueMediaUpload במקום להיזרק. **היקף מכוון, מתועד ב-docstring של syncQueue.ts:** טיוטה שנשמרת בפעם הראשונה במצב לא-מקוון (עדיין אין לה מזהה שרת) לא ניתנת "לחידוש" ממכשיר/הפעלה אחרת עד שהסנכרון מסתיים בפועל ומקבלת מזהה אמיתי — מתועד למשתמש בהודעת draftNotice הרלוונטית. **אימות:** בדיקת דפדפן חיה (Browser pane click-driven) הייתה לא-אמינה בסביבה זו (flakiness בין ריענוני עמוד/HMR/מצב הכפתור, לא נמצא באג אמיתי בקוד עצמו) — הבדיקה המכרעת בוצעה בקריאה ישירה למודולים המקומפלים בפועל בדפדפן (דרך `import()` דינמי ל-/src/api/client.ts ו-/src/offline/syncQueue.ts, אותם מודולים בדיוק שה-React component טוען): 1) `updateDraftIncident` מול שרת מנותק נכשל תוך 26ms כצפוי, 2) `enqueueDraftSubmit`/`enqueueMediaUpload` נשמרים ב-IndexedDB תוך מילישניות, 3) לאחר החזרת השרת, `trySync()` עיבד את שתי הפעולות בהצלחה (2/2), ואומת ישירות מול ה-API ש-הטיוטה הקיימת (INC-2026-017) עברה בפועל מ-is_draft:true ל-is_draft:false עם התוכן המעודכן — **ללא יצירת אירוע כפול**, בדיוק התיקון המיועד. `npx tsc --noEmit` נקי, 132/132 טסטים בבקאנד עוברים (לא נגעתי בבקאנד). **תגלית אגבית:** בתהליך האימות התגלה גם ש-DB המקומי (LocalDB) לא היה מעודכן לראש שרשרת המיגרציות (ר' דיווח קודם על שני heads מפוצלים) — טופל זמנית מקומית (alembic upgrade heads) כדי לאפשר את בדיקת הדפדפן; התיקון הקבוע של שרשרת המיגרציות עצמה כבר דווח כמשימת רקע נפרדת ולא טופל כאן.

[x] חילוץ EXIF ממדיה — חילוץ והצגת מטא-דאטה של מיקום וזמן מתמונות המועלות דרך טופס דיווח הנזק.
✅ בוצע ב-branch feature/exif-media-metadata: נוסף utility חדש frontend/src/utils/exif.ts (`extractExifSummary(file)`) שמחלץ קואורדינטות GPS ותאריך/שעת צילום (EXIF DateTimeOriginal/CreateDate) מתמונות שהועלו — פועל אך ורק בצד הלקוח (הקובץ לא יוצא מהדפדפן בשלב הזה), באמצעות ספריית `exifr` (נוספה כתלות חדשה ל-package.json). MediaUploader.tsx (הרכיב המשותף שמשמש את טופס דיווח האירוע) הורחב: לכל תמונה שנוספת מופעלת חילוץ EXIF אסינכרוני (keyed ב-Map לפי הפניית File עצמה, כדי לא לחלץ מחדש קבצים שכבר טופלו), והתוצאה מוצגת כ-caption קטן מתחת לשם הקובץ בכרטיסיית התצוגה המקדימה — שעת/תאריך צילום (עם אייקון שעון) וקואורדינטות (עם אייקון מיקום), כל אחד מוצג רק אם קיים בפועל. קבצים שאינם תמונה (PDF/וידאו) או תמונות בלי EXIF שמיש (צילומי מסך, PNG, תמונות שנשמרו-מחדש, טלפונים שמסירים GPS כברירת מחדל) פשוט לא מציגים caption — מצב תקין וצפוי, לא שגיאה. **היקף מכוון:** לא בוצעה אינטגרציה עם הבחירה האוטומטית של נכס לפי GPS ([[GPS זיהוי נכס]]) — המשימה מבקשת "חילוץ והצגה" בלבד; קישור בין מיקום-תמונה למיקום-נכס יישאר להרחבה עתידית נפרדת אם תידרש. **באג שנתפס ותוקן באימות:** הניסיון הראשון להשתמש באופציית `pick` של exifr יחד עם `gps: true` גרם לכך שקואורדינטות GPS תמיד חזרו null (למרות ש-DateTimeOriginal עבד תקין) — `pick` מסנן את תגי הרכיב הגולמיים (GPSLatitude/GPSLongitudeRef) לפני ש-exifr מחשב מהם את הערכים הנוחים latitude/longitude, כך שה-pick "דורס" את עצמו. תוקן ע"י בקשת ה-IFDs gps+exif במלואם והוצאת השדות הרצויים ידנית מהתוצאה. **אימות:** נוצרה תמונת JPEG אמיתית עם EXIF תקין (Pillow, בקאנד venv) הכוללת GPS (32.0917°N, 34.8042°E) ו-DateTimeOriginal — הועלתה זמנית ל-frontend/public ונבדקה בקריאה ישירה למודול הרץ בפועל בדפדפן (`import('/src/utils/exif.ts')`, אותו מודול ש-MediaUploader טוען), כמתודולוגיית האימות שהוכחה אמינה יותר מקליקים ב-UI במשימה הקודמת: הוחזרו latitude/longitude/capturedAt מדויקים. נבדקו גם מקרי קצה: PNG בלי EXIF (מחזיר null בכל השדות, ללא caption), קובץ PDF (מחזיר null כולל — לא תמונה כלל). קבצי הבדיקה הזמניים (public/__exif_test.jpg, utils/__exif_debug.ts) נמחקו לפני ה-commit. `npx tsc --noEmit -p tsconfig.json` נקי (מלבד שגיאת stylis הקיימת מראש שאינה קשורה), 132/132 טסטים בבקאנד עוברים (לא נגעתי בבקאנד).

5. Frontend — מסכים ודשבורדים חסרים
[x] ניהול משתמשים והרשאות (Users.tsx / Roles.tsx) — פיתוח מסכי הניהול ל-ADMIN, כולל הוספתם לתפריט הניווט (NAV).
✅ בוצע ב-branch feature/admin-users-roles-screens: **Users.tsx** — מסך ניהול משתמשים (טבלה: שם/אימייל/תפקיד/סטטוס/תאריך יצירה, כפתור "משתמש חדש", עריכה בכל שורה) עם UserDialog.tsx (יצירה/עריכה — כולל שינוי תפקיד, איפוס סיסמה אופציונלי, הפעלה/השבתה) הבנוי על POST/PATCH /api/users הקיימים. **Roles.tsx** — מסך קטלוג ההרשאות (Role_Permissions: תפקיד/מפתח הרשאה/תיאור, סינון לפי תפקיד, הוספה/עריכה/מחיקה) עם RolePermissionDialog.tsx, הבנוי על ה-CRUD הקיים ב-routers/role_permissions.py — כולל הבהרה מפורשת במסך עצמו שהקטלוג הזה תיעודי בלבד ואינו האכיפה בפועל (require_roles בכל router), תואם לתיעוד הקיים ב-role_permissions.py. שני המסכים ADMIN-בלבד (guard בצד ה-client כמו AuditLog.tsx, ותלויים ב-endpoints ADMIN-only בצד השרת), ונוספו לתפריט הניווט ב-Layout.tsx (roles: ["ADMIN"], כמו audit-log). **תוספת בקאנד נדרשת:** ה-endpoint הפתוח הקיים GET /api/users מכוון להיות מינימלי (user_id/full_name/role בלבד — משמש כ-picker גם למשתמשים לא-ADMIN במסכים אחרים), ולכן לא התאים לטבלת ניהול מלאה. נוסף endpoint חדש נפרד GET /api/users/admin (ADMIN-only, מחזיר UserAdminOut — כולל email/is_active/created_at) מבלי לגעת בהתנהגות ה-endpoint הפתוח הקיים ובלי להשפיע על אף קורא קיים שלו. נוספו 7 טסטים חדשים (test_api_users_admin.py: auth נדרש, 403 ללא ADMIN, שדות מלאים מוחזרים) — 13/13 עוברים בקובץ, 135/135 בבקאנד כולו. **אימות בדפדפן:** לאחר login כ-ADMIN אמיתי (admin@company.co.il), שני המסכים נטענו בהצלחה מול הבקאנד האמיתי עם נתוני ה-seed (טבלת /users הציגה 9 משתמשים עם כל השדות; טבלת /roles הציגה 25 הרשאות מקוטלגות לפי 7 תפקידים) — אומת גם ש-/users ו-/roles מופיעים בתפריט הניווט בפועל (querySelectorAll על אלמנטי `<a href>` בעמוד). **מתודולוגיית האימות למוטציות (יצירה/עדכון/מחיקה)**: בהמשך למסקנת המשימה הקודמת שקליקים ב-UI על דיאלוגים לא היו אמינים בסביבת ה-sandbox, המוטציות נבדקו ישירות דרך `import('/src/api/client.ts')` בקונסולת הדפדפן (אותו מודול קליינט בדיוק שה-dialogs טוענים): נוצר משתמש בדיקה → עודכן (תפקיד+השבתה) → אומת ברשימה שהעדכון השתקף; נוצרה הרשאת בדיקה → עודכנה → נמחקה → אומת שנעלמה מהרשימה. `npx tsc --noEmit -p tsconfig.json` נקי (מלבד שגיאת stylis הקיימת מראש).

[x] ניהול נכסים ותיק נכס (PropertyDetail.tsx) — טופס להוספה/עריכה של נכס, ומסך Drill-down מלא המציג את הפוליסה, המסמכים וסקר הסיכונים של נכס בודד.
✅ בוצע ב-branch feature/property-detail-drilldown: **PropertyDialog.tsx** — טופס הוספה/עריכה של נכס (קוד, שם, כתובת, אזור, קואורדינטות, סוג נכס, שווי כינון/ערך בספרים, מנהל אחראי), הבנוי על ה-CRUD הקיים במלואו ב-routers/properties.py (POST/PUT/DELETE כבר היו קיימים בבקאנד — לא נדרש שינוי שרת כלל במשימה הזו). שדה "אזור" קבוע לשלושת האזורים שב-seed.py (מרכז/צפון/דרום, עם מיפוי ל-region_id תואם) — אין API ל-Regions עצמם, כך שהרחבתו נשארת מחוץ להיקף. **PropertyDetail.tsx** (route חדש `/properties/:id`) — מסך Drill-down מלא: כרטיס פרטי נכס, כרטיס פוליסה פעילה (ממנוף את ה-active_policy שכבר מחושב ב-GET /api/properties/{id}), כרטיס סקר סיכונים (**תצוגה בלבד** — עריכת הסקר בפועל היא המשימה הבאה המפורשת ב-TODO_SPEC.md, RiskSurveyDialog.tsx, וכרטיס זה נכתב כך שיתחבר אליה כשתמומש), וכרטיס מסמכים מצורף לנכס (רשימה + העלאה, על בסיס fetchDocumentsForEntity/uploadDocument הקיימים, אותו two-step signed-URL contract שכבר בשימוש ב-IncidentDetail.tsx/Documents.tsx). נוסף גם כפתור "השבתה" (DELETE הקיים, soft-delete) לשלמות ניהול המחזור — לא התבקש במפורש אך זמין ללא עלות שכן ה-endpoint כבר קיים. Properties.tsx (רשימת הנכסים) עודכנה: כרטיסי הנכס עצמם לחיצים (CardActionArea → ניווט ל-drill-down), נוסף כפתור עריכה על כל כרטיס (stopPropagation כדי לא לנווט בטעות) וכפתור "נכס חדש" גלובלי — כל אלה מוסתרים למשתמשים ללא הרשאת כתיבה (RISK_MANAGER/PROPERTY_MANAGER/ADMIN, תואם ל-_PROPERTIES_WRITE_ROLES בבקאנד). **אימות בדפדפן:** login אמיתי כ-ADMIN מול הבקאנד האמיתי — רשימת 15 הנכסים נטענה עם כפתור "נכס חדש"; ניווט ישיר ל-/properties/1 הציג תיק נכס מלא ותקין (פוליסה POL-2026-CENTRAL, סקר סיכונים עם כל השדות, מסמך "דוח סקר" קיים מה-seed). מוטציות (יצירה→עדכון→שליפה→השבתה) אומתו ישירות דרך `api/client.ts` בקונסולה, תואם למתודולוגיה שהוכחה אמינה במשימות הקודמות — round-trip מלא כולל אימות ש-is_active הפך ל-false לאחר השבתה. `npx tsc --noEmit -p tsconfig.json` נקי, 135/135 טסטים בבקאנד עוברים (לא נגעתי בבקאנד).

[x] מסך סקר סיכונים (RiskSurveyDialog.tsx) — ממשק להזנת ציוני הצפה, שריפה, רעידת אדמה ואמצעי מיגון.
✅ בוצע ב-branch feature/risk-survey-dialog: RiskSurveyDialog.tsx — דיאלוג ליצירה/עדכון של סקר הסיכונים היחיד שיכול להיות לכל נכס (Asset_Risk_Profiles הוא 1:1 עם Property; POST אם אין סקר קיים, PUT אם יש — בדיוק כמו שה-API הקיים ב-routers/risk_profiles.py כבר מתעד, לא נדרש שינוי בקאנד). ממשק: Slider (1-5, עם marks) לכל אחד משלושת ציוני הסיכון (הצפה/שריפה/רעידת אדמה), שדה MFL, Switch למתזים ("אמצעי מיגון"), הערות חופשיות, ותאריך סקר. מחובר לכרטיס "סקר סיכונים" ב-PropertyDetail.tsx (המשימה הקודמת) — כפתור "סקר חדש" מוצג אם אין עדיין risk_profile לנכס, "עדכון סקר" אם יש, שניהם ADMIN/RISK_MANAGER/PROPERTY_MANAGER בלבד (אותו canWrite guard כמו שאר הפעולות בדף). נוספו client.ts types (RiskProfileCreate/Update) ופונקציות createRiskProfile/updateRiskProfile. **אימות בדפדפן:** login אמיתי כ-ADMIN — round-trip מלא אומת דרך `api/client.ts`: נוצר נכס בדיקה חדש ללא סקר → אומת ש-risk_profile הוא null → נוצר סקר (POST, ציון הצפה 4) → אומת שהוחזר ומשתקף ב-GET הנכס → עודכן (PUT, ציון הצפה 5 + הערה חדשה) → אומת שהעדכון השתקף. אומת גם ויזואלית: לחיצה על "עדכון סקר" בנכס קיים עם סקר אמיתי מה-seed פתחה את הדיאלוג עם כל השדות ממולאים נכון מראש (סליידרים בערכים 2/2/3 התואמים בדיוק לנתוני הנכס, MFL וההערה תואמים). `npx tsc --noEmit -p tsconfig.json` נקי, 135/135 טסטים בבקאנד עוברים (לא נגעתי בבקאנד).

[x] דשבורד מותאם לשטח (Field Worker) — יצירת תצוגה מצומצמת שמסתירה נתונים פיננסיים (MFL, VaR) ומציגה רק סטטוס אירועים וסנכרון רשת.
✅ בוצע ב-branch feature/field-worker-dashboard: FieldWorkerDashboard.tsx — דף חדש עם כרטיס חיבור/סנכרון גדול (מבוסס useOnlineStatus + subscribeToSyncQueue/trySync הקיימים מ-syncQueue.ts, אותם בדיוק ששימשו את הצ'יפ הקטן ב-Layout.tsx), כפתור "דיווח אירוע חדש" בולט, ורשימת 12 האירועים האחרונים (קוד, סטטוס, חומרה, סוג מפגע, "טיוטה" אם רלוונטי) — כל שורה מקושרת לתיק האירוע. **ללא נתון כספי אחד** — לא TIV/MFL, לא Loss Ratio, לא VaR/מטריצת סיכונים, לא Cashflow. Dashboard.tsx (route "/") הופך למתג: FIELD_WORKER מקבל FieldWorkerDashboard, כל תפקיד אחר ממשיך לקבל את דשבורד המנהלים המקורי ללא שינוי (עטיפה בלבד — לא נגעתי בלוגיקת השליפה/החישוב של הדשבורד הקיים). **הרציונל לגישה הזו (מתג דפים שלם ולא הסתרת widgets בודדים):** ל-FIELD_WORKER כבר אין גישה בפועל בצד השרת לנתונים הפיננסיים שהדשבורד הקיים בנוי סביבם (routers/analytics.py's _FINANCIAL_READ_ROLES, מהמשימה הקודמת של אכיפת RBAC על GET) — טעינת הדשבורד הישן כ-FIELD_WORKER הייתה מייצרת שגיאות 403 שקטות על כל widget, לא רק "מציגה נתון שאסור". **תגלית אגבית (דווחה כמשימת רקע נפרדת, לא תוקנה כאן):** תוך כדי אימות דשבורד המנהלים לתפקידים אחרים התגלה באג אמיתי ולא קשור — GET /api/analytics/loss-ratio-trend מחזיר 500 (UnicodeEncodeError בפענוח EncryptedString) כי seed.py כותב adjuster_name/reference_number ישירות דרך pyodcb גולמי (עוקף לגמרי את הצפנת ה-ORM על העמודות המוצפנות האלה), כך שנתוני ה-seed נשמרים כטקסט גלוי אך ה-ORM מנסה תמיד לפענח אותם כ-ciphertext. זה גורם ל-KPIs (TIV/MFL/Loss Ratio) בדשבורד המנהלים להיראות כ-0 באופן שקט. דווח דרך spawn_task (task_70be218c) — לא תוקן כאן, אינו קשור לשינוי הזה (אומת ש-Dashboard.tsx לא נגע בלוגיקת השליפה). **אימות בדפדפן:** login אמיתי כ-FIELD_WORKER (עומר בר) הציג את הדשבורד המצומצם בלבד עם 12 אירועים אמיתיים מה-seed ומצב "מחובר לרשת / אין דיווחים ממתינים"; login כ-ADMIN הציג את דשבורד המנהלים המלא (מפה, מטריצת סיכונים, גרפים) ללא שינוי במבנה. `npx tsc --noEmit -p tsconfig.json` נקי, 135/135 טסטים בבקאנד עוברים (לא נגעתי בבקאנד).

[x] מסכי אינטגרציה והתראות (Notifications.tsx / Integrations.tsx) — ממשקים לצפייה ביומני התראות וסנכרון ידני מול מערכות ERP/GIS.
✅ בוצע ב-branch feature/notifications-integrations-screens: **Notifications.tsx** — תצוגה מקדימה (מי יקבל התראה כרגע, GET /preview), כפתור "שליחה עכשיו" (POST /dispatch, עם אישור), יומן שליחות (GET /log), וניהול נמענים (GET/POST/PATCH/DELETE /recipients, כתיבה ADMIN-בלבד) עם NotificationRecipientDialog.tsx חדש. **Integrations.tsx** — ארבעה אזורים תואמים לארבע האינטגרציות המדומות הקיימות: ERP (ערכי ספרים + כפתור "רישום קבלות תביעות"), GIS (שכבות סיכון מול הסקר הפנימי), מזג אוויר (פתוח לכל תפקיד מחובר — תואם למדיניות השרת), ומדדים כלכליים + הצעות עדכון שווי כינון. שני המסכים ADMIN/הרשאות תואמות בדיוק ל-_NOTIFICATIONS_ROLES/_ERP_ROLES/_GIS_ROLES/_ECONOMICS_ROLES הקיימים ב-routers/notifications.py ו-routers/integrations.py — לא נדרש שינוי הרשאות, כל ה-endpoints כבר היו קיימים ומיושמים במלואם (כולל סימולציית ה-ERP/GIS/מזג אוויר/מדדים, ר' התיעוד הקיים ב-app/integrations/*.py). נוספו לתפריט הניווט (Layout.tsx) — "התראות" לתפקידי RISK_MANAGER/CFO/ADMIN, "אינטגרציות" ללא הגבלת roles (כי אזור מזג האוויר פתוח לכולם, וכל אזור אחר מסתתר בעצמו לפי תפקיד). **שני באגים אמיתיים ולא-קשורים נמצאו ותוקנו/דווחו תוך כדי אימות:** (1) **תוקן כאן** — `services/notifications.py`'s `_load_recipients` השתמש ב-`.is_(True)` להשוואת עמודת is_active, שמתקמפל ל-SQL הבלתי-תקין `WHERE ... IS 1` על dialect ה-mssql (בניגוד ל-`== True` שמתקמפל נכון ל-`= 1`, כפי שכל שאר הקוד בבסיס הקוד כבר עושה, למשל properties.py) — גרם ל-500 על כל קריאה ל-GET /notifications/preview ברגע שהייתה שורת נמען פעילה בטבלה. תוקן לתבנית `== True  # noqa: E712` התואמת לשאר בסיס הקוד. הבאג לא נתפס ב-135 הטסטים הקיימים כי סוויטת הבדיקות רצה על SQLite בזיכרון, שסובלנית לתחביר "IS 1" — זו מגבלה ידועה ומתועדת של סוויטת הבדיקות (SQLite לעומת mssql אמיתי), לא נוסף טסט רגרסיה חדש כי SQLite לא יכול לתפוס את המחלקה הזו של באג ממילא; אומת ידנית מול ה-LocalDB האמיתי (ר' אימות למטה). (2) **דווח בלבד, לא תוקן — אותה בעיה שכבר דווחה במשימה הקודמת (task_70be218c)**: כפתור "רישום קבלות תביעות" (ERP) נכשל באותו UnicodeEncodeError בפענוח Claim_Payments.reference_number (עמודה מוצפנת ש-seed.py כתב אליה טקסט גלוי ישירות דרך pyodbc, עוקף את הצפנת ה-ORM) — לא תוקן שוב כאן, מכוסה כבר ע"י הדיווח הקיים; ה-UI מציג כשל בצורה מבוקרת (Alert אדום דרך `postReceiptsMutation.isError`, לא קריסת עמוד) כפי שנבדק בקוד ואומת התנהגותית מול ה-API. **אימות בדפדפן:** login אמיתי כ-ADMIN. Notifications.tsx: לפני התיקון — GET /preview החזיר 500 ברגע שהיה נמען פעיל בטבלה; אחרי התיקון — round-trip מלא אומת דרך `api/client.ts`: נוצר נמען → preview הראה 4 התראות מנותבות אליו (2 סוגי חומרה × 2 ערוצים) → dispatch שלח 6 התראות בפועל (עם fallback ל-DEFAULT_RECIPIENTS אחרי מחיקת הנמען) → יומן השליחות אימת 6 רשומות עם status="simulated". גם אומת חזותית ב-UI עצמו (get_page_text) שכל הטבלאות מציגות את הנתונים הנכונים. Integrations.tsx: כל ארבעת האזורים נטענו עם נתונים אמיתיים (15 נכסים ב-ERP/GIS, 3 התראות מזג אוויר, מדדים כלכליים + 15 הצעות עדכון שווי). `npx tsc --noEmit -p tsconfig.json` נקי, 135/135 טסטים בבקאנד עוברים (כולל אחרי תיקון notifications.py).

6. אנליטיקה חזותית וחישובים אינטראקטיביים — חדש
[x] סימולציית VaR אינטראקטיבית (Simulation.tsx) — יצירת מסך המציג היסטוגרמה של התפלגות הנזק מתוך פלט מונטה-קרלו, עם סליידרים לאיטרציות ושנות אופק.
✅ בוצע ב-branch feature/interactive-var-simulation: מסך Simulation.tsx וההיסטוגרמה (SimulationDistributionChart.tsx, BarChart עם קווי ייחוס ל-VaR95/VaR99) כבר היו קיימים ומיושמים במלואם — הפער היחיד מול הדרישה היה שבחירת מספר ההרצות/אופק השנים נעשתה דרך TextField (select + number input) ולא דרך "סליידרים" כמבוקש במפורש. הוחלפו שני הבקרים ל-MUI `Slider` אמיתיים: **מספר הרצות** (1,000–100,000, צעד 1,000, marks ב-1K/10K/25K/50K/100K) ו-**אופק בשנים** (1–50, צעד 1, marks ב-1/5/10/25/50) — הטווחים תואמים בדיוק ל-MAX_ITERATIONS/MAX_HORIZON_YEARS הקיימים כבר ב-routers/simulation.py (gt=0/le=max על שני ה-query params), כך שהסליידר לא יכול לבקש ערך שהשרת היה דוחה ב-422. שאר הזרימה (בחירת היקף Portfolio/נכס בודד, הרצה, KPI cards, היסטוגרמה) נשארה ללא שינוי — לא נדרש שינוי בקאנד כלל. **אימות בדפדפן:** login אמיתי כ-ADMIN — שני הסליידרים מוצגים עם ה-marks והערכים הנכונים (ברירת מחדל 10,000 הרצות / שנה 1), לחיצה על "הרץ סימולציה" הריצה קריאה אמיתית ל-API והחזירה תוצאות מלאות (הפסד שנתי צפוי, VaR95/99, היסטוגרמה עם 20 buckets וקווי ייחוס) עבור 15 נכסי התיק. `npx tsc --noEmit -p tsconfig.json` נקי, 135/135 טסטים בבקאנד עוברים (לא נגעתי בבקאנד).

[x] מחשבון השתתפות עצמית (RetentionCalculator.tsx) — פיתוח כלי הממליץ האם לספוג נזק (ABSORB) או לתבוע (CLAIM), על בסיס שווי האירוע והתייקרות הפרמיה הצפויה.
✅ כבר היה ממומש בפועל — לא נדרש קוד חדש, רק תיקון תיעוד (אותו דפוס כמו "סיווג אוטומטי בטופס הדיווח" קודם לכן): `frontend/src/components/RetentionCalculator.tsx` + `frontend/src/pages/Retention.tsx` (route `/retention`, קישור ניווט "השתתפות עצמית") כבר מומשו במלואם ב-branch `feature/retention-calculator` וכבר ממוזגים ל-`main` — בורר נכס מבוטח, שדה נזק משוער עם ברירת מחדל מ-MFL, כפתור "חשב המלצה", Chip צבעוני ABSORB/CLAIM, ושתי כרטיסיות השוואה. מפורט בהרחבה (כולל אימות מלא מול RiskDB אמיתי, כולל מקרה קצה של נזק מתחת להשתתפות העצמית) בסעיף המפורט למעלה במסמך זה, תחת "מסך/רכיב אופטימיזציית השתתפות עצמית". פשוט לא היה מסומן ב-item המקוצר הזה בסעיף 5. אומת כאן רק מול הקוד הקיים (`ls`, `grep` על הראוטים) + `npx tsc --noEmit -p tsconfig.json` נקי — ללא branch/מיזוג חדשים נדרשים (ה-branch `feature/retention-calculator` הישן, שכבר ממוזג, נמחק).

[x] שדרוג מפת הסיכונים (Risk Map) — הוספת Layer Control למפה להדלקה/כיבוי של שכבות: סיכות לפי סטטוס, אזורי הצפה וסימון אשכולות חשיפה (Clusters). הוספת כפתור מעבר לתיק הנכס מתוך הפופ-אפ.
✅ בוצע ב-branch feature/risk-map-layer-control: ה-Layer Control עצמו (מתגי הדלקה/כיבוי לנכסים/אירועים פעילים/אזורי הצפה/אזורי שריפה/אשכולות חשיפה TIV) כבר היה קיים ומיושם במלואו ב-RiskMap.tsx — הפער היחיד מול הדרישה היה "כפתור מעבר לתיק הנכס מתוך הפופ-אפ", שלא היה קיים (הפופ-אפ של נכס הציג מידע תצוגתי בלבד: שווי כינון, אירועים פתוחים, מנהל אחראי, פוליסה פעילה — ללא כל דרך לנווט הלאה). נוסף כפתור "מעבר לתיק הנכס" בתחתית ה-Popup של כל CircleMarker של נכס, שמנווט (`useNavigate` מ-react-router-dom, זמין כי RiskMap תמיד מרונדר בתוך ה-BrowserRouter הגלובלי מ-main.tsx) ל-`/properties/{property_id}` — מסך תיק הנכס המלא שנבנה במשימה קודמת בסשן זה (PropertyDetail.tsx). לא נדרש שינוי בקאנד. **אימות בדפדפן:** login אמיתי כ-ADMIN, מפת הדשבורד האמיתית עם 15 נכסים ואירועים אמיתיים. קליק על ה-marker של "מרלו\"ג מודיעין" פתח את הפופ-אפ הנכון (עם כל הפרטים) וכפתור "מעבר לתיק הנכס" בתוכו ניווט בהצלחה ל-`/properties/1` — ואומת שדף היעד הציג את הנכס הנכון בדיוק (PRP-001, "מרלו\"ג מודיעין"). בבדיקה הראשונית, לולאת קליקים מהירה על כל סימוני המפה יצרה בטעות פופ-אפים ישנים שנשארו ב-DOM והובילה לניווט לנכס לא-נכון — אומת שזו הייתה חפיפת state בין קליקים רצופים מהירים מדי בבדיקה עצמה (לא באג בקוד): קליק בודד נקי על marker יחיד, המתנה לפופ-אפ יחיד ב-DOM, ואז קליק על הכפתור בתוכו — נותן תוצאה נכונה ועקבית בכל פעם. `npx tsc --noEmit -p tsconfig.json` נקי, 135/135 טסטים בבקאנד עוברים (לא נגעתי בבקאנד).

[x] שילוב מזג אוויר — שילוב התראות מזג אוויר אזוריות בדשבורד המרכזי באמצעות רכיב הבאנר.
✅ בוצע ב-branch feature/dashboard-weather-banner: AlertsBanner.tsx (רכיב הבאנר הקיים, שהציג עד כה רק התראות סף פיננסיות/תפעוליות מ-GET /analytics/alerts) הורחב לקבל גם `weatherAlerts?: WeatherAlert[]` אופציונלי ולרנדר אותן באותו סגנון חזותי בדיוק (Alert + AlertTitle, אייקון סופה ייעודי, מיפוי severity ADVISORY/WATCH/WARNING ל-info/warning/error). חובר ל-Dashboard.tsx (דשבורד המנהלים) עם `fetchWeatherAlerts()` חדש (משתמש ב-endpoint הקיים GET /api/integrations/weather/alerts, הפתוח לכל תפקיד מחובר) לצד ה-alerts הפיננסיים הקיימים. **הרחבה נוספת, מעבר למבוקש במפורש אך זולה וטבעית**: אותו רכיב-באנר חובר גם ל-FieldWorkerDashboard.tsx (מהמשימה כמה צעדים קודם) — שכן מזג אוויר פתוח לכל תפקיד בצד השרת, ועובד שטח בפועל צריך אזהרת סופה/הצפה לא פחות ממנהל סיכונים. **באג קטן שנתפס ותוקן אגב כך**: `Integrations.tsx` (מהמשימה הקודמת) הגדיר `WEATHER_SEVERITY_COLOR` עם מפתחות `low/moderate/high/extreme` — אך ה-severity האמיתי המוחזר מ-`app/integrations/weather.py`'s `_SEVERITIES` הוא `ADVISORY/WATCH/WARNING`, כך שהצ'יפים בטבלת ה-Integrations תמיד נפלו ל-`default` (אפור) ללא קשר לחומרה בפועל — תוקן שם גם המיפוי (Hebrew labels + צבעים נכונים) וגם label לסוג ההתראה (STORM/FLOOD_WARNING/HEATWAVE). לא נדרש שינוי בקאנד. **אימות בדפדפן:** login אמיתי כ-ADMIN — הדשבורד הראשי הציג 3 באנרי מזג אוויר עם התוויות הנכונות (אזהרת הצפה/סופה/גל חום, רמות אזהרה/מייעצת) לצד 2 באנרי הסף הפיננסיים הקיימים. login כ-FIELD_WORKER (עומר בר) — דשבורד השטח הציג את אותם 3 באנרי מזג אוויר בראש העמוד, ללא כל נתון פיננסי. עמוד Integrations.tsx אומת שהוא כעת מציג תוויות/צבעים נכונים לאותן 3 התראות. `npx tsc --noEmit -p tsconfig.json` נקי, 135/135 טסטים בבקאנד עוברים (לא נגעתי בבקאנד).

7. אינטגרציות (חיבורים חיצוניים "אמיתיים")
[ ] תקשורת: החלפת הלוגים (Simulated) במימוש מול שירותי Email / SMS (למשל: Twilio / SendGrid).

[ ] אחסון ענן: החלפת שמירת הקבצים המקומית ב-AWS S3 או Azure Blob.

[ ] סנכרון שווי: בניית מחבר פעיל לסנכרון שווי נכסים מול מערכת ERP.

[ ] שכבות מפה: קריאה לשירות Govmap או Mapbox לקבלת מפות הצפה/אקלים אמיתיות במקום חישובים פנימיים.

[ ] הזדהות ארגונית: השלמת זרימת OAuth2 / OIDC מול שירותי Azure AD / Okta למשתמשי המטה.

8. דוחות מודפסים וייצוא (Reports)
[x] ייצוא ל-Excel — הוספת פונקציונליות ייצוא לדוחות חשיפה תקופתיים ודוחות השתתפות עצמית.
✅ בוצע ב-branch feature/excel-export-reports: המערכת כבר כללה תבנית ייצוא Excel עובדת (`exportClaims.ts`, ייצוא תביעות דרך ספריית `xlsx` שכבר הייתה תלות ב-package.json) — אותה תבנית שוכפלה לשני קבצים חדשים: **`exportExposureReport.ts`** (`exportExposureReportToExcel`) — חוברת (workbook) עם גיליון "חשיפה לפי אזור" (TIV/MFL/סה"כ נתבע לפי GET /analytics/exposure-by-region) וגיליון "מגמה רב-שנתית" אופציונלי (מה-multi_year_trends שכבר נשלף בדוח הרגולטורי הקיים), כפתור "ייצוא דוח חשיפה ל-Excel" נוסף לכרטיס "דוח רגולטורי" ב-Reports.tsx. **`exportRetentionReport.ts`** (`exportRetentionReportToExcel`) — חוברת שדה/ערך אחת לכל שורות מודל העלות (השתתפות עצמית, סכום בר-החזר, תשלום מכיס, תוספת פרמיה, עלות כוללת בשתי החלופות, המלצה וחיסכון), כפתור "ייצוא ל-Excel" נוסף לתוצאת RetentionCalculator.tsx (מהמשימה שסומנה קודם בסשן זה כבר-קיימת). לא נדרש שינוי בקאנד. **מגבלת אימות שהתגלתה:** GET /analytics/exposure-by-region נכשל ב-500 מול ה-LocalDB האמיתי — אותה בעיית שורש בדיוק שכבר דווחה כמשימת רקע נפרדת (task_70be218c: seed.py כותב שדות מוצפנים כטקסט גלוי דרך pyodbc גולמי) — כאן התברר שההיקף רחב עוד יותר ממה שדווח במקור: הבאג פוגע כעת גם ב-loss-ratio-trend (דווח קודם), ERP claim-receipts (דווח קודם), **וגם** ב-exposure-by-region (מתגלה כאן) — כל query שנוגע ב-Claims/Claim_Payments דרך ה-ORM. לא דווח שוב (המשימה הקיימת כבר מכסה את התיקון הנדרש — עדכון seed.py + reseed יפתור את כולם יחד). **אימות שבוצע בפועל, לאור המגבלה:** מחשבון ההשתתפות העצמית (RetentionCalculator, שלא תלוי בטבלת Claims כלל — רק בפוליסות, ששדותיהן המוצפנים per_event_limit/specific_deductible כן עובדים תקין) הורץ בהצלחה מול ה-API האמיתי (GET /api/retention/recommendation החזיר תוצאה מלאה ותקינה), ואז `exportRetentionReportToExcel` הופעל ישירות (`import('/src/exportRetentionReport.ts')`) עם הנתון האמיתי הזה — הריצה הושלמה עד שלב הורדת הקובץ (מזוהה ב-monkey-patch על `HTMLAnchorElement.prototype.click`, ללא שגיאה). `exportExposureReportToExcel` (שאי אפשר לבדוק מול הנתון האמיתי בגלל הבאג החוצץ) נבדק באופן זהה עם נתונים סינתטיים תואמי-טיפוס (RegionExposure[]/MultiYearTrend[]) — גם היא הושלמה ללא שגיאה, כולל שני הגיליונות. `npx tsc --noEmit -p tsconfig.json` נקי, 135/135 טסטים בבקאנד עוברים (לא נגעתי בבקאנד).

[x] דוח הפחתת סיכון מודפס (MitigationReportPrintable.tsx) — יצירת תצוגה מותאמת להדפסה של דוח משימות המיטיגציה וחישובי ה-ROI.
✅ בוצע ב-branch feature/mitigation-report-printable: MitigationReportPrintable.tsx — תצוגה off-screen (HTML/inline-styles טהור, לא MUI, בדיוק כמו ExecutiveReportPrintable.tsx הקיים, כדי ש-html2canvas ירנדר בצורה צפויה) הכוללת כותרת, ארבעת ה-KPIs שכבר מחושבים ב-Mitigation.tsx (משימות פתוחות/באיחור, עלות כוללת, חיסכון שנתי, ROI ממוצע) וטבלת משימות מלאה עם עמודות ROI/החזר השקעה — ממוזגת מ-fetchMitigationRoiSummary (נשלף כעת גם ב-Mitigation.tsx) כשזמין, עם נפילה חינניים לשדות ה-ROI הבסיסיים שכבר על ה-MitigationTask עצמו כשאין. חובר לכפתור חדש "ייצוא ל-PDF" ב-Mitigation.tsx, המשתמש ב-`exportElementToPdf` הקיים (אותו מנגנון html2canvas+jsPDF ששימש כבר את דוח ההנהלה ב-Reports.tsx) — ללא צורך בקוד ייצוא PDF חדש. הדוח המיוצא תואם את הרשימה המסוננת המוצגת כרגע על המסך (כולל סינון סטטוס), לא רק את כל המשימות. לא נדרש שינוי בקאנד. **אימות בדפדפן:** login אמיתי כ-ADMIN, עמוד /mitigation הציג את כפתור "ייצוא ל-PDF" החדש לצד 15 משימות אמיתיות ו-4 KPIs. לחיצה על הכפתור הריצה את זרימת ה-PDF המלאה (html2canvas capture + jsPDF) עד סיום — הכפתור חזר למצב רגיל ללא שגיאה ומבלי לקרוס את האפליקציה (הורדת הקובץ עצמה חסומה בסביבת התצוגה המבודדת, כמצופה — לא ניתן לבדוק חזותית את קובץ ה-PDF שנוצר, אך זרימת היצירה עצמה אומתה כתקינה). `npx tsc --noEmit -p tsconfig.json` נקי, 135/135 טסטים בבקאנד עוברים (לא נגעתי בבקאנד).

[x] פילוח חשיפה בדוח הנהלה — שילוב התפלגות החשיפה (TIV/MFL) לפי אזור גיאוגרפי (צפון, מרכז, דרום) בתוך ה-Executive Report הקיים.
✅ בוצע ב-branch feature/exposure-by-region-report: GET /analytics/exposure-by-region כבר היה נשלף ב-Reports.tsx (`exposureByRegion`), אך שימש עד כה **רק** את הטבלה בתוך ExecutiveReportPrintable.tsx (המסמך הבלתי-נראה שנתפס ל-PDF) — לא הייתה שום תצוגה חזותית של הפילוח הזה במסך "דוחות ותובנות AI" עצמו. נוסף רכיב חדש **ExposureByRegionChart.tsx** (BarChart מקובץ של TIV מול MFL לכל אזור, אותה תבנית recharts בדיוק כמו HazardChart.tsx/LossRatioTrendChart.tsx הקיימים) וחובר בתוך כרטיס "דוח רגולטורי" ב-Reports.tsx, מעל טבלת המגמה הרב-שנתית — כך שהפילוח הגיאוגרפי גלוי כעת גם על המסך, לא רק בקובץ ה-PDF המיוצא. לא נדרש שינוי בקאנד. **מגבלת אימות (אותה בעיה שדווחה קודם, task_70be218c):** GET /analytics/exposure-by-region עדיין 500-אה מול ה-LocalDB האמיתי בסביבה זו (אותה שרשרת בעיית הצפנה שכבר מתועדת) — לכן לא ניתן היה לאמת חזותית את הגרף מול נתונים אמיתיים בסביבת הפיתוח הנוכחית. אומת בפועל: (1) עמוד Reports.tsx נטען במלואו וללא קריסה כשה-query הזה נכשל — התנאי `{exposureByRegion.data && ... && (<ExposureByRegionChart/>)}` פשוט לא מרנדר, בהתאם למוסכמת ה-graceful-degradation שכבר קיימת בדף (בדיוק כמו simulation/roiSummary/trends האופציונליים ב-ExecutiveReportPrintable), כל שאר הדף (דוח רגולטורי, KPIs, מגמה רב-שנתית) המשיך לעבוד תקין; (2) הרכיב עצמו נבדק בקוד מול הטיפוס RegionExposure[] הקיים ותואם באופן מלא ל-schema; לאחר שהבאג שדווח יתוקן, הגרף יתמלא אוטומטית ללא כל שינוי נוסף בקוד. `npx tsc --noEmit -p tsconfig.json` נקי, 135/135 טסטים בבקאנד עוברים (לא נגעתי בבקאנד).

9. בדיקות, תיעוד והגשה (Testing & Demo Prep)
[ ] בדיקות Backend — הוספת טסטים למודולים שטרם כוסו (Cashflow, Financials, Compliance, Analytics, Notifications).

[ ] בדיקות הרשאות רגרסיה — יצירת טסטים למערך ה-RBAC על פונקציות הקריאה (GET) כדי לוודא שמשתמשי שטח לא נחשפים למידע פיננסי.

[ ] בדיקות Frontend ו-E2E — הוספת Vitest ו-React Testing Library, ומימוש E2E לבדיקת זרימת שטח: Offline -> Sync -> Incident -> Claim.

[ ] תסריט הדגמה (docs/DEMO_SCRIPT.md) — כתיבת מסמך Walkthrough המנחה את המרצה/הבודק בצעדי שימוש במערכת מקצה לקצה.

[ ] תיעוד ארכיטקטורה (docs/ARCHITECTURE.md + ROLES_MATRIX.md) — עדכון תרשימי ה-ERD בעזרת Mermaid, ויצירת מטריצת הרשאות ברורה כרפרנס לכלל תפקידי המערכת.
