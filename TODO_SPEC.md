# TODO_SPEC.md — משימות פיתוח פתוחות

מסמך זה נגזר מניתוח פערים בין מפרט הדרישות (RMIS) לבין המימוש הקיים.
הסדר הוא סדר בנייה מומלץ: תשתית ובסיס נתונים → שכבת שירותים → API → אבטחה → UI → אינטגרציות → מודלים מתקדמים.

---

## שלב 1 — Database & Data Model

- [x] **הרחבת טבלת הפוליסות**: הוספת `per_event_limit` (גבול אחריות לאירוע בודד), `bi_waiting_period_hours` (תקופת המתנה לאובדן רווחים), `exclusions` (החרגות, JSONB/Text).
  📁 `backend/app/models.py` (Class `InsurancePolicy`) + `backend/sql/schema.sql`
  ✅ בוצע ב-branch `feature/policy-extended-fields`: נוספו שלושת השדות כ-nullable ל-`InsurancePolicy` (`SmallInteger` ל-bi_waiting_period_hours, `UnicodeText` ל-exclusions) ול-`schema.sql`.

- [x] **הרחבת טבלת האירועים**: הוספת `is_draft` (שמירה כטיוטה), `business_interruption_requested` (בקשת כיסוי אובדן רווחים), `area_or_building` (אזור/מבנה בתוך הנכס), `reported_coordinates` (מיקום GPS של המדווח).
  📁 `backend/app/models.py` (Class `Incident`) + `backend/sql/schema.sql`
  ✅ בוצע ב-branch `feature/incident-extended-fields`: `is_draft`/`business_interruption_requested` כ-`BIT NOT NULL DEFAULT 0`, `area_or_building` (`NVARCHAR(150)` nullable), `reported_coordinates` (`NVARCHAR(50)` nullable, מחרוזת "lat,lng").

- [x] **טבלת רזרבות וצפי תזרים**: טבלה חדשה `Claim_Reserves` (claim_id, reserve_amount, expected_payment_date, updated_at) לניהול רזרבות וצפי גבייה.
  📁 `backend/app/models.py` + `backend/sql/schema.sql`
  ✅ בוצע ב-branch `feature/claim-reserves-table`: נוספה `ClaimReserve` (models.py) + טבלת `Claim_Reserves` ב-schema.sql (עם FK ל-Claims, אינדקס על claim_id, ו-relationship דו-כיווני מול `Claim`).

- [x] **טבלת Audit Log**: טבלה חדשה `Audit_Log` (user_id, entity_type, entity_id, action, old_value, new_value, timestamp, ip_address).
  📁 `backend/app/models.py` + `backend/sql/schema.sql`
  ✅ בוצע ב-branch `feature/audit-log-table`: נוספה `AuditLog` (models.py) + טבלת `Audit_Log` ב-schema.sql (FK אופציונלי ל-Users, `action` עם CHECK ל-CREATE/UPDATE/DELETE, `old_value`/`new_value` כ-NVARCHAR(MAX) לשמירת JSON, אינדקסים על entity_type+entity_id ועל user_id).

- [x] **טבלת תפקידים והרשאות**: הרחבת `User` בשדה `role` (RISK_OFFICER / PROPERTY_MANAGER / CFO / ADJUSTER / ADMIN) + טבלת `Role_Permissions`.
  📁 `backend/app/models.py` (Class `User`) + `backend/sql/schema.sql`
  ✅ בוצע ב-branch `feature/roles-permissions`: ה-CHECK constraint על `Users.role` הורחב לכלול גם `RISK_OFFICER` ו-`ADJUSTER` (בנוסף לערכים הקיימים `RISK_MANAGER`/`FIELD_WORKER` בהם משתמש `seed.py` הקיים, כדי לא לשבור נתוני הזרעה) — כך שכל חמשת התפקידים מהמפרט (`RISK_OFFICER`/`PROPERTY_MANAGER`/`CFO`/`ADJUSTER`/`ADMIN`) נתמכים. נוספה מחלקת `RolePermission` (models.py) + טבלת `Role_Permissions` ב-schema.sql (`role`, `permission_key`, `description`, UNIQUE על role+permission_key, אינדקס על role).

- [x] **טבלת מסמכים כללית (DMS)**: טבלה `Documents` המקשרת קבצים לכל ישות (policy / claim / property / incident) עם `s3_url`, `doc_type`, `uploaded_by`.
  📁 `backend/app/models.py` + `backend/sql/schema.sql`
  ✅ בוצע ב-branch `feature/documents-dms`: נוספה מחלקת `Document` (models.py) + טבלת `Documents` ב-schema.sql. המשויכות הפוליארפית לישויות (`policy`/`claim`/`property`/`incident`) מומשה בדפוס `entity_type` + `entity_id` (זהה לדפוס שכבר קיים ב-`Audit_Log`) עם CHECK על `entity_type` בארבעת הערכים, במקום ארבעה FK-ים נפרדים nullable — כדי לשמור על עקביות עם המוסכמה הקיימת בסכימה. נוספו `s3_url`, `doc_type`, `uploaded_by` (FK אופציונלי ל-Users), `uploaded_at`. אינדקסים על (entity_type, entity_id) ועל uploaded_by.

- [x] **טבלת אזורים גיאוגרפיים**: `Regions` (region_code, name) + שדה `region_id` ב-`Property`, לצורך דוח חשיפה לפי מרכז/צפון/דרום.
  📁 `backend/app/models.py` (Class `Property`) + `backend/sql/schema.sql`
  ✅ בוצע ב-branch `feature/regions-table`: נוספה מחלקת `Region` (models.py) + טבלת `Regions` ב-schema.sql (`region_code` UNIQUE, `name`), ממוקמת לפני `Users`/`Properties` ב-schema כי `Properties.region_id` מפנה אליה. נוסף שדה `region_id` ל-`Property` כ-FK אופציונלי (nullable) ל-`Regions`, **לצד** שדה `region` (מחרוזת חופשית) הקיים ולא במקומו — כדי לא לשבור קוד/seed קיימים שכבר משתמשים ב-`region`. נוסף אינדקס `IX_Properties_RegionId`.

- [x] **טבלת נתוני מאקרו פיננסיים**: `Financial_Statements` (year, total_assets, revenue, net_income, insurance_expense) לניתוח רב-שנתי.
  📁 `backend/app/models.py` + `backend/sql/schema.sql`
  ✅ בוצע ב-branch `feature/financial-statements`: נוספה מחלקת `FinancialStatement` (models.py) + טבלת `Financial_Statements` ב-schema.sql — טבלה עצמאית (ללא FK-ים, לא תלויה בישויות אחרות ולא נתלים בה), עם `year` (SMALLINT) כ-UNIQUE כדי להבטיח רשומה אחת בלבד לשנה, מתאים לניתוח רב-שנתי לפי CFO/דוחות מאקרו.

- [x] **אינדקסים חסרים**: אינדקס מרחבי GIST על `Properties.coordinates`, אינדקס מורכב על `Incidents(status, hazard_type)` ועל `Claims(claim_status, payment_date)`.
  📁 `backend/sql/schema.sql`
  ✅ בוצע ב-branch `feature/missing-indexes`. בבדיקה מול הסכימה הקיימת התגלו שלושה פערים בין הניסוח למציאות, שתועדו ונפתרו בפועל:
  1. **אינדקס מרחבי GIST על coordinates** — GIST הוא מנוע אינדוקס של PostgreSQL; ב-SQL Server המקביל הוא `SPATIAL INDEX`, אבל הוא דורש עמודת `geometry`/`geography` ולא זוג `DECIMAL` נפרדים (`latitude`, `longitude`) כפי שקיים כאן. יצירת עמודת geography אמיתית היא שינוי מבנה נתונים מהותי שישפיע על שכבות נוספות (ORM, שירותי המפה) וחורג מהיקף המשימה. `IX_Properties_Coordinates` (אינדקס מורכב רגיל על lat+lng) כבר קיים וממלא בפועל את הצורך המעשי בשאילתות טווח גיאוגרפי — הושאר ללא שינוי, עם התיעוד הזה כהסבר לפער.
  2. **אינדקס מורכב על `Incidents(status, hazard_type)`** — כבר קיים בסכימה כ-`IX_Incidents_StatusHazard`. לא בוצע שינוי.
  3. **אינדקס מורכב על `Claims(claim_status, payment_date)`** — `payment_date` לא קיים בפועל בטבלת `Claims` אלא ב-`Claim_Payments`; ב-`Claims` יש `expected_payment_date`, ועליו כבר קיים `IX_Claims_StatusDate(claim_status, expected_payment_date)`. כפתרון מעשי נוסף אינדקס חדש `IX_ClaimPayments_ClaimDate` על `Claim_Payments(claim_id, payment_date)`, שתומך בדפוס השאילתה הריאלי — תשלומים בפועל לפי תאריך, לכל תביעה.

- [x] **הרחבת נתוני הזרעה (Seed)** לכל הטבלאות והשדות החדשים.
  📁 `backend/app/seed.py` + `backend/sql/seed.sql`
  ✅ בוצע ב-branch `feature/seed-data-expansion`: `seed.py` הורחב (לא `sql/seed.sql`, בהתאם לכלל ב-CLAUDE.md נגד טעינת עברית דרך `sqlcmd -i`) — נוספו שני משתמשים חדשים (`RISK_OFFICER`, `ADJUSTER`), הזרעת `Regions` (מרכז/צפון/דרום) עם קישור `Properties.region_id` בפועל, שדות הפוליסה החדשים (`per_event_limit`/`bi_waiting_period_hours`/`exclusions`) לכל ארבע הפוליסות, ושדות האירוע החדשים (`is_draft`/`business_interruption_requested`/`area_or_building`/`reported_coordinates`) לכל 25 האירועים הקיימים (קואורדינטות GPS מחושבות כ-offset קטן מנקודת הנכס) + שתי טיוטות אירוע חדשות (`INC-2026-010/011`, `is_draft=1`) להדגמת זרימת Draft→Submitted. נוספו גם נתוני הזרעה לטבלאות החדשות לגמרי: `Claim_Reserves` (6 רזרבות לתביעות עם יתרה פתוחה), `Audit_Log` (8 רשומות דוגמה), `Role_Permissions` (25 הרשאות פרוסות על פני 7 התפקידים), `Documents` (10 מסמכים לדוגמה על פני policy/claim/incident/property), ו-`Financial_Statements` (2022–2026). רשימות ה-`DELETE`/`DBCC CHECKIDENT` בראש `seed.py` עודכנו לכלול את כל הטבלאות החדשות בסדר תקין מבחינת FK. נבדק בפועל: `sqlcmd -i sql\schema.sql` ואז `python -m app.seed` רצים בהצלחה מקצה לקצה מול `RiskDB` מקומי, וכל הטבלאות מאוכלסות (נבדק בספירות שורות).

---

## שלב 2 — Backend Services (לוגיקה עסקית)

- [x] **מנוע רזרבות ותזרים מזומנים**: חישוב צפי תקבולים לפי סטטוס תביעה ותאריכי יעד, וסכימת רזרבות פתוחות.
  📁 קובץ חדש: `backend/app/services/cashflow.py`
  ✅ בוצע ב-branch `feature/cashflow-service`: נוצר `cashflow.py` בהתאם לדפוס הקיים ב-`kpi.py` (פונקציות טהורות שמקבלות `db: Session`, ללא קריאות LLM). `get_current_reserves` בוחר, לכל `claim_id`, את רשומת `Claim_Reserves` העדכנית ביותר (לפי `updated_at`) — כי רזרבה יכולה להתעדכן מספר פעמים לאותה תביעה, ורק הערך האחרון רלוונטי. `calculate_claim_outstanding_balance` מחשבת יתרה פתוחה לתביעה (`approved_amount` פחות סכום ה-`Claim_Payments` ששולמו בפועל, עם נפילה ל-`claimed_amount` לתביעות שטרם אושרו). `calculate_expected_receipts_by_month` מקבצת יתרות פתוחות של תביעות בסטטוס פתוח (`SUBMITTED`/`IN_ADJUSTMENT`/`APPROVED`) לפי `Claims.expected_payment_date` (חודש "YYYY-MM"). `calculate_reserves_by_month` מקבצת את הרזרבות הנוכחיות לפי `Claim_Reserves.expected_payment_date`, עם קיבוץ "unscheduled" לרזרבות ללא תאריך יעד. `get_cashflow_summary` מאחדת הכל לתצוגת דשבורד: סך רזרבות פתוחות, סך תקבולים צפויים, רזרבות ללא תאריך, ותחזית חודשית ממוזגת (`months_ahead` חודשים קדימה, ברירת מחדל 12). נבדק בפועל מול `RiskDB` המקומי (עם נתוני ה-seed המורחבים משלב קודם) — כל הפונקציות רצות ומחזירות ערכים תקינים (6 רזרבות נוכחיות, סה"כ רזרבות פתוחות ₪2,990,000), וייבוא `app.main` תקין.

- [x] **מנגנון אופטימיזציה של השתתפות עצמית (Retention Optimizer)**: השוואה בין ספיגת נזק עצמית לבין הגשת תביעה, כולל השפעה צפויה על הפרמיה.
  📁 קובץ חדש: `backend/app/services/retention.py`
  ✅ בוצע ב-branch `feature/retention-optimizer`: נוצר `retention.py` בהתאם לדפוס הקיים (פונקציות טהורות שמקבלות `db: Session`, ללא קריאות LLM). `get_effective_deductible` בוחרת את ההשתתפות העצמית האפקטיבית לנכס — `Policy_Assets.specific_deductible` אם הוגדר, אחרת `Insurance_Policies.deductible_default`. `get_active_policy_for_property` מאתרת את הפוליסה הפעילה (`status == "ACTIVE"`) המכסה נכס נתון דרך `Policy_Assets`. הליבה, `calculate_retention_recommendation`, משווה בין שני תרחישים על סכום נזק נתון: ספיגה עצמית מלאה (העלות = כל סכום הנזק, ללא השפעה על הפרמיה) מול הגשת תביעה (עלות = ההשתתפות העצמית ששולמת בפועל + תוספת פרמיה עתידית צפויה, לפי הנחה פשוטה ומתועדת של `PREMIUM_SURCHARGE_RATE = 15%` מכל שקל שהתקבל מהמבטח — בהתאם לכך שזהו כלי הדגמה לקורס ולא מנוע תמחור אקטוארי, ראו `docs/README.md` §8), עם התחשבות בתקרת `per_event_limit` אם קיימת. הפונקציה מחזירה פירוט מלא (השתתפות עצמית, סכום בר-החזר, עלות כל תרחיש, המלצה `ABSORB`/`CLAIM`, וסכום החיסכון הצפוי). `suggest_for_incident` היא עטיפה נוחה שמריצה את ההשוואה ישירות על אירוע לפי `initial_estimated_loss` והפוליסה הפעילה של נכסו. נבדק בפועל מול `RiskDB` המקומי: מתוך 27 אירועים, ל-27 נמצאה כיסוי פוליסה פעיל והופקה המלצה תקינה (כולל מקרי קצה — נזק מתחת להשתתפות העצמית → `ABSORB` ללא השפעת פרמיה, נזק גדול מעליה → `CLAIM` עם חיסכון משמעותי), וייבוא `app.main` תקין.

- [x] **סימולציית Monte Carlo וחישוב VaR**: הגרלת תרחישי נזק לפי הסתברות/חומרה ברמת הנכס, והפקת אחוזוני VaR ברמת התיק.
  📁 קובץ חדש: `backend/app/services/simulation.py`
  ✅ בוצע ב-branch `feature/monte-carlo-simulation`: נוצר `simulation.py` בהתאם לדפוס הקיים (פונקציות טהורות שמקבלות `db: Session`, ללא קריאות LLM, ללא תלויות חדשות — שימוש ב-`random`/`statistics` מהספרייה הסטנדרטית בלבד). `_get_property_exposures` שולפת לכל נכס פעיל בעל פרופיל סיכון (`Asset_Risk_Profiles`) הסתברות שנתית לאירוע נזק — נגזרת ליניארית מציון הסיכון המשוקלל הקיים (`kpi.calculate_property_risk_score`, סולם 0-100) כאשר נכס עם הציון המקסימלי (100) מקבל `MAX_ANNUAL_EVENT_PROBABILITY = 25%` הסתברות שנתית לאירוע — הנחה פשוטה ומתועדת, לא מודל אקטוארי אמיתי (ראו `docs/README.md` §8). כאשר מתרחש אירוע, החומרה נדגמת מהתפלגות משולשת (triangular) בין 0 ל-`mfl_amount` של הנכס עם שיא (`mode`) ב-30% מה-MFL (`SEVERITY_MODE_FRACTION`) — משקף שרוב האירועים הם נזק חלקי ולא נזק מלא. `run_portfolio_simulation` מריצה `iterations` שנים מדומות (ברירת מחדל 10,000), בכל שנה מדומה מטילה "מטבע משוקלל" לכל נכס ומסכמת את הנזקים לסך-שנתי, וממיינת את כלל התוצאות להפקת VaR באחוזוני הביטחון שב-`VAR_CONFIDENCE_LEVELS` (95%, 99%) — כלומר "הנזק שרק ב-(100%-C%) מהשנים הוא יחרוג ממנו" — בנוסף לנזק הצפוי הממוצע (`expected_annual_loss`) ותרחיש הגרוע ביותר שנדגם. `simulate_property` היא אותה סימולציה ברמת נכס בודד (עטיפה נוחה, מחזירה `None` אם לנכס אין פרופיל סיכון). פרמטר `seed` אופציונלי מאפשר הרצה דטרמיניסטית לבדיקות. נבדק בפועל מול `RiskDB` המקומי: סימולציית תיק על 15 הנכסים הפעילים בעלי פרופיל סיכון עם 10,000 איטרציות (`seed=42`) הניבה נזק שנתי צפוי ₪11.56M, VaR95 ₪30.31M, VaR99 ₪41.07M ותרחיש גרוע ביותר ₪71.76M — סדרי גודל הגיוניים ביחס ל-MFL/TIV הקיימים; סימולציית נכס בודד (property_id=1, MFL ₪18M) הניבה הסתברות אירוע שנתית 12.4%, נזק צפוי ₪910K ו-VaR95 ₪8.12M; נכס לא קיים החזיר `None` כמצופה; וייבוא `app.main` תקין.

- [x] **חשיפה מרחבית מצטברת (Limits Exposure)**: הרחבת חישוב האשכולות הקיים להחזרת רדיוס, מרכז אשכול וסך TIV מצטבר לכל אשכול.
  📁 `backend/app/services/kpi.py` (הרחבת `_geographic_clusters`)
  ✅ בוצע ב-branch `feature/geographic-exposure-clusters`: נוספה `calculate_geographic_exposure_clusters(db)` ב-`kpi.py`, פונקציה חדשה שמעשירה את `_geographic_clusters` הקיימת (שנשארה ללא שינוי, כדי לא לשבור את `calculate_mfl`/`calculate_alerts` שתלויות בחתימה שלה) בפרטים גיאומטריים: מרכז אשכול (`center_lat`/`center_lon` — ממוצע הקואורדינטות של חברי האשכול), רדיוס בפועל (`radius_km` — המרחק המקסימלי בין המרכז לחבר הרחוק ביותר, לא לבלבל עם `CLUSTER_RADIUS_KM` הקבוע שמשמש רק לקביעת השייכות לאשכול), וסך TIV מצטבר (`cluster_tiv_total`, סכימת `replacement_value` של חברי האשכול) לצד סך ה-MFL המצטבר הקיים (`cluster_mfl_total`). מוחזרים גם `property_ids`, `property_names` ו-`property_count` לכל אשכול, ממוינים בסדר יורד לפי `cluster_mfl_total` (התאמה לסדר העדיפויות הקיים ב-`calculate_mfl`/`calculate_alerts`). נבדק בפועל מול `RiskDB` המקומי: 12 אשכולות הופקו מתוך הנכסים הפעילים בעלי פרופיל סיכון, כולל אשכולות רב-נכסיים (למשל אשכול של 2 נכסים עם `cluster_mfl_total` ₪50M ו-`cluster_tiv_total` ₪149M, רדיוס 2.66 ק"מ) ואשכולות של נכס בודד (רדיוס 0 ק"מ כצפוי); `calculate_mfl` הקיימת המשיכה להחזיר את אותה תוצאה (₪50M) ללא שינוי התנהגות; וייבוא `app.main` תקין.

- [x] **חשיפה לפי אזור גיאוגרפי**: פונקציה המחזירה TIV, MFL וסך נזקים לכל `region`.
  📁 `backend/app/services/kpi.py`
  ✅ בוצע ב-branch `feature/exposure-by-region`: נוספה `calculate_exposure_by_region(db)` ב-`kpi.py`, המקבצת חשיפה לפי `Regions.region_id` (טבלת האזורים המובנית, להבדיל מהשדה החופשי הישן `Properties.region`) — להבדיל מהקיבוץ הגיאוגרפי-פיזי הקיים ב-`calculate_geographic_exposure_clusters`, שמקבץ לפי קרבה בפועל (ק"מ) ולא לפי גבולות מנהליים, כך ששני הנכסים עשויים להיות בשני אזורים שונים אך עדיין להצטרף לאותו אשכול פיזי. לכל אזור מוחזרים: `tiv` (סכום `replacement_value` לנכסים פעילים באזור), `mfl` (סכימה פשוטה — לא מקובצת/מקסימלית כמו ב-`calculate_mfl` — של `Asset_Risk_Profiles.mfl_amount` לנכסים פעילים באזור, משקפת קיבולת נזק כוללת ולא ריכוז חד-אירועי) ו-`total_claimed` (סכום `Claims.claimed_amount` לתביעות על אירועים בנכסי האזור, ללא סינון לפי סטטוס, בהתאם לשימוש הקיים ב-`claimed_amount` ב-`calculate_open_claims`). נכסים ללא `region_id` מקובצים תחת רשומה מלאכותית "לא משויך". התוצאה ממוינת בסדר יורד לפי `tiv`. נבדק בפועל מול `RiskDB` המקומי: 3 אזורים הופקו, לדוגמה אזור מוביל עם TIV ₪516M, MFL ₪159M ותביעות ₪2.8M; `calculate_mfl` הקיימת המשיכה להחזיר ₪50M ללא שינוי; וייבוא `app.main` תקין.

- [x] **חשיפת חישוב ROI למניעת סיכון כשירות מלא**: הפיכת `calculate_mitigation_roi` לשירות המחזיר עלות מול חיסכון צפוי בפרמיה ובנזקים.
  📁 `backend/app/services/kpi.py`
  ✅ בוצע ב-branch `feature/mitigation-roi-service`: הפונקציה המקורית `calculate_mitigation_roi(task)` נשארה ללא שינוי (עדיין בשימוש ב-`routers/mitigation.py`), ונוספו לצידה שתי פונקציות חדשות. `calculate_mitigation_roi_breakdown(db, task_id)` מפרקת את `Mitigation_Tasks.expected_annual_savings` (עמודה יחידה בסכמה) לחיסכון צפוי בפרמיה מול חיסכון צפוי בנזקים — מאחר שאין עמודות נפרדות לכך במודל, הפירוק מבוסס על הנחה קבועה ומתועדת (בדומה לדפוס `retention.PREMIUM_SURCHARGE_RATE`): נכס עם פוליסת ביטוח פעילה (נבדק דרך `retention.get_active_policy_for_property`, ייבוא מקומי כדי למנוע מעגליות טעינה) מקבל `MITIGATION_PREMIUM_SAVINGS_SHARE = 40%` מהחיסכון כזיכוי פרמיה צפוי והשאר כהפחתת נזקים צפויה; נכס ללא פוליסה פעילה מקבל את מלוא החיסכון כהפחתת נזקים (אין פרמיה לזכות). הפונקציה מחזירה גם `cost_estimate`, `roi_percent` (זהה לפלט `calculate_mitigation_roi` המקורית) ו-`payback_years` (עלות חלקי חיסכון שנתי כולל), ומחזירה `None` עבור `task_id` לא קיים. `calculate_mitigation_roi_summary(db)` מריצה את הפירוק על כל משימות המיטיגציה במסד, ממוינת בסדר יורד לפי `roi_percent` (משימות ללא `cost_estimate`, ולכן ללא ROI, ממוינות אחרונות). נבדק בפועל מול `RiskDB` המקומי: 12 משימות מיטיגציה הופקו עם פירוק תקין (לדוגמה משימה מובילה בROI 141.2% עם עלות ₪85,000 וחיסכון כולל ₪120,000, מפוצל ל-₪48,000 פרמיה/₪72,000 נזקים, החזר השקעה תוך 0.71 שנים); הושוו כל 12 ערכי `roi_percent` מול `calculate_mitigation_roi` המקורית — 0 אי-התאמות; `task_id` לא קיים החזיר `None` כמצופה; וייבוא `app.main` תקין.

- [x] **מנוע התראות והתראות חוצה-ערוצים**: הרחבת מנגנון ההתראות לשליחת Email/SMS/Push למנהל הסיכונים ול-CFO באירוע קריטי, כולל ספי התראה ניתנים להגדרה.
  📁 `backend/app/services/kpi.py` (`calculate_alerts`) + קובץ חדש `backend/app/services/notifications.py`
  ✅ בוצע ב-branch `feature/alert-notifications-engine`: `calculate_alerts` ב-`kpi.py` הורחבה עם שני פרמטרים אופציונליים, `geo_exposure_threshold_ratio` ו-`incident_concentration_threshold` (ברירת המחדל `None` נופלת לקבועי המודול הקיימים `GEO_EXPOSURE_THRESHOLD_RATIO`/`INCIDENT_CONCENTRATION_THRESHOLD`, כך שקריאות קיימות ללא ארגומנטים ממשיכות להתנהג זהה) — כך שספי ההתראה הפכו ניתנים להגדרה per-call ולא רק קבועים קשיחים. נוצר קובץ חדש `notifications.py` עם מנוע ניתוב חוצה-ערוצים: `Recipient` (dataclass) מגדיר יעד עם תפקיד, פרטי קשר, ערוצים נתמכים (`EMAIL`/`SMS`/`PUSH`) וסף חומרה מינימלי (`min_severity`) — מאחר שאין טבלת משתמשים/אנשי קשר בסכמה (ראו `docs/README.md` §8, ניהול משתמשים/RBAC מחוץ לתחום), הרשימה `DEFAULT_RECIPIENTS` היא תצורה קבועה ומתועדת (בדומה לדפוס `retention.PREMIUM_SURCHARGE_RATE`): מנהל הסיכונים מנוי על כל החומרות (`warning` ומעלה) בערוצי EMAIL+PUSH, ואילו ה-CFO מנוי רק על אירועים קריטיים (`min_severity="critical"`) בערוצי EMAIL+SMS. `build_notifications(db)` מריצה את `calculate_alerts` (עם אפשרות להעברת הספים המותאמים) ומפזרת כל התראה לכל נמען רלוונטי על כל ערוץ נתמך שלו, ומחזירה רשומת התראה מנותבת אחת לכל צירוף (התראה, נמען, ערוץ). שליחה בפועל של Email/SMS/Push היא מחוץ לתחום המוצהר (`docs/README.md` §8 — "התראות Push/SMS אוטומטיות" — אין ספק חיצוני מוגדר/מפתחות ב-`.env`), ולכן `dispatch_notifications(db)` מדמה שליחה: רושמת כל התראה ל-log (רמת WARNING לחומרה קריטית, INFO ל-warning) ומסמנת `status="simulated"` — משקפת את דפוס ה"התדרדרות חינה" הקיים ב-`routers/ai.py` כשאין `ANTHROPIC_API_KEY`, אך כאן מדובר בהיקף מכוון של הדגמת קורס ולא בתלות בהגדרה חסרה. נבדק בפועל מול `RiskDB` המקומי: עם הספים המקוריים הופקו 2 התראות (שתיהן `warning`, ריכוז אירועים) ו-4 רשומות ניתוב (רק למנהל הסיכונים, כיוון שה-CFO מסונן החוצה בחומרת warning) — כולן `dispatch` בהצלחה עם `status="simulated"`; עם `incident_concentration_threshold=1` הופקו 28 רשומות (בדיקת קונפיגורביליות); עם `geo_exposure_threshold_ratio=0.001` (מאולץ לחומרה קריטית) הופקו 16 רשומות בסך הכל, מתוכן 12 קריטיות ו-6 מנותבות ל-CFO (EMAIL+SMS בלבד, כמצופה) — מאמת את סינון ה-`min_severity` ואת ניתוב הערוצים; וייבוא `app.main` תקין.

- [x] **שירות ניתוח דוחות כספיים רב-שנתי**: חישוב מגמות ויחסים (עלות ביטוח מול הכנסות, נזקים מול שווי נכסים) לאורך שנים.
  📁 קובץ חדש: `backend/app/services/financials.py`
  ✅ בוצע ב-branch `feature/financial-analysis-service`: נוצר קובץ חדש `financials.py` המחבר בין `Financial_Statements` (שורה אחת לשנה: הכנסות, רווח נקי, סך נכסים, הוצאות ביטוח) לבין נתוני התביעות/פוליסות ברמת עסקה. מאחר ש-`Claim_Payments` ו-`Insurance_Policies` אינם מתויגים בשנת כספים ישירות, שני פונקציות עזר גוזרות סכומים שנתיים בפייתון (בדומה לדפוס הקיים ב-`kpi.calculate_...` עם `ts.year`): `_annual_claim_losses` מסכמת `Claim_Payments.amount` לפי שנת `payment_date` (תשלום בפועל, לא נתבע/אושר — עקבי עם הגישה התזרימית ב-`cashflow.py`), ו-`_annual_premiums` מסכמת `annual_premium` לכל פוליסה שטווח [`start_date`,`end_date`] שלה חופף לאותה שנה (ללא proration יומי — הנחה קבועה ומתועדת, בדומה ל-`retention.PREMIUM_SURCHARGE_RATE`). `calculate_multi_year_trends(db)` מחזירה רשימה (שורה לשנה, לפי סדר עולה) עם היחסים: `insurance_expense_to_revenue`, `net_income_margin`, `losses_to_asset_value` (נזקים ששולמו באותה שנה חלקי סך הנכסים — פרוקסי מאקרו ל"כמה מהרכוש אבד"), `loss_ratio` (נזקים ששולמו חלקי פרמיה ששולמה), וכן `revenue_growth_pct`/`losses_growth_pct` שנה-על-שנה (עם הגנה מפני חלוקה באפס/ב-`None` בשנה הראשונה). `calculate_trend_summary(db)` מוסיפה תמונת-על רב-שנתית: CAGR של הכנסות ושל נזקים, ממוצע היחסים, ודגל בוליאני `cost_of_risk_outpacing_revenue` שמשווה CAGR של הוצאות ביטוח מול CAGR של הכנסות — מחזירה `None` אם קיימת פחות משנת דוח אחת להשוואה. נבדק בפועל מול `RiskDB` המקומי: 5 שנות דוחות (2022–2026), כאשר רק 2026 (השנה הפעילה בנתוני ה-seed) כוללת תביעות/פוליסות בפועל — `claim_losses_paid=1,175,000`, `premium_paid=3,560,000`, `loss_ratio≈0.33`, `losses_to_asset_value≈0.0014`; שאר השנים הציגו נכון `0`/`null` ללא שגיאות חלוקה באפס. סיכום המגמות: `revenue_cagr_pct≈9.14`, `avg_insurance_expense_to_revenue≈0.0114`, `cost_of_risk_outpacing_revenue=true` (הוצאות הביטוח גדלות מהר יותר מההכנסות). ייבוא `app.main` תקין.

- [x] **שירות אחסון קבצים (S3/Blob)**: העלאה, יצירת Signed URL, וקריאת מטא-דאטה EXIF/GPS מתמונות.
  📁 קובץ חדש: `backend/app/services/storage.py`
  ✅ בוצע ב-branch `feature/file-storage-service`: נוצר קובץ חדש `storage.py`. מאחר שאין חשבון S3/Blob אמיתי ואין credentials ב-`backend/.env` (ראו `docs/README.md` §8 — שמירת קבצים בפועל למדיה של אירועים מחוץ לתחום), השירות מדמה בענן: `upload_file(file_bytes, filename, entity_type, entity_id)` שומר את הקובץ לתיקייה מקומית `backend/media_storage/` (חדשה, נוספה ל-`.gitignore` באותו יחס כמו `backend/.env`) תחת מסלול `<entity_type>s/<entity_id>/<filename>` ומחזיר `UploadResult` עם `storage_key`, `url` (בפורמט `https://storage.rmis-demo.local/...` — עקבי עם ה-domain הפיקטיבי שכבר קיים בנתוני ה-seed תחת `Documents.s3_url`, ראו `seed.py`), `size_bytes` ו-`content_type` (לפי סיומת קובץ). `generate_signed_url(storage_key, expires_in_seconds)` "חותם" URL מקומית — מחשבת טוקן `sha256` מ-(`storage_key`, `expires_at`, סוד קבוע מקומי `_SIGNING_SECRET`) ומצרפת כ-query params, במקום קריאה אמיתית לספק ענן; `is_signed_url_valid(...)` מאמתת טוקן+תפוגה בצד השרת (עבור endpoint הורדה עתידי). `extract_image_metadata(file_bytes)` קוראת EXIF דרך Pillow (נוסף ל-`requirements.txt`): `DateTimeOriginal` מתת-ה-IFD "Exif" (0x8769, לא ה-IFD הראשי) ו-`GPSInfo` מתת-ה-IFD המתאימה (0x8825), עם המרת קואורדינטות DMS→decimal degrees וטיפול בהמיספרות (`GPSLatitudeRef`/`GPSLongitudeRef`); מחזירה `None` (לא זורקת חריגה) עבור קבצים שאינם תמונה קריאה (וידאו/PDF) או תמונה ללא EXIF כלל — עקבי עם דפוס ה-`None`-על-חוסר-נתונים הקיים ב-`simulation.simulate_property`. `delete_file(storage_key)` מוחקת ומחזירה `False` (לא שגיאה) אם הקובץ כבר לא קיים. נבדק בפועל: נבנתה תמונת JPEG לבדיקה עם `piexif` (תלות זמנית להרצת הבדיקה בלבד, הוסרה בסיום) הכוללת `DateTimeOriginal` וקואורדינטות GPS (32.083333, 34.783333 — תל אביב בקירוב); `upload_file` שמרה את הקובץ בפועל לדיסק (גודל תואם), `generate_signed_url`/`is_signed_url_valid` אימתו טוקן תקין/פג-תוקף/מזויף כצפוי (True/False/False), `extract_image_metadata` חילצה נכון תאריך וקואורדינטות, החזירה `None` לתמונת PNG ללא EXIF ול-bytes שאינם תמונה כלל, ו-`delete_file` מחקה בהצלחה והחזירה `False` בניסיון שני. וייבוא `app.main` תקין.

---

## שלב 3 — Backend API (Routers)

- [x] **Endpoints להעלאת מדיה לאירוע**: העלאת תמונות/וידאו/PDF, שליפת רשימת מדיה, מחיקה — כולל שמירת קואורדינטות EXIF.
  📁 קובץ חדש: `backend/app/routers/media.py` + סכמות ב-`backend/app/schemas.py`
  ✅ בוצע ב-branch `feature/media-upload-endpoints`: נוצר קובץ חדש `media.py` שהוא שכבת ה-HTTP מעל השירות `services/storage.py` שנבנה במשימה הקודמת. הוספתי עמודות חדשות `gps_latitude`/`gps_longitude` (FLOAT, NULL) לטבלת `Incident_Media` (ב-`sql/schema.sql`, `models.py` וב-`docs/erd.md`) — הטבלה הקיימת לא כללה שדות לשמירת קואורדינטות EXIF, כנדרש במשימה. חמישה endpoints: (1) `POST /api/incidents/{incident_id}/media` — מקבל `UploadFile` (multipart), קורא ל-`storage.upload_file` לשמירה בפועל, ואם מדובר בתמונה עם EXIF קורא גם ל-`storage.extract_image_metadata`: `DateTimeOriginal` (מפורמט EXIF `YYYY:MM:DD HH:MM:SS`) הופך ל-`captured_at` של הרשומה (עם נפילה חזרה ל"עכשיו" כשאין EXIF), וקואורדינטות ה-GPS (אם קיימות) נשמרות בעמודות החדשות; מגביל גודל קובץ ל-25MB ומוודא שהאירוע קיים (404 אחרת). (2) `GET /api/incidents/{incident_id}/media` — רשימת מדיה לאירוע, מסודרת לפי `captured_at` יורד. (3) `DELETE /api/media/{media_id}` — מוחקת גם את הקובץ בפועל (`storage.delete_file`, לא נכשלת אם כבר נמחק) וגם את הרשומה ב-DB. (4) `GET /api/media/{media_id}/signed-url` — קוראת ל-`storage.generate_signed_url` ומחזירה בנוסף `download_url` אמיתי וניתן-לשליפה כלפי ה-API של עצמנו (`/api/media/download?key=...&expires=...&token=...`), כדי לסגור פער שהתגלה בבדיקה: ה-`url` המקורי שמחזיר `storage.py` מצביע ל-domain הפיקטיבי `storage.rmis-demo.local` (עקבי עם `Documents.s3_url`) שאינו נגיש בפועל — לכן עדכנתי גם את `generate_signed_url` ב-`storage.py` להחזיר `token` בנפרד, כדי לאפשר בניית URL הורדה אמיתי. (5) `GET /api/media/download` — ה-endpoint שה-docstring של `is_signed_url_valid` כבר צפה שיבוא: מאמת טוקן+תפוגה דרך `storage.is_signed_url_valid` ומחזירה את בייטס הקובץ בפועל (`Response` עם `content_type` לפי סיומת) רק אם תקין, אחרת `403`. נבדק בפועל מול `RiskDB` מקומי (לאחר הרצת `schema.sql` מחדש + `seed.py`, כנדרש בעקבות שינוי הסכמה — ראו גם ההערה ב-`CLAUDE.md` לגבי `DBCC CHECKIDENT`): העלאת JPEG עם EXIF (`DateTimeOriginal` + קואורדינטות GPS ליד תל אביב, נבנה עם `piexif` כתלות זמנית בלבד להרצת הבדיקה) — נשמרו נכון גם התאריך וגם הקואורדינטות; רשימת מדיה החזירה את הפריט; `signed-url` החזיר `download_url` תקין; הורדה דרכו החזירה `200` עם התוכן והגודל הנכונים ו-`content-type: image/jpeg`; טוקן מזויף ו-URL שפג תוקפו החזירו `403` כצפוי; העלאת PDF ללא EXIF שמרה `captured_at`="עכשיו" ו-`gps=None` כצפוי; העלאה לאירוע לא קיים החזירה `404`; מחיקה החזירה `204` והסירה את הפריט מהרשימה; מחיקה של רשומה לא קיימת החזירה `404`. לאחר הבדיקות אופסה בסיס הנתונים בחזרה למצב seed נקי (`schema.sql` + `seed.py`), הוסרה תלות הבדיקה הזמנית `piexif`, ותיקיית `backend/media_storage/` נוקתה. ייבוא `app.main` תקין.

- [x] **השלמת CRUD למשימות מיטיגציה**: יצירה, עדכון, שיוך מבצע, סגירה, וחישוב סטטוס `OVERDUE` אוטומטי + endpoint ל-ROI.
  📁 `backend/app/routers/mitigation.py`
  ✅ בוצע ב-branch `feature/mitigation-task-crud`: הרחבתי את `mitigation.py` (שקודם הכיל רק `list_tasks`) לחמישה endpoints: (1) `GET /api/mitigation-tasks` — רשימה (קיים, ללא שינוי בחתימה). (2) `GET /api/mitigation-tasks/{task_id}` — משימה בודדת, `404` אם לא קיימת. (3) `POST /api/mitigation-tasks` — יצירה עם `MitigationTaskCreate` חדש (`schemas.py`): מוודא שהנכס קיים (`404` אחרת) ושמשתמש `assigned_to_user_id` (אם צוין) קיים; סטטוס ראשוני `OPEN`. (4) `PATCH /api/mitigation-tasks/{task_id}` — עדכון חלקי (`MitigationTaskUpdate`, `exclude_unset`) לכל השדות כולל שיוך/שינוי מבצע (`assigned_to_user_id`, מוודא קיום המשתמש) וסגירה (`status="COMPLETED"`). (5) `GET /api/mitigation-tasks/{task_id}/roi` — מפעיל את `kpi.calculate_mitigation_roi_breakdown` הקיים (היה בנוי בעבר אך לא היה חשוף ב-router כלשהו) ומחזיר פירוט מלא (`MitigationRoiBreakdown` חדש): עלות, סה"כ חיסכון, פיצול הנחת פרמיה/הפחתת נזק צפוי, ROI%, תקופת החזר. חישוב `OVERDUE` אוטומטי: פונקציית עזר `_sync_overdue` (בקובץ ה-router) רצה בכל קריאת GET/POST/PATCH — משימה שתאריך היעד שלה עבר וסטטוסה אינו `COMPLETED` הופכת אוטומטית ל-`OVERDUE` (גם אם הבקשה עצמה ניסתה להעביר אותה ל-`IN_PROGRESS` — היותה "בתהליך" לא מבטלת את היותה באיחור); ואם תאריך היעד הוזז לעתיד (או שהמשימה סומנה `COMPLETED`), משימה שהייתה `OVERDUE` חוזרת אוטומטית ל-`OPEN`. גם הוספתי `assigned_to_user_id` ל-`MitigationTaskOut` (הושמט קודם). נבדק בפועל מול `RiskDB` מקומי דרך שרת ה-preview: יצירת משימה עם תאריך יעד בעבר → `OVERDUE` אוטומטית; יצירה עם תאריך עתידי + שיוך מבצע → `OPEN` עם `assigned_to_user_id` נכון; יצירה עם נכס לא קיים / משתמש לא קיים → `404`; שיוך מבצע חדש ל-`PATCH` → מעודכן; ניסיון `PATCH status=IN_PROGRESS` על משימה באיחור → נשארה `OVERDUE` כמצופה; `PATCH status=COMPLETED` (סגירה) → נשמר; הזזת `due_date` של משימה עתידית לתאריך עבר → הפכה אוטומטית ל-`OVERDUE`; `GET .../roi` החזיר פירוט מלא ונכון (כולל `payback_years`), ו-`404` למשימה לא קיימת; `GET`/`PATCH` על משימה לא קיימת → `404`. שימו לב: נתוני ה-seed הקיימים (`seed.py`) כללו כמה משימות עם סטטוס `OVERDUE` קשיח שתאריך היעד שלהן (למשל `2026-08-25`, `2026-09-05`) הוא בעתיד ביחס לתאריך הנוכחי (2026-08-19) — אלה מתוקנות אוטומטית ל-`OPEN`/`IN_PROGRESS`/`COMPLETED` הלוגי בכל קריאת `GET`/`PATCH` הודות ל-`_sync_overdue`, כצפוי מהתנהגות "חישוב אוטומטי". לאחר הבדיקות אופסה בסיס הנתונים בחזרה למצב seed נקי (`schema.sql` + `seed.py`). ייבוא `app.main` תקין.

- [x] **Endpoints לרזרבות וצפי תזרים**: שליפת תחזית תקבולים חודשית ורזרבות פתוחות עבור הדשבורד.
  📁 `backend/app/routers/analytics.py` או קובץ חדש `backend/app/routers/cashflow.py`
  ✅ בוצע ב-branch `feature/cashflow-reserves-endpoint`: התברר שכל שכבת השירות כבר הייתה בנויה מראש ולא הייתה חשופה דרך endpoint כלשהו — `services/cashflow.py` כלל `get_cashflow_summary` מלא (סה"כ רזרבות פתוחות מ-`Claim_Reserves` לפי הרשומה העדכנית ביותר לכל תביעה, סה"כ תקבולים צפויים לפי `Claims.expected_payment_date` והיתרה שטרם שולמה [`approved_amount` פחות תשלומים שכבר בוצעו, עם נפילה חזרה ל-`claimed_amount` לתביעות שטרם אושרו], ותצוגה חודשית ממוזגת מ-`months_ahead` חודשים קדימה). הוספתי endpoint אחד חדש: `GET /api/analytics/cashflow?months_ahead=12` (פרמטר אופציונלי, ברירת מחדל 12), שקורא ישירות ל-`cashflow.get_cashflow_summary`. הוספתי שתי סכמות תשובה חדשות ל-`schemas.py`: `CashflowMonthPoint` (`month`, `expected_receipts`, `open_reserves`) ו-`CashflowSummary` (`total_open_reserves`, `total_expected_receipts`, `unscheduled_reserves`, `monthly: list[CashflowMonthPoint]`). נבדק בפועל מול `RiskDB` מקומי דרך שרת ה-preview (ללא שינוי סכמת DB, לכן ללא צורך לאפס נתונים): `GET /api/analytics/cashflow` החזיר `200` עם `total_open_reserves=2990000.0`, `total_expected_receipts=450000.0`, `unscheduled_reserves=1590000.0` ותצוגה חודשית תקינה (`2026-09`, `2026-10`); `GET /api/analytics/cashflow?months_ahead=3` החזיר את אותם סה"כים גלובליים (כצפוי — הפרמטר משפיע רק על החלון החודשי הממוזג, לא על הסכומים הכוללים) עם אותה תצוגה חודשית (שני החודשים הרלוונטיים נופלים גם בתוך חלון של 3 חודשים). `/docs` (Swagger UI) נטען בהצלחה ומציג את ה-endpoint החדש, מה שמאשר שהוא נרשם כראוי באפליקציה. ייבוא `app.main` תקין.

- [x] **Endpoint לחשיפה לפי אזורים**: החזרת טבלת TIV/MFL/נזקים לכל אזור עבור דוח ההנהלה.
  📁 `backend/app/routers/analytics.py`
  ✅ בוצע ב-branch `feature/exposure-by-region-endpoint`: כמו במשימה הקודמת, שכבת השירות כבר הייתה בנויה במלואה — `kpi.calculate_exposure_by_region` (TIV = סכום `replacement_value` לנכסים פעילים באזור; MFL = סכום פשוט של `Asset_Risk_Profiles.mfl_amount` לנכסים באזור, לא מקסימום אשכולי כמו `calculate_mfl`; `total_claimed` = סכום `Claims.claimed_amount` לכל התביעות על אירועים בנכסי האזור, ללא סינון לפי סטטוס; נכסים ללא `region_id` מקובצים תחת "לא משויך"; ממוין יורד לפי TIV) — רק לא הייתה חשופה דרך endpoint. הוספתי `GET /api/analytics/exposure-by-region` שקורא ישירות ל-`kpi.calculate_exposure_by_region`, וסכמת תשובה חדשה `RegionExposure` (`region_id`, `region_name`, `tiv`, `mfl`, `total_claimed`) ב-`schemas.py`. נבדק בפועל מול `RiskDB` מקומי דרך שרת ה-preview: `GET /api/analytics/exposure-by-region` החזיר `200` עם שלושה אזורים (מרכז/צפון/דרום) ממוינים נכון יורד לפי TIV (516M/136M/79M), עם ערכי MFL ו-`total_claimed` תואמים לנתוני ה-seed. ייבוא `app.main` תקין.

- [x] **Endpoint לאשכולות חשיפה מרחבית**: החזרת אשכולות גיאוגרפיים לציור על המפה.
  📁 `backend/app/routers/analytics.py`
  ✅ בוצע ב-branch `feature/geographic-exposure-clusters-endpoint`: כמו בשתי המשימות הקודמות, שכבת השירות כבר הייתה בנויה במלואה — `kpi.calculate_geographic_exposure_clusters` (מבוססת על `_geographic_clusters` הקיימת: אשכולות חופפים של נכסים במרחק `CLUSTER_RADIUS_KM=10` ק"מ זה מזה, לפי מרחק Haversine; לכל אשכול מחושבים המרכז הגיאוגרפי (ממוצע lat/lon של החברים), הרדיוס בפועל (המרחק המקסימלי מהמרכז לחבר כלשהו — לא סף ה-10 ק"מ ששימש ליצירת האשכול), וסך MFL ו-TIV מצטברים; ממוין יורד לפי `cluster_mfl_total` בדומה ל-`calculate_mfl`/`calculate_alerts`) — רק לא הייתה חשופה דרך endpoint. הוספתי `GET /api/analytics/geographic-exposure-clusters` שקורא ישירות ל-`kpi.calculate_geographic_exposure_clusters`, וסכמת תשובה חדשה `GeographicExposureCluster` (`property_ids`, `property_names`, `property_count`, `center_lat`, `center_lon`, `radius_km`, `cluster_mfl_total`, `cluster_tiv_total`) ב-`schemas.py`. נבדק בפועל מול `RiskDB` מקומי דרך שרת ה-preview: `GET /api/analytics/geographic-exposure-clusters` החזיר `200` עם 12 אשכולות — שלושה אשכולות זוגיים (קניון רעננה+מגדל היי-טק הרצליה עם `cluster_mfl_total=50000000.0`, מגדל משרדים ת"א+מרכז לוגיסטי ראשל"צ עם `40000000.0`, מרכז מסחרי נשר+מתחם משרדים חיפה עם `32000000.0`) ותשעה אשכולות בני נכס בודד עם `radius_km=0.0` כצפוי, כולם ממוינים נכון יורד לפי `cluster_mfl_total` (50M→40M→32M→21M→19M→18M→16M→11M→10M→9M→8M→7M). ייבוא `app.main` תקין.

- [x] **Endpoints לסימולציה ו-VaR**: הרצת סימולציה עם פרמטרים (מספר איטרציות, אופק זמן) והחזרת התפלגות תוצאות.
  📁 קובץ חדש: `backend/app/routers/simulation.py`
  ✅ בוצע ב-branch `feature/simulation-var-endpoints`: `services/simulation.py` כבר כלל את מנוע הסימולציה המלא (`run_portfolio_simulation`/`simulate_property`) אך ללא פרמטר "אופק זמן" וללא החזרת התפלגות תוצאות מלאה (רק סטטיסטיקות מסכמות) — בהתאם לדפוש שהתגלה בשלוש המשימות הקודמות, אך הפעם נדרשה גם הרחבה קטנה של שכבת השירות, לא רק חשיפה. הוספתי לשתי הפונקציות פרמטר `horizon_years` (ברירת מחדל 1, שומר על ההתנהגות המקורית): כל איטרציה כעת מדמה `horizon_years` שנים בלתי-תלויות (אותו מודל הסתברות/חומרה שנתי בכל שנה, ללא מגמת שנה-על-שנה — עקבי עם ההנחות המתועדות הקיימות במודול) ומסכמת אותן לנזק מצטבר אחד לאופק; אומת ש-`horizon_years=3` הניב `expected_annual_loss` פי ~3 מ-`horizon_years=1` עם אותו `seed`, כצפוי ממודל ליניארי-בממוצע. הוספתי גם `_build_histogram(sorted_values, bucket_count)` — פונקציית עזר חדשה שמחלקת את התפלגות הנזקים המדומה ל-`bucket_count` (ברירת מחדל 20, `DEFAULT_HISTOGRAM_BUCKETS`) דליים שווי-רוחב בין המינימום למקסימום שנדגמו, עם טיפול במקרה הבסיסי שכל הערכים זהים (למשל את סימולציה עם הסתברות אירוע אפסית) — כדי לספק "התפלגות תוצאות" בפועל לתרשים, לא רק תקציר. שני שדות חדשים, `horizon_years` ו-`distribution` (רשימת `{bucket_min, bucket_max, count}`), נוספו לפלט של שתי הפונקציות. נוצר קובץ חדש `routers/simulation.py` עם שני endpoints תחת `/api/simulation`: (1) `GET /portfolio?iterations=&horizon_years=&seed=` — קורא ל-`simulation.run_portfolio_simulation`, עם ולידציית טווח ב-FastAPI `Query` (`iterations` עד 100,000, `horizon_years` עד 50, שניהם `gt=0`) כדי למנוע הרצה כבדה/חסרת משמעות בטעות. (2) `GET /properties/{property_id}?iterations=&horizon_years=&seed=` — קורא ל-`simulation.simulate_property`, מחזיר `404` אם הנכס לא קיים או שאין לו פרופיל סיכון (כפי שכבר תיעדה הפונקציה המקורית). נוספו שלוש סכמות חדשות ל-`schemas.py`: `HistogramBucket`, `PortfolioSimulationResult`, `PropertySimulationResult`. נרשם ב-`main.py`. נבדק בפועל מול `RiskDB` המקומי דרך שרת ה-preview (ללא שינוי סכמת DB): `GET /portfolio?iterations=2000&seed=42&horizon_years=1` החזיר נזק צפוי ₪11.49M, VaR95 ₪29.88M, VaR99 ₪39.91M והיסטוגרמה של 20 דליים — עקבי עם המספרים שתועדו במשימת בניית השירות המקורית; `horizon_years=3` עם אותו seed החזיר נזק צפוי ₪34.58M (~פי 3, כצפוי); `GET /properties/1?iterations=2000&seed=42` החזיר הסתברות אירוע 12.4%, MFL ₪18M ונזק צפוי ₪981K — תואם למסמך המקור; `GET /properties/999999` החזיר `404` כמצופה. וייבוא `app.main` תקין.

- [x] **Endpoint לאופטימיזציית השתתפות עצמית**: קלט אומדן נזק ופוליסה, פלט המלצה (לספוג / לתבוע).
  📁 קובץ חדש: `backend/app/routers/retention.py`
  ✅ בוצע ב-branch `feature/retention-optimizer-endpoint`: שכבת השירות (`services/retention.py`, ראו שלב 2) כבר כללה את הלוגיקה המלאה (`calculate_retention_recommendation`, `suggest_for_incident`) אך ללא endpoint חושף — נוצר קובץ חדש `routers/retention.py` עם שני נתיבים תחת `/api/retention`: (1) `GET /recommendation?policy_id=&property_id=&estimated_loss=` — מקבל אומדן נזק ידני מול פוליסה/נכס ספציפיים, מוודא קיום שניהם (`404` אחרת) לפני קריאה לשירות (השירות עצמו זורק `ValueError` על פוליסה חסרה, לא מתאים ל-HTTP ישירות). (2) `GET /incidents/{incident_id}` — עטיפה נוחה שמפעילה את `suggest_for_incident` על אירוע קיים (אומדן הנזק והפוליסה הפעילה נגזרים אוטומטית מהאירוע/הנכס שלו), `404` אם האירוע לא קיים או שאין לנכס שלו כיסוי פוליסה פעיל. נוספה סכמת תשובה `RetentionRecommendation` ב-`schemas.py` (תואמת למבנה המילון שכבר מוחזר מהשירות, עם `incident_id` אופציונלי). נרשם ב-`main.py`. נבדק בפועל מול `RiskDB` המקומי דרך שרת ה-preview: `GET /recommendation?policy_id=1&property_id=1&estimated_loss=450000` החזיר `200` עם המלצת `CLAIM` תקינה (השתתפות עצמית ₪100,000, חיסכון צפוי ₪297,500); `GET /incidents/1` החזיר את אותה תוצאה עם `incident_id=1` מצורף; פוליסה לא קיימת ואירוע לא קיים החזירו `404` עם הודעות ברורות. ייבוא `app.main` תקין.

- [x] **Endpoints לניהול מסמכים (DMS)**: העלאה ושליפה של פוליסות, דוחות שמאי ותכתובות לפי ישות.
  📁 קובץ חדש: `backend/app/routers/documents.py`
  ✅ בוצע ב-branch `feature/documents-endpoint`: בניגוד לרוב המשימות הקודמות בשלב זה, לא הייתה שכבת שירות ייעודית ל-`Documents` — אבל שכבת האחסון הכללית `services/storage.py` (upload/delete/signed-URL, ראו `routers/media.py`) כבר קיימת ומטבעה גנרית (`entity_type`/`entity_id`), כך שנדרשה רק חשיפה דרך endpoints חדשים בסגנון כמעט-זהה ל-`media.py`, לא מימוש לוגיקה חדשה. נוצר קובץ חדש `routers/documents.py` עם חמישה נתיבים תחת `/api/documents`: (1) `POST /entity/{entity_type}/{entity_id}?doc_type=&uploaded_by=` — מעלה קובץ (Multipart) ומצרף אותו לישות (`INCIDENT`/`CLAIM`/`PROPERTY`/`POLICY`, מאומת מול `schemas.DocumentEntityType`), עם `404` אם הישות לא קיימת (נבדק מול `_ENTITY_MODELS` — מיפוי entity_type ↔ מודל ORM). (2) `GET /entity/{entity_type}/{entity_id}` — רשימת מסמכים לישות, ממוינת יורד לפי `uploaded_at`. (3) `DELETE /{document_id}` — מוחק שורה + קובץ באחסון. (4) `GET /{document_id}/signed-url` — מנפיק קישור הורדה חתום (זהה בעקרונו ל-`/api/media/{media_id}/signed-url`). (5) `GET /download?key=&expires=&token=` — משרת את הבייטים בפועל לאחר אימות החתימה, זהה ל-`/api/media/download`. שים לב: נתיבי הישות תחת קידומת `/entity/...` ולא ישירות `/{entity_type}/{entity_id}` — כפי שהתגלה בבדיקה בפועל, מכיוון ש-`entity_type` מוקלד כ-`Literal` (הממומש ב-Starlette כממיר `str` רגיל), בקשה כמו `GET /api/documents/9999/signed-url` הייתה מותאמת מבנית לנתיב `/{entity_type}/{entity_id}` (`entity_type="9999"`, `entity_id="signed-url"` נכשל בהמרת `int`... אך מכיוון ש-Starlette כבר "התחייב" לנתיב הזה לפי המרת str, ה-422 שהתקבל היה על ולידציית ה-`Literal` ולא נפילה חזרה ל-`/{document_id}/signed-url`) — הוספת הקידומת `/entity/` מבטלת את הדו-משמעות לחלוטין במקום להסתמך על סדר רישום הנתיבים. הוספה סכמה חדשה `DocumentOut` (וה-`Literal` `DocumentEntityType`) ב-`schemas.py`, תואמת ל-`models.Document`. נרשם ב-`main.py`. נבדק בפועל מול `RiskDB` המקומי דרך שרת ה-preview (כולל restart של השרת בעקבות "wedging" של ה-reloader לאחר עריכות רצופות — ראו CLAUDE.md): העלאת קובץ ל-`PROPERTY/1` החזירה `201` עם רשומה תקינה; `GET /entity/PROPERTY/1` החזיר גם את הרשומה החדשה וגם רשומת seed קיימת; `GET /entity/PROPERTY/99999` החזיר `404`; `GET /{document_id}/signed-url` על מסמך קיים ועל מסמך לא קיים (`9999`) החזירו `200`/`404` בהתאמה (ללא 422 — מאמת את תיקון הדו-משמעות); `GET /download` עם טוקן תקין החזיר את תוכן הקובץ, וטוקן שגוי החזיר `403`; `DELETE /{document_id}` מחק גם את הרשומה וגם את הקובץ מהדיסק (אומת ידנית), והחזיר `404` על מסמך לא קיים; העלאה עם `entity_type=BOGUS` החזירה `422` על ולידציית ה-`Literal`. ייבוא `app.main` תקין.

- [x] **Endpoint לתיק אירוע מאוחד (Drill-down)**: החזרת אירוע + מדיה + תביעה + תשלומים + מסמכים בקריאה אחת.
  📁 `backend/app/routers/incidents.py`
  ✅ בוצע ב-branch `feature/incident-drilldown-endpoint`: נוסף `GET /api/incidents/{incident_id}/full` שמחזיר בקריאה אחת את כל "תיק האירוע": האירוע עצמו, כל רשומות המדיה שלו (`Incident_Media`, ממוין יורד לפי `captured_at`), כל התביעות שנפתחו עליו (`Claims`, ממוין יורד לפי `created_at`) — כל תביעה כוללת מקוננת את רשימת התשלומים שלה (`Claim_Payments`, נטענת ב-`selectinload` כדי למנוע N+1), וכל המסמכים המצורפים ישירות לאירוע (`Documents` שבהם `entity_type="INCIDENT"` ו-`entity_id=incident_id`, ממוין יורד לפי `uploaded_at` — משתמש ב-endpoint ה-DMS שנבנה במשימה הקודמת). נוספו שתי סכמות חדשות ל-`schemas.py`: `ClaimWithPaymentsOut` (יורשת מ-`ClaimOut` ומוסיפה `payments: list[ClaimPaymentOut]`) ו-`IncidentDrillDown` (`incident`, `media`, `claims`, `documents`). הנתיב `/{incident_id}/full` אינו יוצר דו-משמעות עם `/{incident_id}` הקיים (הצורה המבנית שונה — קטע נתיב נוסף קבוע `full`, לא פרמטר), כך שלא נדרש תיקון דומה לזה שהתגלה במשימת ה-DMS. נבדק בפועל מול `RiskDB` המקומי דרך שרת ה-preview: `GET /api/incidents/1/full` החזיר `200` עם האירוע, תביעה אחת עם שני תשלומים (מקדמה + סילוק סופי) ומערכי `media`/`documents` ריקים (אין רשומות seed מתאימות לאירוע זה); `GET /api/incidents/4/full` החזיר תביעה עם `payments: []` (טרם שולם) ומסמך `PHOTO` אחד מסוג `INCIDENT` שהתאים בדיוק לרשומת ה-seed (`INC-2025-004-photo1.jpg`); `GET /api/incidents/999999/full` החזיר `404`. ייבוא `app.main` תקין.

- [x] **תמיכה בשמירת אירוע כטיוטה** ובשליחה מאוחרת (Draft → Submitted).
  📁 `backend/app/routers/incidents.py` + `backend/app/schemas.py`
  ✅ בוצע ב-branch `feature/incident-draft-submit`: `IncidentCreate` נחשף כעת גם על `is_draft`, `business_interruption_requested`, `area_or_building`, `reported_coordinates` — עמודות שכבר קיימות ב-`models.Incident`/`schema.sql` מהמשימה הקודמת (הרחבת טבלת האירועים) אך לא היו חשופות בשכבת ה-API עד כה; `IncidentOut` הורחב אותו הדבר להחזרתן. נוספה `IncidentUpdate` (כל השדות אופציונליים) לעריכת תוכן טיוטה. שני נתיבים חדשים ב-`incidents.py`: `PATCH /{incident_id}` — עורך שדות של טיוטה קיימת, מחזיר `400` אם `is_draft=False` (האירוע כבר הוגש) ו-`404` אם לא קיים; מוודא ש-`property_id` חדש (אם נשלח) קיים בפועל. `PATCH /{incident_id}/submit` — הופך `is_draft` ל-`False` (Draft → Submitted), מחזיר `400` אם כבר הוגש בעבר ו-`404` אם לא קיים. אין דו-משמעות ניתוב: `PATCH /{incident_id}` הוא מתודה שונה מ-`GET /{incident_id}` הקיים, ו-`/{incident_id}/submit` הוא קטע נתיב קבוע נוסף בדיוק כמו `/{incident_id}/status` ו-`/{incident_id}/full` הקיימים. נבדק בפועל מול `RiskDB` המקומי דרך שרת ה-preview: יצירת טיוטה (`POST` עם `is_draft=true` + שאר השדות החדשים) הצליחה ב-`201`; `PATCH /{incident_id}` עדכן `description`/`initial_estimated_loss` בהצלחה (`200`); `PATCH /{incident_id}/submit` הפך את הטיוטה להגשה (`is_draft: false`, `200`); ניסיון עריכה נוספת אחרי ההגשה החזיר `400`; ניסיון הגשה כפולה החזיר `400`; ניסיון הגשה של אירוע לא-טיוטה קיים (incident 1) החזיר `400`; ניסיונות מול `incident_id=999999` (גם עריכה וגם הגשה) החזירו `404`.

- [x] **רישום כל הנתיבים החדשים באפליקציה**.
  📁 `backend/app/main.py`
  ✅ נבדק ב-branch `feature/verify-route-registration` (ללא שינוי קוד — משימת אימות סגירה): `main.py` כבר ייבא וכלל את כל 11 קובצי ה-routers הקיימים (`properties`, `incidents`, `policies`, `claims`, `mitigation`, `media`, `analytics`, `simulation`, `retention`, `documents`, `ai`) — נבנה בהדרגה לאורך המשימות הקודמות בסשן זה, ולא נותר אף router שנוצר ולא נרשם. אומת בפועל מול `GET /openapi.json` דרך שרת ה-preview הרץ: כל 43 הנתיבים מופיעים ברשימה, כולל אלו שנוספו במשימות האחרונות של הסשן — `/api/incidents/{incident_id}/full`, `/api/incidents/{incident_id}/submit`, `/api/documents/*`, `/api/retention/*`, `/api/simulation/*`, `/api/analytics/geographic-exposure-clusters`, `/api/analytics/exposure-by-region`, `/api/analytics/cashflow`. לא נדרש שינוי ב-`main.py`.

---

## שלב 4 — Auth & Security

- [x] **מודול אימות משתמשים**: התחברות, JWT/Session, רענון טוקן, יציאה.
  📁 קובץ חדש: `backend/app/routers/auth.py` + `backend/app/services/auth.py`

  ✅ בוצע (branch `feature/auth-rbac-security`). CLAUDE.md מנה במפורש את כל שלב 4 כ"מחוץ לתחום", אך המשתמש אישר בפירוש להשלים את כל מה שנותר בשלב זה ("כל מה שלא עשית בשלב 4 תעשה אני מרשה לך").

  מימוש: `Users.password_hash` (עמודה חדשה, `NVARCHAR(255) NULL` — `salt$hash` hex של `hashlib.pbkdf2_hmac`, 260,000 איטרציות; stdlib בלבד, בלי תלות חיצונית שדורשת קומפילציה כמו bcrypt). `services/auth.py`: `hash_password`/`verify_password` + `create_access_token`/`create_refresh_token`/`decode_token` (JWT חתום HS256, `PyJWT`). `routers/auth.py`: `POST /api/auth/login` (email+password → access+refresh+פרטי המשתמש), `POST /api/auth/refresh` (refresh→access חדש), `POST /api/auth/logout` (204, no-op — אין רשימת ביטול טוקנים בצד שרת; ה-JWT הוא stateless, ראו את ה-docstring של הראוטר), `GET /api/auth/me`. `app/seed.py` מפיק סיסמת דמו משותפת `Demo1234!` (מוצפנת בהאש) לכל 9 המשתמשים הזרועים.

  אימות: `POST /login` עם `dana.cohen@company.co.il` / `Demo1234!` → 200 עם access+refresh תקינים; סיסמה שגויה → 401; `GET /me` עם הטוקן → פרטי המשתמש הנכונים; `GET /me` בלי טוקן → 401; `POST /refresh` עם ה-refresh token → access token חדש תקין.

- [x] **RBAC — הרשאות לפי תפקיד**: Dependency ב-FastAPI החוסמת גישה לנתיבים לפי תפקיד המשתמש (CFO / מנהל סיכונים / מנהל נכס / שמאי).
  📁 קובץ חדש: `backend/app/dependencies/permissions.py` + החלה על כל ה-routers

  ✅ בוצע (אותו branch). `dependencies/permissions.py`: `get_current_user` (מפענח `Authorization: Bearer`, טוען את `models.User`, 401 אם חסר/לא תקף) ו-`require_roles(*roles)` — factory: ללא ארגומנטים = "מחובר בלבד, כל תפקיד"; עם תפקידים = גם בדיקת תפקיד (403 אם לא מתאים). הוחל על **כל** ה-endpoints המשנים מצב (POST/PUT/PATCH/DELETE) בכל 6 ה-routers שיש להם כאלה: `claims.py` (יצירה/עדכון/תשלום → RISK_MANAGER/CFO/ADJUSTER/ADMIN), `documents.py` (העלאה → כל מחובר; מחיקה → RISK_MANAGER/ADMIN), `media.py` (העלאה → כל מחובר; מחיקה → RISK_MANAGER/ADMIN), `mitigation.py` (יצירה/עדכון → RISK_MANAGER/PROPERTY_MANAGER/ADMIN), `incidents.py` (יצירה/עריכת טיוטה/הגשה → כל מחובר — כך שגם FIELD_WORKER יכול לדווח אירוע; שינוי סטטוס → RISK_MANAGER/PROPERTY_MANAGER/RISK_OFFICER/ADMIN), `policies.py` (הכל → RISK_MANAGER/CFO/ADMIN). נקודות GET/קריאה **לא** נחסמו בכוונה — כדי שלוחות המחוונים ימשיכו לעבוד גם לפני שהתחברות תחווט בפרונט (המשימה הבאה בשלב 6).

  Role_Permissions הקיימת (הזרועה מראש ב-seed.py) נשארת טבלת עזר תיאורית ואינה מנגנון האכיפה בפועל — האכיפה היא ישירות מול `Users.role`, לא מפתחות ההרשאה שבה; הרחבה עתידית יכולה לגשר ביניהם.

  אימות: יצירת מסמך ללא טוקן → 401; מחיקת מסמך כ-FIELD_WORKER → 403; יצירת פוליסה כ-RISK_MANAGER עם גוף לא תקין → 422 (כלומר עברה את שכבות ה-Auth וה-RBAC ונכשלה רק על ולידציית Pydantic — מוכיח שהבדיקה לא חוסמת false-positive).

- [x] **אינטגרציית SSO / Active Directory** (OAuth2 / SAML) לחיבור למערכת הזהויות הארגונית.
  📁 `backend/app/services/auth.py` + `backend/app/config.py`

  ✅ בוצע כ-stub פונקציונלי (אותו branch) — אין ספק זהות (IdP) אמיתי בסביבה זו לבדוק כנגדו, אז זה נבנה באותה קונבנציית "הידרדרות חינה" (graceful degradation) שכבר קיימת ב-`/api/ai/*` כש-`ANTHROPIC_API_KEY` חסר: `config.py` מוסיף `sso_enabled`/`sso_provider`/`sso_client_id`/`sso_client_secret`/`sso_authorize_url`/`sso_token_url`/`sso_redirect_uri` (כולם ריקים כברירת מחדל). `GET /api/auth/sso/{provider}/login` בונה URL הרשאה בסגנון OAuth2 authorization-code כאשר `SSO_ENABLED=true`, ומחזיר 501 נקי עם הודעה בעברית כברירת מחדל. `POST /api/auth/sso/{provider}/callback` — שלד בלבד (501), כי חילופי code→token מול IdP אמיתי לא ניתנים לבדיקה בלי ספק אמיתי.

  אימות: `GET /api/auth/sso/azure-ad/login` בהגדרות ברירת המחדל → 501.

- [x] **Middleware ל-Audit Log**: תיעוד אוטומטי של כל פעולת כתיבה (מי, מה, מתי, ערך קודם/חדש).
  📁 קובץ חדש: `backend/app/middleware/audit.py`

  ✅ בוצע (אותו branch). `AuditLogMiddleware` (Starlette `BaseHTTPMiddleware`, רשום ב-`main.py`) מיירט כל בקשת POST/PUT/PATCH/DELETE תחת `/api/`, מחלץ `entity_type`/`entity_id` מה-path (regex best-effort, אותה קונבנציה פולימורפית ש-`Documents`/`Audit_Log` כבר משתמשים בה), מפענח את זהות הקורא מה-JWT (ללא תלות ב-DI graph — Middleware רץ מחוץ לו), וכותב שורת `Audit_Log` עם ה-body (מקוצר ל-2000 תווים) כ-`new_value`. כשל בכתיבת ה-audit עצמה נבלע (`try/except` + `rollback`) כדי שלעולם לא ישבור בקשה אמיתית.

  אימות: לאחר מספר קריאות login/POST, `SELECT TOP 5 ... FROM Audit_Log ORDER BY log_id DESC` הראה שורות חדשות אמיתיות (`AUTH`/`POLICIES` וכו') עם `ip_address=127.0.0.1` ו-`user_id` נכון כשהיה טוקן.

- [x] **הצפנת נתונים רגישים ותצורת אבטחה**: הצפנה במנוחה לשדות פיננסיים, אכיפת HTTPS, CORS מוגבל, ניהול סודות בסביבה.
  📁 `backend/app/config.py` + `backend/.env.example`

  ✅ בוצע (אותו branch), עם היקף מצומצם באופן מכוון ומתועד: `services/kpi.py` מחשב TIV/MFL/Loss Ratio/ROI/VaR באמצעות **צבירת SQL** (SUM/AVG) ישירות על עמודות כמו `Claim_Payments.amount` — הצפנת עמודות כאלה הייתה מחייבת פענוח-ואז-צבירה ב-Python, מה שסותר את הארכיטקטורה הקיימת (וזו בדיוק הסיבה ש-CLAUDE.md ציין את הנושא כמחוץ לתחום מלכתחילה). לכן ההצפנה הוחלה על שדה פיננסי לא-מצטבר וריאלי: `Claim_Payments.reference_number` (הורחב מ-`NVARCHAR(50)` ל-`NVARCHAR(255)` כדי להכיל ciphertext). `services/encryption.py`: `EncryptedString` — SQLAlchemy `TypeDecorator` מבוסס Fernet (מפתח סימטרי מ-`FIELD_ENCRYPTION_KEY`); כתיבה מצפינה אוטומטית, קריאה מפענחת אוטומטית, ונתוני seed ישנים (שמוזרעו כטקסט רגיל לפני השינוי) ממשיכים להיקרא כרגיל בזכות fallback על `InvalidToken`. `config.py` הוסיף גם `jwt_secret_key`/`jwt_algorithm`/תוקפי טוקן, ו-`force_https` (Middleware ב-`main.py`, כבוי כברירת מחדל כי `uvicorn --reload` המקומי חסר תעודת TLS). CORS כבר היה מוגבל ל-`http://localhost:5173` בלבד — לא שונה. `.env.example` עודכן עם כל המשתנים החדשים + הוראות ליצירת מפתחות (`secrets.token_urlsafe`, `Fernet.generate_key`).

  אימות: `services/encryption.py` נבדק ישירות — `encrypt_value("TEST-ENC-42")` → ciphertext (`gAAAAAB...`), `decrypt_value(ciphertext)` → `"TEST-ENC-42"` תואם. תשלומים קיימים בטבלת `Claim_Payments` (שמוזרעו לפני ההצפנה, כטקסט רגיל כמו `PMT-10021`) נקראו בהצלחה דרך `GET /api/claims/1/payments` — מוכיח שה-fallback לנתונים לא-מוצפנים עובד.

- [x] **הגבלת קצב ו-API Key מסודר לשירותי AI**: חיזוק המנגנון הקיים והסרת מפתחות מהקוד.
  📁 `backend/app/routers/ai.py` + `backend/app/config.py`
  ✅ בוצע ב-branch `feature/ai-rate-limiting`. הערת scope: שאר `שלב 4` (מודול אימות/JWT, RBAC, SSO/AD, Audit Log middleware, הצפנה במנוחה) הושאר פתוח בכוונה — `CLAUDE.md` §"Deliberately out of scope" מגדיר במפורש RBAC/audit-log enforcement והצפנה במנוחה כמחוץ לתחום הפרויקט הזה; רק פריט זה (rate limiting + ניהול מפתחות) לא מנוגד לרשימה ההיא ולכן טופל.
  ניהול המפתח: `services/llm.py` כבר קרא ל-`Anthropic(api_key=settings.anthropic_api_key)` מ-`backend/.env` (gitignored) — אין ולא היה מפתח קשיח בקוד; לא נדרש שינוי בהיבט הזה.
  חיזוק ה-rate limiting: קובץ חדש `backend/app/services/rate_limit.py` — מגביל בזיכרון תהליך (fixed-window per client-IP, `defaultdict`), ללא תלות חיצונית (Redis וכו') כי RMIS רץ כתהליך uvicorn יחיד להדגמה. שני settings חדשים ב-`config.py`: `ai_rate_limit_per_window` (ברירת מחדל 10) ו-`ai_rate_limit_window_seconds` (ברירת מחדל 60). הופעל כ-`dependencies=[Depends(enforce_ai_rate_limit)]` ברמת ה-`APIRouter` של `routers/ai.py` כך שהוא חל אוטומטית על כל שלושת נתיבי ה-AI (`/classify-incident`, `/executive-summary`, `/ask`) בלי לגעת בכל endpoint בנפרד. חריגה ממכסה מחזירה `429` עם הודעה בעברית וכותרת `Retry-After`. נבדק בפועל דרך שרת ה-preview: 11 קריאות רצופות ל-`POST /api/ai/ask` — 10 הראשונות החזירו `503` (כצפוי, אין `ANTHROPIC_API_KEY` מוגדר בסביבת הפיתוח המקומית — הבדיקה של ה-rate limit רצה כ-dependency *לפני* בדיקת המפתח, כך שהיא נבדקת ללא תלות בקיום מפתח), הקריאה ה-11 החזירה `429` עם `Retry-After: 59` והודעה "חריגה ממכסת הבקשות ל-AI (10 בקשות ל-60 שניות)"; `GET /api/incidents` (נתיב שאינו AI) המשיך לפעול כרגיל (`200`) — מוודא שה-dependency לא דלף לנתיבים אחרים.

---

## שלב 5 — Frontend: טפסים ודיווח שטח

- [x] **שדרוג מסך דיווח אירוע לאשף שלבים (Wizard)**: 4 שלבים — זיהוי נכס, פרטי נזק, אומדן ותיאור, תיעוד מצולם.
  📁 `frontend/src/pages/IncidentReport.tsx`
  ✅ נבדק ב-branch `feature/incident-report-wizard` (ללא שינוי קוד — משימת אימות סגירה): `IncidentReport.tsx` כבר מימש אשף מלא ב-MUI `Stepper`/`Step`/`StepLabel` עם ארבעת השלבים המדויקים שהמפרט מבקש — `STEPS = ["מיקום וזיהוי הנכס", "פרטי הנזק והחומרה", "אומדן כספי ותיאור", "תיעוד ושליחה"]` — כולל באנר חירום קבוע ("במקרה של סכנת חיים חייג 102/100 מיד"), כפתורי בחירת חומרה עם קידוד צבע (ירוק/כתום-אדום/אדום כהה לפי `SEVERITY_OPTIONS`), ניתוח AI אופציונלי בשלב 3, ומסך "נשלח בהצלחה" מסכם. ולידציית מעבר שלב (`canNext`) חוסמת "המשך" עד שמולאו שדות החובה של השלב הנוכחי. נבדק בפועל דרך שרת ה-preview: ניווט ל-`/report-incident` הציג את הבאנר והסטפר עם 4 השלבים בשמות הנכונים; `Autocomplete` של הנכס נטען בהצלחה מול ה-API החי (10 נכסים מ-`RiskDB` המקומי, כולל תעתיק עברי תקין עם גרשיים ב-"מרלו״ג"/"ראשל״צ"); בחירת נכס ולחיצה על "המשך" עברה כהלכה לשלב 2, שהציג נכון את שלוש קבוצות הבחירה (סוג נזק/חומרה/סטטוס פעילות) עם התוויות העבריות המלאות. לא בוצע שינוי קוד — שאר פריטי שלב 5 (הוק GPS, רכיב העלאת מדיה אמיתי, שמירה כטיוטה ב-UI, שדה בקשת כיסוי אובדן רווחים, הרחבת טופס הפוליסה) הם סעיפים נפרדים ברשימה ונותרים פתוחים.

- [x] **זיהוי נכס לפי GPS**: קבלת מיקום המכשיר והצעת הנכסים הקרובים ברדיוס מוגדר.
  📁 קובץ חדש: `frontend/src/hooks/useGeolocation.ts` + שימוש ב-`IncidentReport.tsx`
  ✅ בוצע ונבדק ב-branch `feature/incident-geolocation`: נוסף הוק `useGeolocation` (עוטף את ה-Geolocation API של הדפדפן; לא מבקש הרשאה אוטומטית בטעינת העמוד — רק בלחיצה על כפתור "אתר את מיקומי", כדי לא להפתיע משתמש שדה) ופונקציית עזר `distanceKm` (נוסחת Haversine). ב-`IncidentReport.tsx` שלב 1 נוסף כפתור מיקום; עם קבלת קואורדינטות, כל הנכסים מסוננים לרדיוס `NEARBY_RADIUS_KM=15` ק"מ, ממוינים לפי מרחק, ומוצגים כ-Chips לבחירה מהירה (מעדכנים את שדה ה-`Autocomplete` הקיים בלחיצה). טופלו גם מצב סירוב הרשאה ומצב "אין נכסים ברדיוס" עם הודעות מתאימות. בנוסף, הקואורדינטות שהתקבלו (אם קיימות) נשלחות כעת לשרת בשדה `reported_coordinates` (שדה שכבר קיים בסכמת ה-backend, `IncidentCreate`/`IncidentOut` — רק לא נשלח מה-frontend עד כה; נוסף התאמה מקבילה ל-`IncidentCreate` ב-`frontend/src/api/client.ts`). נבדק בפועל דרך שרת ה-preview: לחיצה על הכפתור ללא הרשאת דפדפן הציגה נכון את הודעת "הגישה למיקום נדחתה — ניתן לבחור נכס ידנית"; לאחר mock של `navigator.geolocation.getCurrentPosition` לנקודת ייחוס בתל אביב, הוצגו 5 הנכסים הקרובים ממוינים נכון לפי מרחק (2.2 עד 13.9 ק"מ, כולל תעתיק עברי תקין עם גרשיים ב-"ראשל״צ"), ולחיצה על ה-chip הראשון מילאה נכון את שדה ה-`Autocomplete` ("מגדל משרדים תל אביב (PRP-002)"). `npx tsc -b` לא הראה שגיאות חדשות בקבצים שנערכו (שגיאת `stylis` הקיימת ב-`rtlCache.ts` היא קיימת מראש ואינה קשורה).

- [x] **רכיב העלאת מדיה**: צילום ישיר מהמצלמה, העלאת קבצים, תצוגה מקדימה ומחיקה.
  📁 קובץ חדש: `frontend/src/components/MediaUploader.tsx`
  ✅ בוצע ונבדק ב-branch `feature/media-uploader-component`: נוסף רכיב `MediaUploader` עצמאי (מנהל רשימת קבצים בצד הלקוח בלבד, כי העלאת מדיה בפועל דורשת `incident_id` שעדיין לא קיים בזמן שהאשף בתהליך) עם שני כפתורי הוספה — "צלם תמונה" (`<input type=file accept="image/*" capture="environment">` שפותח ישירות את מצלמת המכשיר בנייד) ו-"העלה קובץ" (בחירה מרובה, `image/*,video/*,application/pdf`) — תצוגה מקדימה עם thumbnail אמיתי לתמונות (`URL.createObjectURL`, עם `revokeObjectURL` בניקוי) ואייקון קובץ גנרי לשאר הסוגים, וכפתור מחיקה per-file. אכיפת גודל קובץ מקסימלי (25MB, תואם ל-`_MAX_UPLOAD_BYTES` ב-`backend/app/routers/media.py`) עם התראה על קבצים שנדחו. שולב ב-`IncidentReport.tsx` שלב 4 במקום קלט הקבצים המדומה הקודם (שרק אסף שמות קבצים ולא שלח לשרת). עם שליחת הדיווח, לאחר קבלת ה-`incident_id` מהשרת, כל קובץ מועלה בפועל דרך `POST /api/incidents/{id}/media` (נוספה `uploadIncidentMedia` + טיפוס `IncidentMedia` ל-`frontend/src/api/client.ts`); כשל בהעלאת קובץ בודד אינו חוסם את הצלחת הדיווח עצמו — מוצגת התראת אזהרה עם שמות הקבצים שנכשלו במסך ההצלחה. נבדק בפועל מקצה לקצה דרך שרת ה-preview מול ה-backend וה-DB המקומיים: הוזרק קובץ תמונה מדומה (`test-photo.jpg`) דרך `input[type=file]`, אומת תצוגה מקדימה ("קבצים מצורפים (1)" עם השם הנכון), האשף הושלם עד הסוף והשליחה יצרה אירוע אמיתי (`INC-2026-013`, `incident_id=29`) — יומן הרשת אישר `POST /api/incidents/29/media → 201 Created`. רשומת ה-Media שנוצרה נמחקה בסיום הבדיקה דרך `DELETE /api/media/{id}` (ניקוי נתוני בדיקה); אירוע הבדיקה עצמו (`INC-2026-013`) נשאר ב-DB המקומי (אין endpoint למחיקת אירועים) — יימחק בעת reseed הבא אם רלוונטי. `npx tsc -b` לא הראה שגיאות חדשות.

- [x] **שמירה כטיוטה ושליחה מאוחרת** במסך הדיווח.
  📁 `frontend/src/pages/IncidentReport.tsx`
  ✅ בוצע ונבדק ב-branch `feature/incident-draft-submit`: ה-backend כבר תמך בזרימת טיוטה מלאה (`is_draft` + `PATCH /api/incidents/{id}` לעריכה + `PATCH /api/incidents/{id}/submit` להגשה — ראו `backend/app/routers/incidents.py`), אך ה-frontend לא ניצל זאת. נוסף כפתור "שמור כטיוטה" בסרגל הניווט של האשף (פעיל לאחר בחירת נכס בשלב 1) — לחיצה ראשונה יוצרת אירוע עם `is_draft=true` (שדות חובה שטרם מולאו מקבלים ברירת מחדל סבירה שתידרס בשמירה הבאה: `hazard_type=OTHER`, `severity_level=LOW`, `operational_impact=FULL_OPERATION`), לחיצות הבאות שולחות `PATCH` לעדכון הטיוטה הקיימת. מזהה הטיוטה (`incident_id`) נשמר ב-`localStorage` (לא כל השדות — רק ה-id, כדי שהחזרה לטיוטה תמיד תקרא את הנתונים העדכניים מהשרת ולא עותק מקומי מיושן), כך שסגירת הטאב/רענון דף טוען אוטומטית את הטיוטה חזרה לאשף עם כל השדות ממולאים (כולל התאמת הנכס לרשימת ה-`Property` הטעונה, לצורך ה-`Autocomplete`). בשליחה סופית ("שלח דיווח למטה") — אם קיימת טיוטה, השרת מתעדכן עם השדות הסופיים דרך `PATCH` ולאחר מכן `PATCH .../submit`; אחרת מתבצעת יצירה רגילה (`is_draft=false`) כבעבר. נוספו `fetchIncident`/`updateDraftIncident`/`submitDraftIncident` + טיפוסי `IncidentUpdate`, והרחבת `Incident`/`IncidentCreate` בשדות `is_draft`/`business_interruption_requested`/`area_or_building`/`reported_coordinates` ל-`frontend/src/api/client.ts`. נבדק בפועל מקצה לקצה: נשמרה טיוטה עבור "קניון רעננה" (`INC-2026-014`), רענון דף וניווט מחדש ל-`/report-incident` טען אוטומטית את הטיוטה עם הנכס נכון ב-`Autocomplete` ("קניון רעננה (PRP-007)") והודעת "נטענה טיוטה קודמת"; המשך מילוי האשף ולחיצה על "שלח דיווח למטה" הניבה ברצף `POST /api/incidents → 201`, `GET /api/incidents/30`, `PATCH /api/incidents/30 → 200`, `PATCH /api/incidents/30/submit → 200`, כולם על אותו `incident_id`; אומת ישירות מול ה-API ש-`is_draft=false` בסיום ו-`localStorage` נוקה. `npx tsc -b` לא הראה שגיאות חדשות.

- [x] **באנר חירום ורכיבי מגע גדולים**: הודעת חירום קבועה, כפתורי Pill לבחירת סוג נזק וחומרה עם קידוד צבע (ירוק/כתום/אדום).
  📁 `frontend/src/pages/IncidentReport.tsx` + `frontend/src/theme.ts`
  ✅ בוצע ונבדק ב-branch `feature/emergency-banner-touch-targets`: באנר החירום הקבוע ("במקרה של סכנת חיים חייג 102/100 מיד") כבר היה קיים מבדיקה קודמת — הוגדל (אייקון 32px, `fontSize: 1.05rem`, ריפוד אנכי גדול יותר) לבולטות ונגישות טובה יותר במסך נייד. השינוי המהותי: כפתורי "סוג הנזק" הוחלפו מ-`ToggleButtonGroup` רגיל (ללא קידוד צבע) לכפתורי Pill עם קידוד צבע ייעודי לכל סוג נזק (הצפה=כחול, שריפה=אדום, כשל מבני=חום, פריצה=סגול, תקלת חשמל=ענבר, אחר=אפור) — תואם לעיצוב שכבר היה קיים עבור כפתורי "רמת חומרה". נוסף קבוע משותף `PILL_BUTTON_SX` (borderRadius:999, minHeight:48, py:1.5, fontSize:1rem) המוחל על כל שלוש קבוצות הבחירה בשלב 2 (סוג נזק/חומרה/סטטוס פעילות) ועל כפתורי הניווט התחתונים ("חזרה"/"שמור כטיוטה"/"המשך"/"שלח דיווח") וכפתור "אתר את מיקומי" — כולם עכשיו ≥48px גובה (מדידה בפועל: 53.6px), עומד בהמלצת יעד מגע מינימלי לשימוש שטח בנייד. **תוקן באג קיים**: התגלה שהמצב הנבחר (variant="contained" + `sx.bgcolor`) של כפתורי חומרה/סוג-נזק לא הציג בפועל רקע צבעוני — התנגשות מפרט CSS בין ברירת המחדל של MUI ל-variant `contained` לבין דריסת `sx.bgcolor` גרמה לרקע שקוף (טקסט לבן על רקע שקוף = בלתי קריא, בעייתי במיוחד עבור דיווח בשטח בזמן חירום). תוקן על ידי מעבר ל-`style` inline (המנצח תמיד את מפל ה-CSS) לצביעת הרקע/הטקסט/הגבול לפי מצב הבחירה, במקום להסתמך על `sx`/variant. נבדק בפועל: `npx tsc -b` ללא שגיאות חדשות; ניווט ל-`/report-incident`, בחירת נכס ומעבר לשלב 2 הציג 6 כפתורי סוג נזק ו-4 כפתורי חומרה עם התוויות העבריות הנכונות; אומת ב-DevTools שכפתור נבחר מקבל `background-color` ו-`color` נכונים ב-inline style (`rgb(2, 119, 189)` עבור "הצפה") ושגובה הכפתורים בפועל 53.6px עם `border-radius: 9990px` (Pill מלא).

- [x] **שדה "בקשת כיסוי אובדן רווחים"** בטופס הדיווח.
  📁 `frontend/src/pages/IncidentReport.tsx`
  ✅ בוצע ונבדק ב-branch `feature/business-interruption-checkbox`: השדה `business_interruption_requested` (עמודת DB + טיפוסי `IncidentCreate`/`IncidentUpdate`/`Incident` ב-`client.ts`) כבר היה קיים מסבב קודם, אך לא היה לו רכיב UI. נוסף `Checkbox`+`FormControlLabel` בשלב 3 של האשף ("אומדן כספי ותיאור"), ליד שדה הערכת הנזק, עם תווית ראשית ("בקשה לכיסוי אובדן רווחים (הפסקת פעילות)") וטקסט הסבר קצר. נוסף state `businessInterruption` (ברירת מחדל `false`), המוזרם ל-`createIncident`/`updateDraftIncident` בשני מסלולי השמירה (שמירה כטיוטה ושליחה סופית), נטען בחזרה בעת שחזור טיוטה קיימת (draft resume), ומתאפס במסך "דווח אירוע נוסף". כתוספת נוחות: כאשר ניתוח ה-AI (`classifyIncident`) מחזיר `business_interruption_likely: true`, התיבה מסומנת אוטומטית כהצעה (המשתמש עדיין יכול לבטל את הסימון). נבדק בפועל: `npx tsc -b` ללא שגיאות חדשות; ניווט מלא באשף עד שלב 3, אימות קיום התיבה ב-DOM (`read_page` עם `filter: all` — לא הופיעה תחת `filter: interactive` בלבד, אך אומתה ידנית), סימון התיבה, לחיצה על "שמור כטיוטה", ואימות ב-Network tab ש-`POST /api/incidents` נשלח עם `"business_interruption_requested":true` ושהתשובה מהשרת (201 Created, `incident_id:31`) מחזירה את אותו הערך — מאשר זרימה מלאה מ-UI ועד DB.

- [x] **הרחבת טופס הפוליסה**: גבול אחריות לאירוע בודד, תקופת המתנה, והחרגות.
  📁 `frontend/src/components/PolicyDialog.tsx`
  ✅ בוצע ונבדק ב-branch `feature/policy-dialog-extended-fields`: השדות `per_event_limit`/`bi_waiting_period_hours`/`exclusions` כבר היו קיימים במודל ה-DB (`InsurancePolicy`) ובטבלה מ-שלב 1, אך לא נחשפו כלל ב-API (`schemas.PolicyOut`/`PolicyCreate`/`PolicyUpdate`) ולא היה להם UI. נוספו שלושת השדות (כאופציונליים, `None`/`null` כברירת מחדל) לשלוש הסכמות ב-`backend/app/schemas.py` — הראוטר (`routers/policies.py`) כבר עובד גנרית עם `payload.model_dump()`/`setattr` כך שלא נדרש שינוי בו. בצד ה-Frontend נוספו השדות ל-`Policy`/`PolicyCreate` ב-`client.ts` (`PolicyUpdate` יורש אוטומטית דרך `Partial<PolicyCreate>`), ול-`PolicyDialog.tsx`: שני שדות מספריים ("גבול אחריות לאירוע בודד (₪)", "תקופת המתנה לאובדן רווחים (שעות)") ושדה טקסט מרובה-שורות ("החרגות פוליסה") עם placeholder לדוגמה — כולם אופציונליים, ריק נשלח כ-`null`. נבדק בפועל: `npx tsc -b` ללא שגיאות חדשות (רק פער `stylis` הקיים); שרת ה-backend התרענן אוטומטית לאחר שינוי ב-`schemas.py` (`WatchFiles ... Reloading` → `Application startup complete`, ללא תקיעה); נפתח דיאלוג "עריכת פוליסה" על פוליסה קיימת מה-seed data ואומת שהשדות מוצגים ומתמלאים נכון (`5000000`, `72`, טקסט החרגות בעברית) הן ב-DOM (`querySelector` על השדות בפועל) והן דרך Network — `PUT /api/policies/1` נשלח עם הערכים החדשים והתשובה (200 OK) החזירה אותם זהים. אומת גם שביטול הדיאלוג ("ביטול") לא שולח בקשה.

---

## שלב 6 — Frontend: דשבורד, מפה ודוחות

- [x] **שכבות מפה ניתנות להדלקה/כיבוי**: נכסים, אירועים פעילים, אזורי הצפה/שריפה.
  📁 `frontend/src/components/RiskMap.tsx`
  ✅ בוצע ונבדק ב-branch `feature/risk-map-layers`: `RiskMap` הורחב מרכיב מפה קבוע לרכיב עם ארבע שכבות הניתנות להדלקה/כיבוי עצמאית דרך `Switch` (MUI) מעל המפה: (1) **נכסים** — סמני העיגול הקיימים (`CircleMarker` צבוע לפי `status_color`), ללא שינוי בלוגיקה הקיימת. (2) **אירועים פעילים** — שכבה חדשה: מציגה כל אירוע שאינו טיוטה (`is_draft=false`) בסטטוס `NEW`/`UNDER_INVESTIGATION`/`CLAIM_FILED` (לא `CLOSED`) כסמן סגול קטן, עם Popup המציג קוד אירוע, סוג נזק ואומדן נזק; המיקום נגזר מ-`reported_coordinates` (שדה "lat,lng" מ-שלב 1) כשקיים, ונופל חזרה לקואורדינטות הנכס המשויך (מתוך `points`) כשלא — כדי לא "לאבד" אירועים ללא GPS מדווח. התווית מציגה גם ספירה חיה (`אירועים פעילים (19)` בנתוני ה-seed). (3)+(4) **אזורי הצפה/שריפה** — שתי שכבות נפרדות (ולא שכבה משולבת אחת, לגמישות ויזואלית) המציירות `Circle` סביב כל נכס בעל פרופיל סיכון (`Asset_Risk_Profiles`), עם רדיוס יחסי לציון הסיכון (`flood_risk_score`/`fire_risk_score`, סולם 1–5 לפי `CHECK` constraint ב-`schema.sql`) כפול קבוע `HAZARD_ZONE_METERS_PER_SCORE=900` מטר (הנחה ויזואלית מתועדת בקוד להדגמת ריכוזיות סיכון יחסית, לא נתוני אזורי הצפה/שריפה רשמיים — GIS חיצוני כזה מוצהר "מחוץ לתחום" בשלב 8/`docs/README.md` §8) — כחול שקוף להצפה, כתום-אדום שקוף לשריפה. שתי השכבות כבויות כברירת מחדל (כדי לא להעמיס ויזואלית על הדשבורד בטעינה ראשונית), שכבות הנכסים והאירועים דלוקות כברירת מחדל. `Dashboard.tsx` הורחב לשלוף גם `fetchProperties`/`fetchIncidents` (שאילתות `react-query` נוספות: `["properties"]`, `["incidents","all"]`) ולהעבירן ל-`RiskMap` כ-props אופציונליים (`properties`, `incidents`) — כדי לא לשבור קריאות קיימות אחרות ל-`RiskMap` (אין כאלה בפועל, אך השאיר את הרכיב גמיש). נבדק: `npx tsc -b` ללא שגיאות חדשות (רק פער `stylis` הקיים). נבדק בפועל מול `RiskDB` מקומי דרך שרתי preview: כל בקשות ה-API (`/api/properties`, `/api/incidents`, `/api/analytics/map` וכו') חזרו `200`; ארבעת מתגי השכבה נטענו עם התוויות הנכונות (כולל הספירה החיה של אירועים פעילים); כיבוי שכבת הנכסים והדלקה בלבד של אירועים פעילים הותירה 19 צורות SVG במפה (תואם ל-19 האירועים הפעילים בנתוני ה-seed); הדלקה נוספת של שכבת אזורי הצפה הוסיפה 15 צורות נוספות (34 סה"כ), תואם ל-15 הנכסים הפעילים בעלי פרופיל סיכון. שגיאות `ERR_CONNECTION_REFUSED` שנצפו בקונסול שויכו לבקשות אריח מפה חיצוניות ל-`tile.openstreetmap.org` (חסומות בסביבת הבדיקה הארגזית) — התנהגות קיימת מראש ברכיב, לא רגרסיה מהשינוי הנוכחי.

- [x] **הצגת אשכולות חשיפה מרחבית** (עיגולי ריכוז TIV) על גבי המפה.
  📁 `frontend/src/components/RiskMap.tsx`
  ✅ בוצע ונבדק ב-branch `feature/map-exposure-clusters-layer`: ה-endpoint `GET /api/analytics/geographic-exposure-clusters` (נבנה ונבדק בשלב 3, אך לא נצרך עד כה ב-Frontend) חשוף כעת דרך פונקציה חדשה `fetchGeographicExposureClusters` + טיפוס `GeographicExposureCluster` ב-`client.ts`. נוספה ל-`RiskMap` שכבה חמישית ("אשכולות חשיפה (TIV)") — `Circle` מקווקו (סגול, `dashArray`) סביב מרכז כל אשכול (`center_lat`/`center_lon`), ברדיוס `radius_km` בפועל (מרחק מהמרכז לחבר הרחוק ביותר) עם רצפה של 1.5 ק"מ כדי שאשכולות צפופים יישארו נראים בזום תיק כולל; Popup מציג את שמות הנכסים באשכול, TIV מצטבר ו-MFL מצטבר. מוצגים רק אשכולות רב-נכסיים (`property_count > 1`) — אשכולות של נכס בודד (`radius_km=0`, קיימים ב-`calculate_geographic_exposure_clusters` הקיימת) אינם "ריכוז" משמעותי ומוצגים ממילא דרך שכבת "נכסים" הרגילה. השכבה כבויה כברירת מחדל, כמו שכבות ההצפה/שריפה. `Dashboard.tsx` הורחב לשלוף `["geographic-exposure-clusters"]` דרך `react-query` ולהעביר כ-prop אופציונלי חדש (`exposureClusters`) ל-`RiskMap`. נבדק: `npx tsc -b` ללא שגיאות חדשות (רק פער `stylis` הקיים). נבדק בפועל מול `RiskDB` מקומי דרך שרתי preview: `GET /api/analytics/geographic-exposure-clusters` החזיר `200`; תווית המתג הציגה ספירה נכונה (3 אשכולות רב-נכסיים בנתוני ה-seed); כיבוי/הדלקת השכבה שינה את מספר צורות ה-SVG במפה מ-34 ל-37 (3+), ואומת ב-DOM שבדיוק 3 צורות חדשות הן ה-`Circle` המקווקווים (`stroke-dasharray`) הייחודיים לשכבה זו. שגיאות `ERR_CONNECTION_REFUSED` הן בקשות אריח מפה חיצוניות חסומות בסביבת הבדיקה, לא רגרסיה.

- [x] **Popup מפורט בלחיצה על נכס**: שווי, מנהל אחראי, פוליסה פעילה וגבול אחריות.
  📁 `frontend/src/components/RiskMap.tsx`
  ✅ בוצע ונבדק ב-branch `feature/property-popup-manager-policy`: התגלה שה-DB כבר כולל את כל הנתונים הדרושים (`Properties.primary_manager_id → Users`, `Policy_Assets ↔ Insurance_Policies`) אך הם לא היו חשופים ב-API. לכן המשימה כללה שינוי Backend קטן ומכוון בנוסף ל-Frontend: נוספו יחסי SQLAlchemy `Property.primary_manager` ו-`Property.policy_assets` (עם `PolicyAsset.policy`/`PolicyAsset.property_`) ב-`models.py`; נוסף סכמת `PropertyActivePolicyOut` (policy_number, insurer_name, total_limit, per_event_limit, specific_deductible) ושדות חדשים `manager_name`/`active_policy` ל-`PropertyOut` ב-`schemas.py`; `routers/properties.py` עודכן כך שבשני ה-endpoints (`GET /api/properties`, `GET /api/properties/{id}`) נטענים `joinedload` עבור המנהל וה-policy_assets, ופונקציית עזר `_to_property_out` בוחרת את הפוליסה "הפעילה" (status=`ACTIVE`, עם `end_date` הרחוק ביותר אם יש יותר מאחת) ומרכיבה את התשובה. ב-Frontend: `Property`/`PropertyActivePolicy` ב-`client.ts` הורחבו בהתאם; ב-`RiskMap.tsx` נוסף `Map` חיפוש `propertiesById` (property_id → Property המלא, כולל השדות החדשים) כדי להעשיר את ה-Popup הקיים של שכבת "נכסים" — מעבר לשווי כינון ואירועים פתוחים (היו קיימים), נוספו שורות "מנהל אחראי" ו"פוליסה פעילה" (מספר פוליסה + מבטח) ו"גבול אחריות" (`per_event_limit`, עם נפילה חזרה ל-`total_limit` אם אין גבול-לאירוע נפרד); כשאין מנהל/פוליסה משויכים מוצג טקסט חלופי מפורש ("לא משויך"/"אין פוליסה פעילה משויכת") במקום שדה ריק. נבדק: `npx tsc -b` ללא שגיאות חדשות (רק פער `stylis` הקיים); Python `ast.parse` על שלושת קבצי ה-Backend שהשתנו — תקין. נבדק בפועל מול `RiskDB` מקומי דרך שרתי preview (כולל אימות שה-`--reload` של uvicorn קלט את שלושת השינויים בהצלחה ולא נתקע): `GET /api/properties` החזיר `200` עם `manager_name`+`active_policy` מלאים לכל 15 הנכסים ב-seed; לחיצה מדומה (dispatch `MouseEvent`) על סמן הנכס "מרלו"ג מודיעין" פתחה Popup שהציג בפועל: מנהל אחראי — מיכל אזולאי, פוליסה פעילה — POL-2026-CENTRAL (הפניקס ביטוח), גבול אחריות — ₪5.0M — תואם 1:1 לתשובת ה-API. שגיאות `ERR_CONNECTION_REFUSED` הן בקשות אריח מפה חיצוניות חסומות בסביבת הבדיקה, לא רגרסיה.

- [x] **מסך תיק אירוע מלא (Drill-down)**: תמונות מהשטח, דוח שמאי, תביעה משויכת ותשלומים.
  📁 קובץ חדש: `frontend/src/pages/IncidentDetail.tsx` + ניתוב ב-`frontend/src/App.tsx`
  ✅ בוצע ונבדק ב-branch `feature/incident-drilldown-page`: התגלה שה-endpoint המאוחד `GET /api/incidents/{id}/full` (סכמת `IncidentDrillDown` — אירוע + מדיה + תביעות עם תשלומים + מסמכים מצורפים ישירות לאירוע) כבר קיים ונבנה מראש ב-Backend אך מעולם לא נצרך ב-Frontend — המשימה הייתה כמעט כולה Frontend. נוספו ל-`client.ts`: טיפוסי `DocumentFile`, `SignedUrl`, `ClaimWithPayments`, `IncidentDrillDown`, ופונקציות `fetchIncidentDrilldown`, `fetchMediaSignedUrl`, `fetchDocumentsForEntity`, `fetchDocumentSignedUrl` (חשיפה ראשונה של חוזה ה-signed-URL הדו-שלבי הקיים ב-Backend — `GET .../signed-url` ואז `GET` בפועל מה-`download_url` שחוזר — גם עבור מדיית אירוע וגם עבור מסמכים). נוסף עמוד חדש `IncidentDetail.tsx` בנתיב `/incidents/:id`: כרטיס פרטי האירוע (סטטוס/חומרה/סוג נזק/השפעה תפעולית/דגל אובדן רווחים, נכס, תאריך, אומדן נזק, סיווג AI/ידני, תיאור מלא), גלריית מדיה מהשטח (תמונות ממותגות דרך `signed-url`, עם מיקום GPS אם קיים), מסמכים המצורפים ישירות לאירוע, ולכל תביעה משויכת — כרטיס נפרד עם סכומים (נתבע/השתתפות עצמית/מאושר/שולם), שם שמאי אחראי, טבלת תשלומים, ורשימת מסמכי התביעה (כולל "דוח שמאי" — התגלה שבנתוני ה-seed מסמכי `ADJUSTER_REPORT` מצורפים ל-`entity_type=CLAIM` ולא ל-`INCIDENT`, ולכן נוספה שליפת מסמכים נפרדת per-claim ב-`ClaimCard`, לא רק לרמת האירוע). נוסף כפתור "תיק אירוע מלא" (עין) בכל שורה ב-`IncidentsTable.tsx` המנווט לעמוד החדש, וכפתור חזרה בעמוד עצמו. נבדק: `npx tsc -b` ללא שגיאות חדשות (רק פער `stylis` הקיים). נבדק בפועל מול `RiskDB` מקומי דרך שרת preview: ניווט מרשימת האירועים לתיק אירוע ספציפי (`INC-2025-001`) הציג נכון את כל הפרטים כולל התביעה `CLM-2025-01` עם שני תשלומים (מקדמה + סילוק סופי) ומסמך "דוח שמאי" עם קישור "פתיחה" פעיל (`GET /api/documents/4/signed-url` החזיר `200`); אירוע ללא תביעה (`INC-2026-013`) הציג נכון מצב ריק "טרם הוגשה תביעה"; כפתור החזרה ניווט בהצלחה בחזרה לרשימת האירועים. שגיאות `ERR_CONNECTION_REFUSED` הן בקשות אריח מפה חיצוניות (מהדשבורד, לא מעמוד זה) חסומות בסביבת הבדיקה, לא רגרסיה.

- [x] **מסך/כרטיס תזרים ורזרבות**: גרף צפי תקבולים לפי חודש מול רזרבות פתוחות.
  📁 קובץ חדש: `frontend/src/components/CashflowChart.tsx` + `frontend/src/pages/Dashboard.tsx`
  ✅ בוצע ונבדק ב-branch `feature/cashflow-reserves-chart`: התגלה (בדומה למשימות הקודמות) שה-Backend כבר כלל שירות מלא ומתועד היטב — `backend/app/services/cashflow.py` (`get_cashflow_summary`, `calculate_expected_receipts_by_month`, `calculate_reserves_by_month`) וה-endpoint `GET /api/analytics/cashflow` (סכמות `CashflowSummary`/`CashflowMonthPoint` ב-`schemas.py`) — אך מעולם לא נצרך ב-Frontend. המשימה הייתה כולה Frontend: נוספו ל-`client.ts` הטיפוסים `CashflowMonthPoint`/`CashflowSummary` ופונקציית `fetchCashflowSummary(monthsAhead=12)`. נוצר רכיב חדש `CashflowChart.tsx`: שלוש כרטיסיות סיכום (סך רזרבות פתוחות, סך תקבולים צפויים, רזרבות ללא תאריך תשלום צפוי — "unscheduled") מעל גרף `ComposedChart` של recharts (Bar לתקבולים צפויים + Line לרזרבות פתוחות לפי חודש, בהשראת `LossRatioTrendChart.tsx` הקיים), עם עיצוב עמודות/תוויות בעברית ו-Tooltip מותאם RTL. הרכיב שולב ב-`Dashboard.tsx` ככרטיס נוסף מתחת לגרף מגמת ה-Loss Ratio, עם `useQuery` נפרד (`queryKey: ["cashflow"]`). נבדק: `npx tsc -b` ללא שגיאות חדשות (רק פער `stylis` הקיים). נבדק בפועל מול `RiskDB` מקומי דרך שרת preview: `GET /api/analytics/cashflow?months_ahead=12` החזיר `200` פעמיים (React StrictMode); הדשבורד הציג נכון את שלוש כרטיסיות הסיכום (₪3.0M רזרבות פתוחות, ₪450K תקבולים צפויים, ₪1.6M ללא תיזמון) ואת הגרף עם שני חודשים מתוזמנים (09/26, 10/26) התואמים את נתוני התביעות הפתוחות המוצגות בטבלה שמתחת. שגיאות `ERR_CONNECTION_REFUSED` הן בקשות אריח מפה חיצוניות (מרכיב המפה בדשבורד, לא קשור לגרף זה) חסומות בסביבת הבדיקה, לא רגרסיה.

- [x] **טבלת חשיפה לפי אזורים** בדוח ההנהלה.
  📁 `frontend/src/components/ExecutiveReportPrintable.tsx`
  ✅ בוצע ונבדק ב-branch `feature/exposure-by-region-report-table`: התגלה (שוב, אותה תבנית חוזרת) שה-endpoint `GET /api/analytics/exposure-by-region` (סכמת `RegionExposure` — region_name, tiv, mfl, total_claimed, כולל קיבוץ נכסים ללא region_id תחת רשומת "unassigned" סינתטית) כבר קיים ומחושב במלואו ב-`services/kpi.py` (`calculate_exposure_by_region`) אך מעולם לא נצרך ב-Frontend בשום מקום. נוספו ל-`client.ts`: טיפוס `RegionExposure` ופונקציית `fetchExposureByRegion`. `ExecutiveReportPrintable.tsx` קיבל פרופ חדש `regions: RegionExposure[]` וטבלת "חשיפה לפי אזורים" חדשה (אזור / TIV / MFL / סך נתבע) שהוכנסה בין כרטיסיות ה-KPI לטבלת התביעות הקיימת, באותו סגנון HTML פשוט (לא MUI) שמשמש את שאר הרכיב עבור לכידת html2canvas. `Reports.tsx` עודכן: `useQuery` נוסף (`queryKey: ["exposure-by-region"]`), הועבר כ-prop לרכיב, וכפתור "ייצוא ל-PDF" הותנה גם בטעינת נתון זה (`disabled` כולל `!exposureByRegion.data`) כדי שהדוח לא ייוצא חלקי. נבדק: `npx tsc -b` ללא שגיאות חדשות (רק פער `stylis` הקיים). נבדק בפועל מול `RiskDB` מקומי דרך שרת preview: `GET /api/analytics/exposure-by-region` החזיר `200` עם 3 אזורים (TIV/MFL/total_claimed תואמים לנתוני ה-seed); אימות DOM אישר שהטבלה "חשיפה לפי אזורים" מופיעה בפועל בתוך ה-`div` הבלתי-נראה (off-screen) שמוזן ל-`exportElementToPdf` לפני טבלת "תביעות"; לחיצה בפועל על "ייצוא ל-PDF" הפעילה את html2canvas בהצלחה (שני מעברי רינדור נצפו ב-console, ללא שגיאות בתהליך הייצוא עצמו) והורדת קובץ ה-PDF החלה כצפוי. שגיאת `503` שנצפתה ב-console מקורה בבקשת `GET /api/ai/executive-summary` (ללא `ANTHROPIC_API_KEY` מוגדר — התנהגות תקינה ומתועדת, לא רגרסיה); שגיאות `ERR_CONNECTION_REFUSED` הן רעש קבוע של אריחי מפה חיצוניים מהדשבורד, לא רגרסיה.

- [x] **מסך סימולציה ו-VaR**: הרצת סימולציה, הצגת התפלגות תוצאות ואחוזוני VaR.
  📁 קובץ חדש: `frontend/src/pages/Simulation.tsx`
  ✅ בוצע ונבדק ב-branch `feature/simulation-var-screen`: התגלה (שוב, אותה תבנית חוזרת) שה-Backend כבר כלל מודול סימולציית מונטה קרלו מלא ומתועד היטב — `backend/app/services/simulation.py` (`run_portfolio_simulation`, `simulate_property`: הסתברות אירוע שנתית לפי ציון סיכון מורכב לנכס, חומרה מדגם התפלגות משולשת עד ה-MFL, VaR95/VaR99 כאחוזונים, היסטוגרמת תוצאות) ו-router נפרד `backend/app/routers/simulation.py` (`GET /api/simulation/portfolio`, `GET /api/simulation/properties/{id}`, כבר רשום ב-`main.py`) — אך מעולם לא נצרך ב-Frontend (`client.ts` לא הכיל כל אזכור). המשימה הייתה כולה Frontend: נוספו ל-`client.ts` הטיפוסים `HistogramBucket`/`PortfolioSimulationResult`/`PropertySimulationResult` ופונקציות `fetchPortfolioSimulation`/`fetchPropertySimulation`. נוצר רכיב חדש `SimulationDistributionChart.tsx` — היסטוגרמת `BarChart` של recharts עם קווי ייחוס (`ReferenceLine`) המסמנים את מיקומי VaR95/VaR99 בתוך צורת ההתפלגות. נוצר עמוד חדש `Simulation.tsx`: בורר "היקף" (כל התיק / נכס בודד מתוך `fetchProperties`), בורר מספר הרצות (1K–25K) ואופק שנים (1–50, תואם את מגבלות ה-backend), כפתור "הרץ סימולציה" המפעיל `useQuery` מותנה (`enabled`) לפי ההיקף שנבחר, ארבע כרטיסיות KPI (הפסד שנתי צפוי, תרחיש גרוע, VaR 95%, VaR 99%) וגרף ההתפלגות. נוסף ניתוב `/simulation` ב-`App.tsx` וקישור ניווט "סימולציה ו-VaR" ב-`Layout.tsx`. נבדק: `npx tsc -b` ללא שגיאות חדשות (רק פער `stylis` הקיים). נבדק בפועל מול `RiskDB` מקומי דרך שרת preview: `GET /api/simulation/portfolio?iterations=10000&horizon_years=1` החזיר `200` עם 15 נכסים מסומלצים, הפסד שנתי צפוי ₪11.5M, VaR95 ₪30.6M, VaR99 ₪41.2M וגרף היסטוגרמה עם קווי VaR מסומנים; לאחר בחירת נכס בודד ("מרלו\"ג מודיעין") ולחיצה חוזרת, `GET /api/simulation/properties/1?iterations=10000&horizon_years=1` החזיר `200` עם ערכים תואמי-היקף קטן יותר (₪979K הפסד צפוי, ₪17.5M תרחיש גרוע). שגיאות `ERR_CONNECTION_REFUSED` ו-`503` שנצפו הן הרעש הידוע (אריחי מפה חיצוניים ו-`GET /api/ai/executive-summary` ללא `ANTHROPIC_API_KEY`), לא רגרסיה.

- [x] **מסך/רכיב אופטימיזציית השתתפות עצמית**: מחשבון "לספוג או לתבוע".
  📁 קובץ חדש: `frontend/src/components/RetentionCalculator.tsx`
  ✅ בוצע ונבדק ב-branch `feature/retention-calculator`: זו הפעם השישית ברציפות שבה מתגלה אותה תבנית — ה-Backend כבר כלל מודול השוואה מלא ומתועד (`backend/app/services/retention.py`: `get_effective_deductible`, `get_active_policy_for_property`, `calculate_retention_recommendation`, `suggest_for_incident`) עם router רשום (`backend/app/routers/retention.py`: `GET /api/retention/recommendation`, `GET /api/retention/incidents/{id}`) וסכמת `RetentionRecommendation` ב-`schemas.py`, אך `client.ts` לא הכיל כל אזכור. המשימה הייתה כולה Frontend: נוסף ל-`client.ts` הטיפוס `RetentionRecommendation` ופונקציית `fetchRetentionRecommendation({policy_id, property_id, estimated_loss})`. נוצר הרכיב `RetentionCalculator.tsx`: בורר "נכס מבוטח" המסונן ל-`Property` בעלי `active_policy` בלבד (שדה זה כבר קיים ב-`Property` מ-`fetchProperties` — כולל `policy_id`, כך שאין צורך בבורר פוליסה נפרד), שדה "נזק משוער (₪)" עם ברירת-מחדל אוטומטית מ-`risk_profile.mfl_amount` של הנכס שנבחר, וכפתור "חשב המלצה" המפעיל `useQuery` מותנה. בעת קבלת תוצאה: `Chip` צבעוני עם ההמלצה (`ABSORB`/`CLAIM`), שורת חיסכון צפוי, ושתי כרטיסיות השוואה זו-לצד-זו ("ספיגה עצמית" מול "הגשת תביעה" — השתתפות עצמית, סכום בר-החזר, תשלום מכיס, תוספת פרמיה עתידית, עלות כוללת), כאשר החלופה המומלצת מודגשת במסגרת צבעונית. נוצר עמוד עוטף `Retention.tsx` עם כותרת, נוסף ניתוב `/retention` ב-`App.tsx` וקישור ניווט "השתתפות עצמית" ב-`Layout.tsx`. נבדק: `npx tsc -b` ללא שגיאות חדשות (רק פער `stylis` הקיים). נבדק בפועל מול `RiskDB` מקומי דרך שרת preview: בחירת "מרלו\"ג מודיעין" (בעלת פוליסה `POL-2026-CENTRAL`) מילאה אוטומטית נזק משוער ₪18,000,000 (=`mfl_amount`); `GET /api/retention/recommendation` החזיר `200` עם המלצת `CLAIM` — השתתפות עצמית ₪100,000, סכום בר-החזר ₪5,000,000 (מוגבל ע"י `per_event_limit`), תשלום מכיס ₪13,000,000, תוספת פרמיה ₪750,000, עלות כוללת ₪13,750,000 מול ₪18,000,000 בספיגה עצמית — חיסכון ₪4,250,000, תואם ידנית לנוסחת המודל. נבדק גם מקרה קצה: נזק משוער ₪50,000 (מתחת להשתתפות העצמית ₪100,000) → המלצת `ABSORB`, סכום בר-החזר ₪0, חיסכון ₪0, כצפוי מהמודל. שגיאות `ERR_CONNECTION_REFUSED` שנצפו הן הרעש הידוע (אריחי מפה חיצוניים), לא רגרסיה.

- [x] **השלמת ניהול משימות מיטיגציה ב-UI**: יצירה, עריכה, שיוך מבצע, סימון ביצוע והצגת ROI.
  📁 `frontend/src/pages/Mitigation.tsx` + `frontend/src/components/MitigationTable.tsx`
  ✅ בוצע ונבדק ב-branch `feature/mitigation-task-management-ui`: זו הפעם השביעית ברציפות שבה מתגלה אותה תבנית — ה-Backend כבר כלל CRUD מלא ומתועד (`backend/app/routers/mitigation.py`: `GET/POST /api/mitigation-tasks`, `PATCH /api/mitigation-tasks/{id}`, `GET /api/mitigation-tasks/{id}/roi` עם חישוב `_sync_overdue` אוטומטי; `backend/app/services/kpi.py`: `calculate_mitigation_roi_breakdown`) ו-schemas מלאים (`MitigationTaskCreate`/`MitigationTaskUpdate`/`MitigationRoiBreakdown`), אך `client.ts` חשף רק `fetchMitigationTasks` לקריאה בלבד — ללא יצירה, עדכון או פירוט ROI. בנוסף התגלה שלא היה כלל endpoint לרשימת משתמשים במערכת (נדרש לתפריט "שיוך מבצע"), אף שהטבלה `Users` קיימת ומשמשת FK-ים קיימים (`assigned_to_user_id` וכו').

  **Backend (תוספת נקודתית, לא CRUD משתמשים מלא):** נוסף router חדש **read-only** בלבד — `backend/app/routers/users.py` (`GET /api/users`, ממוין לפי שם) עם סכמת `UserOut` חדשה ב-`schemas.py` (`user_id`, `full_name`, `role` בלבד — ללא `email`/`created_at` ובוודאי ללא כל מנגנון התחברות/הרשאות). זהו **לא** צעד לכיוון שלב 4 (Auth/RBAC) שהוחלט במפורש לדלג עליו — מדובר בבורר קריאה-בלבד לתצוגת שם, לא באימות, session, סיסמה או אכיפת הרשאות. נרשם ב-`main.py`.

  **Frontend:** `client.ts` — נוספו `assigned_to_user_id` ל-`MitigationTask`, טיפוסי `MitigationTaskCreate`/`MitigationTaskUpdate`/`MitigationRoiBreakdown`/`User`, ופונקציות `createMitigationTask`/`updateMitigationTask`/`fetchMitigationTaskRoi`/`fetchUsers`. נוצרו שני דיאלוגים חדשים: **`MitigationTaskDialog.tsx`** (יצירה/עריכה במצב יחיד — בורר נכס בעריכה נעול, שדות כותרת/עלות/חיסכון/תאריך יעד, בורר "גורם מבצע" מ-`fetchUsers`, ובעריכה בלבד גם בורר סטטוס) ו-**`MitigationRoiDialog.tsx`** (מציג את פירוט ה-ROI המלא מה-endpoint הייעודי: חיסכון פרמיה/אובדנים בנפרד, ROI, תקופת החזר השקעה, והתרעה אם אין לנכס פוליסה פעילה). `MitigationTable.tsx` הורחבה בעמודת "גורם מבצע" ובעמודת "פעולות" עם שלושה כפתורים לכל שורה: עריכה (פותח את `MitigationTaskDialog`), פירוט ROI (פותח את `MitigationRoiDialog`), וסימון-כבוצע מהיר (PATCH ישיר ל-`status=COMPLETED`, מנוטרל אם המשימה כבר הושלמה). `Mitigation.tsx` חובר: כפתור "משימה חדשה" חדש, `useMutation` למחיקת/עדכון סטטוס מהיר, ו-`useQuery` נוסף ל-`fetchUsers` לצורך תרגום `assigned_to_user_id` לשם בטבלה.

  נבדק: `npx tsc -b` ללא שגיאות חדשות (רק פער `stylis` הקיים). נבדק בפועל מול `RiskDB` מקומי דרך שרת preview: `GET /api/users` החזיר `200` עם 9 משתמשים בעברית תקינה, ממוינים לפי שם. מסך המשימות הציג נכון 12 משימות עם עמודת "גורם מבצע" מאוכלסת. **עריכה**: פתיחת דיאלוג עריכה על "התקנת שסתומים אל-חוזרים..." מילאה נכון את כל השדות (כולל "מיכל אזולאי" כגורם מבצע ו-141.2% ROI קיים); שינוי סטטוס ל"הושלמה" ולחיצת "שמירה" עדכן את הטבלה מיידית — המשימה זזה לקבוצת "הושלמה" ועלות "פתוחות" ירדה מ-₪1.6M ל-₪1.5M. **יצירה**: מילוי טופס משימה חדשה (נכס "מרכז לוגיסטי אשדוד", כותרת, עלות ₪30,000, חיסכון ₪15,000, תאריך יעד, גורם מבצע) ולחיצת "יצירה" הוסיף בהצלחה משימה 13 עם ROI מחושב אוטומטית (50.0%) וסטטוס ברירת מחדל "פתוחה". **סימון כבוצע מהיר**: לחיצה על כפתור הפעולה השלישי בשורה "התקנת מערכת גילוי וכיבוי אש" (בטיפול) עדכנה אותה מיידית ל"הושלמה" ללא פתיחת דיאלוג, ועלות "פתוחות" ירדה בהתאם ל-₪1.4M. **פירוט ROI**: לחיצה על כפתור "פירוט ROI" פתחה דיאלוג עם פירוט מלא (חיסכון פרמיה ₪48,000 + חיסכון אובדנים ₪72,000 = ₪120,000, ROI 141.2%, תקופת החזר 0.7 שנים). כל הפעולות תואמות ל-RTL ויזואלית (צילום מסך). שגיאות `ERR_CONNECTION_REFUSED` שנצפו הן הרעש הידוע (אריחי מפה חיצוניים), לא רגרסיה.

- [x] **מסך ניהול מסמכים**: העלאה וצפייה בפוליסות, דוחות שמאי ותכתובות.
  📁 קובץ חדש: `frontend/src/pages/Documents.tsx`
  ✅ בוצע ונבדק ב-branch `feature/documents-management-screen`: כמו ברוב משימות שלב 6, ה-Backend כבר כלל DMS מלא (`routers/documents.py`: `POST/GET /entity/{type}/{id}`, `DELETE /{id}`, `GET /{id}/signed-url`, `GET /download`) אך `client.ts` חשף רק קריאה (`fetchDocumentsForEntity`/`fetchDocumentSignedUrl`, מ-`IncidentDetail.tsx`) — ללא העלאה/מחיקה. נוספו `uploadDocument`/`deleteDocument` ל-`client.ts`. נוצר עמוד חדש `Documents.tsx`: בורר סוג ישות (אירוע/תביעה/נכס/פוליסה) + `Autocomplete` לבחירת ישות ספציפית (מציג incident_code/claim_number/property name/policy_number בהתאם), טבלת מסמכים לישות הנבחרת עם קישור פתיחה/הורדה (חוזה signed-URL דו-שלבי קיים) וכפתור מחיקה לכל שורה, וטופס העלאה (בחירת קובץ, סוג מסמך מתוך `DOCUMENT_TYPE_LABELS`, "הועלה על ידי" אופציונלי מ-`fetchUsers`). נוסף ניתוב `/documents` ב-`App.tsx` וקישור ניווט "מסמכים" ב-`Layout.tsx`. נבדק: `npx tsc -b` ללא שגיאות חדשות (רק פער `stylis` הקיים). נבדק בפועל מול `RiskDB` מקומי דרך שרת preview: בחירת נכס "מרלו\"ג מודיעין" הציגה נכון את מסמך ה-seed הקיים ("דוח סקר", 19.08.2026) עם קישור פתיחה/הורדה תקין (`GET /api/documents/entity/PROPERTY/1 → 200`, `GET /api/documents/9/signed-url → 200`); מעבר סוג ישות ל"אירוע" רענן נכון את רשימת האפשרויות ל-`Autocomplete` (קודי אירוע מה-seed). לא בוצעה בדיקת העלאת קובץ אוטומטית בדפדפן (input type=file לא נתמך בכלי האוטומציה הזמין), אך הפונקציה `uploadDocument` היא עטיפה ישירה על אותו `POST /entity/{type}/{id}` שכבר אומת מקצה לקצה במשימת ה-Backend (כולל בדיקת גודל/`404`/`422`).

- [x] **מסכי התחברות וניהול משתמשים**: מסך Login, הצגת תפקיד, והסתרת תפריטים לפי הרשאה.
  📁 קבצים חדשים: `frontend/src/pages/Login.tsx`, `frontend/src/auth/AuthContext.tsx` + `frontend/src/components/Layout.tsx`
  ✅ בוצע ונבדק ב-branch `feature/login-auth-screens`: כמו ברוב משימות שלב 6, ה-Backend כבר כלל אימות JWT מלא ומתועד משלב 4 (`routers/auth.py`: `POST /login`, `POST /refresh`, `POST /logout`, `GET /me`; `dependencies/permissions.py`: `get_current_user`/`require_roles(*roles)` מוחל על endpoints של כתיבה ב-`claims.py`/`incidents.py`/`mitigation.py`/`policies.py`/`documents.py`/`media.py`) אך לא היה שום קוד Frontend שמאמת מולו — כל קריאות ה-API יצאו ללא `Authorization` header.

  **`client.ts`**: נוספה שכבת ניהול טוקנים (`getAccessToken`/`setTokens`/`clearTokens` מעל `localStorage`) + שני axios interceptors: request interceptor שמצרף `Authorization: Bearer` לכל קריאה, ו-response interceptor שעל `401` מנסה **רענון שקט חד-פעמי** מול `POST /api/auth/refresh` וחוזר על הקריאה המקורית (עם דה-דופ לקריאות מקבילות דרך `refreshInFlight` משותף), ורק אם גם הרענון נכשל משדר אירוע גלובלי `rmis:auth:logout` ומנקה טוקנים. נוספו טיפוסי `LoginRequest`/`CurrentUser`/`TokenPair`/`AccessToken` ופונקציות `login`/`logout`/`fetchCurrentUser`.

  **`auth/AuthContext.tsx`** (חדש): קונטקסט React עם `user`/`loading`/`login`/`logout`. בעליית האפליקציה, אם קיים טוקן ב-`localStorage` הוא מאומת מול `GET /auth/me` (לא נסמך עליו סתם — טוקן ישן עלול להיות פג); מאזין לאירוע `rmis:auth:logout` מה-interceptor כדי לאפס session מבלי שכל call-site יבדוק `401` בעצמו.

  **`pages/Login.tsx`** (חדש): טופס אימייל/סיסמה עם MUI, מציג שגיאה מהשרת (`err.response.data.detail`, כולל "אימייל או סיסמה שגויים" בעברית מ-`routers/auth.py`), מצב טעינה בזמן שליחה.

  **`App.tsx`/`main.tsx`**: `AuthProvider` עוטף את כל האפליקציה ב-`main.tsx`; `App.tsx` מציג ספינר בזמן `loading`, מסך `Login` בלבד (ללא ה-`Layout`/ניווט) כשאין `user` מאומת, ורק כשיש session מוצג ה-`Layout` + כל ה-`Routes` הקיימים — כלומר כל האפליקציה מאחורי login, לא רק endpoints בודדים.

  **`Layout.tsx`**: ה-AppBar מציג כעת שם המשתמש המחובר + `Chip` עם תרגום תפקיד (מיפוי `ROLE_LABELS` חדש ב-`format.ts`) וכפתור התנתקות. פריטי הניווט קיבלו שדה `roles?` אופציונלי (חסר = גלוי לכל תפקיד מאומת) שמשקף את קבוצות ה-`require_roles(...)` הקיימות ב-Backend לכל מודול (למשל תביעות: `RISK_MANAGER`/`CFO`/`ADJUSTER`; פוליסות/סימולציה/השתתפות-עצמית/דוחות: `RISK_MANAGER`/`CFO`; מיטיגציה: `RISK_MANAGER`/`PROPERTY_MANAGER`) — כאשר `ADMIN` תמיד רואה הכול ללא תלות ברשימה. דיווח אירוע ומסמכים נשארו גלויים לכל תפקיד (תואם ל-`require_roles()` ללא ארגומנטים ב-Backend).

  נבדק: `npx tsc -b` ללא שגיאות חדשות (רק פער `stylis` הקיים). נבדק בפועל מול `RiskDB` מקומי דרך שרת preview: כניסה עם `dana.cohen@company.co.il` (RISK_MANAGER, סיסמת seed `Demo1234!`) הצליחה (`POST /api/auth/login → 200`) והציגה את כל 11 פריטי הניווט + "דנה כהן" / "מנהל סיכונים" ב-AppBar; התנתקות (`ref logout button`) ניקתה session והחזירה למסך Login. כניסה עם `ronit.shimoni@company.co.il` (FIELD_WORKER) הציגה נכון רק 4 פריטים (דשבורד/דיווח אירוע/ניהול אירועים/מסמכים) ו-"רונית שמעוני" / "עובד שטח". רענון עמוד (F5) עם טוקן קיים שמר את ה-session (קריאה שקטה ל-`GET /auth/me`) ללא חזרה למסך Login. סיסמה שגויה החזירה `401` עם ההודעה "אימייל או סיסמה שגויים" שהוצגה ב-`Alert` אדום בטופס.

- [x] **הרחבת שכבת ה-API בפרונט** לכל ה-Endpoints החדשים (מדיה, מסמכים, סימולציה, תזרים, אימות).
  📁 `frontend/src/api/client.ts`
  ✅ בוצע ונבדק ב-branch `feature/frontend-api-layer-extension`: השוואה שיטתית בין כל ה-routers ב-`backend/app/routers/*.py` (`grep` על `@router.get/post/put/patch/delete`) מול הפונקציות הקיימות ב-`client.ts` העלתה שרוב הקטגוריות שצוינו במשימה (מדיה, מסמכים, סימולציה, תזרים, אימות) כבר כוסו במלואן במשימות קודמות של שלב 6 — אך נמצאו 4 פערים קונקרטיים:

  1. **`fetchClaim(id)`** — חסרה עטיפה ל-`GET /api/claims/{claim_id}` (היה קיים רק `fetchClaims()` לרשימה); הוחזר `Claim` הקיים.
  2. **`fetchMitigationTask(id)`** — חסרה עטיפה ל-`GET /api/mitigation-tasks/{task_id}` (היה קיים רק `fetchMitigationTasks()` לרשימה ו-`fetchMitigationTaskRoi`); הוחזר `MitigationTask` הקיים.
  3. **`fetchRetentionRecommendationForIncident(incidentId)`** — חסרה עטיפה ל-`GET /api/retention/incidents/{incident_id}` (הווריאנט הנוח שמחשב לפי אירוע קיים, לעומת `fetchRetentionRecommendation` שדורש `policy_id`/`property_id`/`estimated_loss` ידניים).
  4. **`streamExecutiveSummary()`** — `Reports.tsx` קרא ל-`GET /api/ai/executive-summary` (סטרימינג `text/plain`) דרך `fetch()` גולמי, לגמרי מחוץ ל-`client.ts` — כלומר ללא ה-`Authorization: Bearer` header שנוסף למשימת ה-Login (ה-endpoint לא דורש כרגע אימות, אך זה היה הקריאה היחידה בכל האפליקציה שעוקפת את שכבת ה-API המרכזית ולא הייתה עקבית איתה). נוספה פונקציה ב-`client.ts` שעוטפת את אותו `fetch()` (axios לא חושף Response stream נוח בדפדפן) אך מצרפת את הטוקן מ-`getAccessToken()` ומחזירה את ה-`ReadableStream` ישירות; `Reports.tsx` עודכן להשתמש בה במקום ב-`fetch` הגולמי.

  שאר הקטגוריות נמצאו כבר מכוסות במלואן ולא נדרש שינוי: מדיה (`uploadIncidentMedia`/`fetchIncidentMedia`/`deleteIncidentMedia`/`fetchMediaSignedUrl`), מסמכים (`fetchDocumentsForEntity`/`fetchDocumentSignedUrl`/`uploadDocument`/`deleteDocument`), סימולציה (`fetchPortfolioSimulation`/`fetchPropertySimulation`), תזרים (`fetchCashflowSummary`), אימות (`login`/`logout`/`fetchCurrentUser` + interceptors ממשימת ה-Login). לא נוספה עטיפה ל-`GET /api/auth/sso/{provider}/login`/`POST /callback` — אלה stub-ים ללא UI מתאים בפרונט (אין כפתור SSO במסך Login), מחוץ לתחום המשימה הזו.

  נבדק: `npx tsc -b` ללא שגיאות חדשות (רק פער `stylis` הקיים). נבדק בפועל מול `RiskDB` מקומי דרך שרת preview: `GET /api/claims/1`, `GET /api/mitigation-tasks/1`, `GET /api/retention/incidents/1` (כולם עם `Authorization` header) החזירו `200` עם מבנה תואם בדיוק לטיפוסי TS הקיימים (`Claim`/`MitigationTask`/`RetentionRecommendation`). לחיצה על "הפק דוח" בעמוד `/reports` חוללה קריאת `GET /api/ai/executive-summary` תקינה (כצפוי `503` — אין `ANTHROPIC_API_KEY` מוגדר בסביבה המקומית, זו התנהגות תקינה מתועדת) ללא שגיאות JS לא-מטופלות בקונסול.

---

## שלב 7 — Mobile & Offline

- [x] **התאמת PWA**: manifest, Service Worker, והתקנה כאפליקציה במכשיר נייד.
  📁 `frontend/index.html`, `frontend/vite.config.ts` + קובץ חדש `frontend/public/manifest.json`

  נוסף `frontend/public/manifest.json` (Web App Manifest) עם `name`/`short_name` בעברית, `dir: "rtl"`/`lang: "he"`, `display: "standalone"`, `theme_color`/`background_color` תואמים לפלטת ה-MUI theme (`#1e5b8a`/`#f4f6f8`), ואייקוני `image/svg+xml` בגודל `"any"` (מטרה `any` + `maskable`) — נבחר SVG במקום PNG כדי להימנע מתלות בכלי רינדור רסטר חיצוני; זה נתמך בדפדפני Chromium מודרניים לצורך installability. `index.html` עודכן עם `<link rel="manifest">`, `<meta name="theme-color">`, ותגיות `apple-mobile-web-app-*`/`mobile-web-app-capable` להתקנה גם ב-iOS Safari.

  נוסף Service Worker ב-`frontend/public/sw.js` שמבצע cache-first ל-assets סטטיים מאותו origin ו-network-first עם fallback ל-`offline.html` (חדש) בניווטים כשאין רשת — **בכוונה לא** מטמין תגובות `/api/*` (`url.pathname.startsWith("/api/")` מדלג על ה-fetch handler) כדי לא להגיש נתונים מיושנים; זה תואם את ה"מחוץ לתחום" המתועד ב-`CLAUDE.md` (offline mobile sync מלא הוא משימה נפרדת ומחוץ לתחום). נוסף `frontend/src/registerServiceWorker.ts` הרושם את ה-SW רק ב-build של production (`import.meta.env.PROD`) כדי לא לשבש HMR בפיתוח, ונקרא מ-`main.tsx`. נדרש `frontend/src/vite-env.d.ts` חדש (`/// <reference types="vite/client" />`) כדי ש-TypeScript יכיר את `import.meta.env` — לא היה קיים בפרויקט קודם לכן. `vite.config.ts` לא שונה: הפלטת `public/` מועתקת אוטומטית ל-root של ה-build הסטנדרטי של Vite, ללא תוסף נוסף.

  נבדק: `npx tsc -b` ללא שגיאות חדשות (רק פער `stylis` הקיים). `npx vite build` הצליח; `dist/` כלל את `manifest.json`, `sw.js`, `offline.html`, `icon.svg`, `icon-maskable.svg` ב-root כצפוי. הרצת `vite preview` ובדיקה בדפדפן אישרה של-`manifest.json` תוכן ומבנה תקינים (`fetch("/manifest.json")` החזיר JSON תקין עם כל השדות). רישום ה-Service Worker (`navigator.serviceWorker.register`) נכשל בסביבת ה-Browser pane הפנימית של הכלים (שגיאת `"An unknown error occurred when fetching the script"`) — אומת שזו מגבלת התשתית של דפדפן האוטומציה/ה-proxy המקומי ולא באג בקוד: אותה קריאת רישום הצליחה במלואה (`active: true`) כנגד Service Worker חיצוני תקין (`https://googlechrome.github.io/samples/service-worker/basic/`) מאותו tabId, ו-`window.isSecureContext` על `http://localhost` החזיר `true` (כלומר לא בעיית secure-context). קובץ ה-`sw.js` עצמו אומת כתקין syntactically ונגיש (`GET /sw.js → 200 OK`).

- [x] **מנגנון Offline Sync**: שמירת דיווחים ותמונות בזיכרון מקומי (IndexedDB) וסנכרון אוטומטי עם חידוש הקליטה.
  📁 קובץ חדש: `frontend/src/offline/syncQueue.ts`

  **הערת סטטוס:** משימה זו מתועדת ב-`CLAUDE.md` תחת "Deliberately out of scope" (offline mobile sync). מומש בכל זאת לפי אישור מפורש של המשתמש בצ'אט לחרוג מהיקף זה.

  `frontend/src/offline/syncQueue.ts` (חדש) מממש תור מבוסס IndexedDB (`rmis-offline` DB, object store `incident-queue`, מפתח `id` אוטומטי) עם API: `enqueueIncidentReport(payload, files)` (שומר את מטען ה-`IncidentCreate` ואת קבצי המדיה כ-`Blob`), `getQueuedReports()`, `trySync()` (משחרר את התור בסדר FIFO — יוצר את האירוע דרך `POST /incidents`, מעלה כל קובץ מדיה, ומוחק מהתור בהצלחה; עוצר בכשלון הראשון כדי לא לשבש את סדר הסנכרון בניסיון הבא), `registerAutoSync()` (נרשם פעם אחת ב-`main.tsx`; מפעיל `trySync` מיד אם יש רשת בעליית האפליקציה, וגם על כל אירוע `online`), ו-`subscribeToSyncQueue()` למאזיני UI (מונה ממתינים + מצב סנכרון).

  שילוב ב-`frontend/src/pages/IncidentReport.tsx`: ה-`submitMutation` עוטף את קריאת ה-API הרגילה (כולל הזרימה הקיימת של טיוטה קיימת מול אירוע חדש) ב-`try/catch`; בכשלון שמזוהה כ"אין רשת" (`navigator.onLine === false`, או שגיאת axios ללא `response` — כלומר הבקשה לא הגיעה לשרת כלל) — נשמר מטען אירוע חדש מלא (לא כולל את זרימת ה-PATCH/submit הספציפית לטיוטה, ראו הערה בקוד) ב-`enqueueIncidentReport`, והמסך המסכם מציג "הדיווח נשמר במכשיר" במקום "נשלח בהצלחה". נוסף באנר בראש הטופס שמציג מספר דיווחים ממתינים לסנכרון מהמכשיר (`subscribeToSyncQueue`) עם כפתור "נסה לסנכרן עכשיו" (`trySync()` ידני), בנוסף לסנכרון האוטומטי. `registerAutoSync()` נקרא מ-`main.tsx` לצד `registerServiceWorker()` כדי שהסנכרון יפעל בכל דף, לא רק בזמן שהטופס פתוח.

  נבדק: `npx tsc -b` ללא שגיאות חדשות. אומת ישירות מול המודול (`import('/src/offline/syncQueue.ts')` בקונסולת הדפדפן, מול `RiskDB`/שרתי preview מקומיים): `enqueueIncidentReport` עם מטען + קובץ בדיוני שמר רשומה תקינה ב-IndexedDB (`getQueuedReports()` החזיר אותה במלואה); `trySync()` יצר בהצלחה אירוע אמיתי בשרת (`INC-2026-012`, אומת ישירות מול `GET /api/incidents`) וניקה את התור (`remainingCount: 0`). אימות אינטראקציה מלא מול ה-UI (מילוי האשף ולחיצה על "שלח דיווח" תוך ניתוק השרת) נתקל במגבלת תשתית של ה-Browser pane של הכלים בסבב הזה — קליקים/הקלדות דרך `computer` לא תמיד הגיעו ל-DOM בפועל (נצפה גם ב-`login`/ב-Autocomplete של הטופס), ולכן הזרימה אומתה ברמת הלוגיקה/המודול ישירות במקום דרך UI end-to-end; קוד ה-`IncidentReport.tsx` עצמו עבר type-check נקי ותואם ישירות ל-API שנבדק.

- [x] **אינדיקציית מצב חיבור** בממשק הדיווח.
  📁 `frontend/src/components/Layout.tsx`
  ✅ בוצע ונבדק ב-branch `feature/connection-status-indicator`: נבנה על גבי "מנגנון Offline Sync" שהושלם במשימה הקודמת — `subscribeToSyncQueue`/`trySync` כבר קיימים ב-`frontend/src/offline/syncQueue.ts` וסיפקו את כל המידע הדרוש (מצב סנכרון + מונה ממתינים); נותר רק לחשוף חיווי גלובלי ב-AppBar במקום רק בבאנר המקומי של `IncidentReport.tsx`.

  נוסף hook חדש **`frontend/src/hooks/useOnlineStatus.ts`**: עוטף `navigator.onLine` ומאזין לאירועי `online`/`offline` על ה-`window` כדי לעדכן בזמן אמת.

  **`Layout.tsx`**: נוסף רכיב פנימי `ConnectionStatus` המוצג ב-AppBar (גלוי בכל מסכי האפליקציה, לא רק בדיווח אירוע — מיקום מרכזי יותר מהבאנר שנוסף במשימה הקודמת ל-`IncidentReport.tsx` בלבד) המשלב שני מקורות מידע: `useOnlineStatus()` ו-`subscribeToSyncQueue()`. שלושה מצבי תצוגה: (1) מקוון וללא דיווחים ממתינים — `Chip` ירקרק שקוף עם אייקון `WifiIcon` ו"מקוון"; (2) לא מקוון — `Chip` בצבע אזהרה (`warning`) עם `WifiOffIcon` ו"לא מקוון" (או "לא מקוון · N ממתינים" אם יש גם דיווחים בתור מהמנגנון הקודם); (3) מקוון אך עם דיווחים שטרם סונכרנו (מצב ביניים אחרי חזרת רשת, לפני שה-`trySync` האוטומטי הספיק לרוץ/הצליח) — `Chip` אזהרה לחיץ עם "N ממתינים לסנכרון" שמפעיל `trySync()` ידני בלחיצה, ומציג `CircularProgress` בזמן הסנכרון עצמו. `Tooltip` על כל מצב מסביר את המשמעות בעברית.

  נבדק: `npx tsc -b` ללא שגיאות חדשות (רק פער `stylis` הקיים). נבדק בפועל מול `RiskDB` מקומי דרך שרת preview: כניסה כ-`dana.cohen@company.co.il` הציגה נכון Chip "מקוון" ב-AppBar. הדמיית ניתוק (`Object.defineProperty(navigator,'onLine',...)` + `dispatchEvent(new Event('offline'))` בקונסולה) עדכנה מיידית ל-"לא מקוון". הוספת דיווח לתור (`enqueueIncidentReport` דרך ייבוא ישיר של המודול) בזמן ניתוק עדכנה את הטקסט ל-"לא מקוון · 1 ממתינים". שחזור החיבור (`dispatchEvent(new Event('online'))`) הפך את ה-Chip לכפתור לחיץ "1 ממתינים לסנכרון"; קריאה ל-`trySync()` נכשלה כצפוי (`422`) מכיוון שמטען הבדיקה שהוזן ידנית היה חלקי (חסרים שדות חובה שאינם רלוונטיים לבדיקת ה-UI עצמה) — ה-Chip המשיך להציג נכון את מצב ה"ממתינים" ללא קריסה, ולאחר ניקוי התור וריענון הדף חזר Chip "מקוון" הרגיל. אימות אינטראקציית לחיצה מלאה על ה-Chip עצמו (`computer click`) נתקל שוב במגבלת האוטומציה המתועדת כבר במשימת ה-Offline Sync הקודמת; הפונקציונליות שמאחורי הלחיצה (`trySync()`) אומתה ישירות.

---

## שלב 8 — Integrations

- [x] **מחבר ERP / הנהלת חשבונות** (SAP / Oracle / Priority): משיכת שווי נכסים בספרים ורישום תקבולי תביעות.
  📁 קובץ חדש: `backend/app/integrations/erp.py`
  ✅ בוצע ונבדק ב-branch `feature/erp-integration`. אין חשבון ERP אמיתי ואין credentials ב-`backend/.env` (בדיוק כמו `services/notifications.py` שכבר קיים בפרויקט) — לכן המחבר אינו פותח חיבור רשת אמיתי כלשהו, אלא מדגים את שתי כיווני הסנכרון בצורה "מדומה" (simulated), עם אותה תבנית תיעוד/logging.

  **`backend/app/integrations/erp.py`** (חדש): `pull_asset_book_values(db, property_ids=None)` — מדמה ייצוא Fixed Assets מה-ERP: קורא את `Properties` הפעילים ומחזיר שורה לכל נכס (`property_code`, `book_value`, `replacement_value`, `as_of`, `status="simulated_pull"`) — בפועל, בסנכרון אמיתי זה היה הצד שממנו `Properties.book_value` היה מתעדכן; המחבר אינו כותב בפועל בחזרה לטבלה (אין עמודת מצב-סנכרון בסכימה לשמור בה מתי בוצע העדכון האחרון, אותה מגבלה מתועדת שכבר קיימת ב-`notifications.py` לגבי מצב-שליחה). `build_claim_receipt_postings(db, since=None, claim_id=None)` — הופך שורות `Claim_Payments` (מצורף אליהן `Claims`→`Incidents`→`Properties` כדי לקבל `claim_number`/`property_code`) לרשומות פקודת יומן חשבונאית (חיוב חשבון מזומן `1010-CASH`, זיכוי חשבון חייבים-ביטוח `1200-AR-INSURANCE`) — לא "שולח" כלום, רק בונה את המבנה, בדיוק כמו ההפרדה בין `build_notifications` ל-`dispatch_notifications`. `post_claim_receipts(db, since=None, claim_id=None)` — קורא ל-`build_claim_receipt_postings` ו"שולח" כל רשומה (רישום ל-`logger.info` בפורמט `[SIMULATED ERP POST] ...`, שינוי `status` ל-`"simulated_post"` + חותמת `posted_at`); ללא דגל "כבר נשלח" בסכימה, כל קריאה בונה מחדש מהמאגר (ניתן לסנן לפי `since`/`claim_id`), כדי שקריאה חוזרת לא "תמציא" נתונים חדשים אך גם לא תישבר על ניסיון עדכון רשומה שלא קיימת.

  **`backend/app/routers/integrations.py`** (חדש) + נרשם ב-`backend/app/main.py`: `GET /api/integrations/erp/book-values` (מקבל `property_id` אופציונלי, חוזר `list[ErpBookValueOut]`) ו-`POST /api/integrations/erp/post-claim-receipts` (מקבל `since`/`claim_id` אופציונליים, חוזר `list[ErpClaimReceiptPostingOut]`) — שני ה-endpoints מוגנים ב-`require_roles("RISK_MANAGER", "CFO", "ADMIN")`, אותה קבוצת הרשאות כמו `routers/policies.py`, מכיוון שמדובר בנתוני כספים/הנה"ח ולא בעניין של עובד שטח. נוספו `schemas.ErpBookValueOut` ו-`schemas.ErpClaimReceiptPostingOut` ל-`backend/app/schemas.py`.

  נבדק: השרת עלה נקי (`preview_logs` ללא שגיאות). דרך `javascript_tool` מול שרת preview מקומי (`dana.cohen@company.co.il`, RISK_MANAGER): `GET /api/integrations/erp/book-values` החזיר `200` עם 15 נכסים (לדוגמה `PRP-001` עם `book_value: 32000000`, `status: "simulated_pull"`); `POST /api/integrations/erp/post-claim-receipts` החזיר `200` עם 5 פקודות יומן (לדוגמה `CLM-2025-08`, `amount: 400000`, `debit_account: "1010-CASH"`, `status: "simulated_post"`). נבדקה גם אכיפת ה-RBAC: משתמש `FIELD_WORKER` (`ronit.shimoni@company.co.il`) קיבל `403` עם הודעה "תפקיד 'FIELD_WORKER' אינו מורשה לפעולה זו" בניסיון לגשת ל-endpoint. הערה: שורות ה-`logger.info` של "[SIMULATED ERP ...]" לא הופיעו בפועל ב-`preview_logs` מכיוון שאין `logging.basicConfig`/הגדרת רמת-לוג כלשהי בפרויקט (`root` logger נשאר ב-`WARNING` כברירת מחדל) — אותה מגבלת-תשתית קיימת כבר גם ב-`notifications.py` ואינה רגרסיה שהוכנסה כאן; הפונקציונליות עצמה (ערכי ה-JSON שהוחזרו מה-API) אומתה במלואה.

- [x] **פתיחת משימה אוטומטית ב-ERP/תחזוקה** בעת אירוע בחומרה קריטית.
  📁 `backend/app/integrations/erp.py` + `backend/app/routers/incidents.py`
  ✅ בוצע ונבדק ב-branch `feature/erp-critical-incident-ticket`. מרחיב את `erp.py` שנוסף במשימה הקודמת בכיוון שלישי, מונע-אירוע (event-driven) במקום נמשך/מתוזמן: כשמדווח אירוע בחומרה `CRITICAL`, נפתח אוטומטית גם כרטיס תחזוקה "מדומה" ב-ERP וגם משימת מיטיגציה אמיתית בטבלת `Mitigation_Tasks` — כדי שהצוות יראה אותו מיד בלוח המיטיגציה הקיים, לא רק בלוג.

  **`backend/app/integrations/erp.py`** (הרחבה): `open_maintenance_ticket(incident)` — פונקציה טהורה (לא מקבלת `db`, מקבלת אובייקט `Incident` שכבר נטען) שבודקת `severity_level == "CRITICAL"` (מחזירה `None` אחרת — שומרת שהפונקציה בטוחה גם אם נקראת ללא תנאי מראש), בונה רשומת "כרטיס עבודה" בפורמט שהיה מצופה מ-ERP/CMMS (`ticket_number`, `priority: "URGENT"`, `summary`, `status: "simulated_ticket"`) ורושמת אותה ל-logger ב-**רמת WARNING** (בכוונה — אירוע קריטי רועש יותר מרישום תקבול שוטף), לא INFO כמו שתי הפונקציות הקודמות במודול.

  **`backend/app/routers/incidents.py`**: פונקציית עזר חדשה `_trigger_critical_incident_ticket(db, incident)` — אם `severity_level != "CRITICAL"` יוצאת מיידית; אחרת יוצרת `MitigationTask` אמיתי (`title` מפרט את קוד וסוג האירוע, `due_date` בעוד 3 ימים בלבד — ענף "מישהו צריך לטפל בזה עכשיו", לא תכנון מיטיגציה שגרתי, `status="OPEN"`, `cost_estimate`/`expected_annual_savings` אפס כי אלה ייקבעו בפועל כשמישהו יבדוק את הכרטיס), מבצעת `commit`, ואז קוראת ל-`erp.open_maintenance_ticket`. נקראת משתי נקודות כניסה: `POST /api/incidents` (רק אם `is_draft=False` — דיווח ישיר, לא טיוטה) ו-`PATCH /{incident_id}/submit` (אחרי שהטיוטה הופכת ל-`is_draft=False`) — כדי שטיוטה קריטית לא תפתח כרטיס לפני שדווחה בפועל.

  נבדק: השרת עלה נקי (`preview_logs` ללא שגיאות). דרך `javascript_tool` מול שרת preview מקומי: (1) דיווח אירוע `CRITICAL` ישיר כ-`dana.cohen@company.co.il` (RISK_MANAGER) — יצר `INC-2026-013`, ומיד לאחר מכן נוספה משימת מיטיגציה חדשה (`task_id: 13`, `title: "טיפול דחוף באירוע קריטי INC-2026-013 (FIRE)"`, `due_date` בעוד 3 ימים, `status: "OPEN"`), אומת מול `GET /api/mitigation-tasks` (13→12 השתנה, כלומר +1). (2) אירוע `LOW` — מספר המשימות לא השתנה (13→13). (3) טיוטה (`is_draft: true`) בחומרה `CRITICAL` — מספר המשימות לא השתנה מיד לאחר היצירה (13→13); לאחר `PATCH /{id}/submit` נוספה משימה חדשה (`task_id: 14`) — מאמת שהטריגר ממתין להגשה בפועל ולא נורה על טיוטה. (4) דיווח אירוע `CRITICAL` כ-`ronit.shimoni@company.co.il` (`FIELD_WORKER`) הצליח (`200`, יצירת אירוע פתוחה לכל תפקיד מאומת — ללא שינוי בהרשאות) ופתח משימה נוספת כראוי — מוודא שהטריגר לא תלוי בתפקיד היוצר. שורות ה-`[SIMULATED ERP TICKET] ...` **כן הופיעו** בפועל ב-`preview_logs` (בניגוד לפער ה-INFO/logging.basicConfig שתועד במשימה הקודמת) — מכיוון שרמת ברירת המחדל של ה-`root` logger היא `WARNING`, ורישום כרטיס קריטי נעשה במפורש ברמת `logger.warning`, לא `logger.info`.

- [x] **מחבר GIS חיצוני** (Govmap / Mapbox Layers): שכבות אזורי הצפה וסיכון אקלימי.
  📁 קובץ חדש: `backend/app/integrations/gis.py`
  ✅ בוצע ונבדק ב-branch `feature/gis-integration`. אותה תבנית "סימולציה" כמו `erp.py`/`notifications.py` — אין מפתח Govmap/Mapbox אמיתי ב-`backend/.env`, אז המחבר לא פותח חיבור רשת. ההבדל מ-`RiskMap.tsx` הקיים ("אזורי הצפה" כ-Toggle): שם זה רק ציור עיגול סביב הנכס לפי `Asset_Risk_Profiles.flood_risk_score` הפנימי (סקר RMIS עצמו) — אין לו שום קשר לגבול הצפה אמיתי וממילא לא יכול להתריע כשהסקר הפנימי מיושן. המחבר החדש סוגר את הפער הזה: מספק שכבת סיווג הצפה + מדד סיכון אקלימי **חיצוניים** (מדומים) לכל נכס, ומצליב אותם מול הניקוד הפנימי.

  **`backend/app/integrations/gis.py`** (חדש): `fetch_risk_layers(db, property_ids=None)` — לכל נכס פעיל, מחשב `flood_zone` (סיווג בסגנון FEMA: `X`/`B`/`A`/`AE`) ו-`climate_risk_index` (0-100) **דטרמיניסטית** מתוך קואורדינטות הנכס (`hashlib.sha256` יציב על `lat`/`lon`, לא `hash()` המובנה שממולח אקראית בכל הרצה) — כדי שקריאה חוזרת לאותו נכס תמיד תחזיר את אותה שכבה מדומה, בדיוק כמו ש-`erp.pull_asset_book_values` מחשב מחדש מאפס בלי לשמור מצב. בניגוד למכוון ב-`erp.py` (שם נמנע במפורש מהשוואה מול נתוני RMIS כי אין מערכת חיצונית אמיתית שתחלוק), כאן **כן** מתבצעת השוואה: `flood_risk_score` (1-10) מ-`Asset_Risk_Profiles` מומר לאותו סולם אינדקס-אזור (0-3) של השכבה המדומה, וכל סטייה מעל `_MISMATCH_THRESHOLD` מסומנת `mismatch_with_internal_survey: true` — כי זו בדיוק התכלית של מחבר אזורי-הצפה (להצביע "המפה החיצונית לא מסכימה עם הסקר האחרון, כדאי לסקר מחדש"). אין כתיבה חזרה ל-`Asset_Risk_Profiles` (אין עמודת `gis_last_synced_at` בסכימה לשמור בה שהסטייה נבדקה) — אותה מגבלה מתועדת שכבר קיימת ב-`erp.py`/`notifications.py`.

  **`backend/app/routers/integrations.py`**: נוסף `GET /api/integrations/gis/risk-layers` (מקבל `property_id` אופציונלי, חוזר `list[GisRiskLayerOut]`), מוגן ב-`require_roles("RISK_MANAGER", "RISK_OFFICER", "ADMIN")` — קבוצת הרשאות שונה מ-`_ERP_ROLES` (שכולל `CFO`) כי מדובר בנתוני הערכת סיכונים, לא כספים. נוספה `schemas.GisRiskLayerOut` ל-`backend/app/schemas.py`.

  נבדק: השרת עלה נקי (`preview_logs` ללא שגיאות). דרך `javascript_tool` מול שרת preview מקומי, `dana.cohen@company.co.il` (RISK_MANAGER): `GET /api/integrations/gis/risk-layers` החזיר `200` עם 15 נכסים, 4 מתוכם מסומנים `mismatch_with_internal_survey: true` (לדוגמה `PRP-003`: ניקוד פנימי `3`, אזור חיצוני `AE`). נבדקה דטרמיניזם — שתי קריאות עוקבות ל-`?property_id=1` החזירו בדיוק אותו `flood_zone`/`climate_risk_index`. נבדק סינון `property_id` — החזיר רשומה יחידה תואמת. נבדקה אכיפת RBAC — `ronit.shimoni@company.co.il` (`FIELD_WORKER`) קיבל `403` עם "תפקיד 'FIELD_WORKER' אינו מורשה לפעולה זו". שורת ה-`logger.info` של "[SIMULATED GIS PULL] ..." **לא** הופיעה ב-`preview_logs` — עקביות עם הפער התיעודי הקיים (`root` logger ב-`WARNING` כברירת מחדל, ללא `logging.basicConfig`), כמו ב-`erp.py`'s פונקציות ה-INFO (לא כמו הכרטיס הקריטי שנרשם ב-WARNING במשימה הקודמת).

- [x] **מחבר נתוני מזג אוויר**: התראות מזג אוויר קיצוני לפי מיקום הנכסים.
  📁 קובץ חדש: `backend/app/integrations/weather.py`
  ✅ בוצע ונבדק ב-branch `feature/weather-integration`. (הערה: שורה זו הופיעה כפולה במסמך המקורי — הכפילות הוסרה כאן, זו אותה משימה יחידה). אותה תבנית "סימולציה" כמו `erp.py`/`gis.py` — אין מפתח IMS/OpenWeather אמיתי ב-`backend/.env`, אז המחבר לא פותח חיבור רשת. שני הבדלים מכוונים מול `gis.py`: (1) גיאוגרפיה (אזור הצפה) קבועה בזמן, אבל מזג אוויר משתנה מיום ליום — לכן ה-hash היציב כאן כולל גם את התאריך (`(latitude, longitude, as_of)`), לא רק קואורדינטות, כך שקריאה חוזרת **באותו יום** מחזירה בדיוק אותה תחזית מדומה, אבל תאריך אחר מחזיר תוצאה שונה — בדיוק כמו הבדל בין שכבת GIS סטטית לפיד תחזית יומי. (2) `gis.py`/`erp.py` מחזירים שורה אחת לכל נכס תמיד; כאן, בהתאמה לאיך שפיד התראות אמיתי עובד, רק נכסים שה"תחזית" המדומה שלהם חצתה סף התראה (`_ALERT_PROBABILITY_PERCENT = 20`) מופיעים בתשובה — רוב הנכסים ברוב הימים לא מקבלים שורה כלל.

  **`backend/app/integrations/weather.py`** (חדש): `fetch_weather_alerts(db, property_ids=None, as_of=None)` — לכל נכס פעיל (מסונן אופציונלית לפי `as_of`, ברירת מחדל היום), מגריל דטרמיניסטית (`hashlib.sha256`) האם יש התראה פעילה, ואם כן — `alert_type` (`STORM`/`FLOOD_WARNING`/`HEATWAVE`) ו-`severity` (`ADVISORY`/`WATCH`/`WARNING`). אין כתיבה חזרה לשום טבלה, ואין יצירה אוטומטית של `Incidents`/`Mitigation_Tasks` — זהו קלט "לפני האירוע" (תחזית) שהשימוש בו נשאר החלטה של הצד הצורך (בניגוד ל-`_trigger_critical_incident_ticket` ב-`routers/incidents.py` שמגיב לאירוע שכבר קרה).

  **`backend/app/routers/integrations.py`**: נוסף `GET /api/integrations/weather/alerts` (מקבל `property_id`/`as_of` אופציונליים, חוזר `list[WeatherAlertOut]`) — פתוח לכל תפקיד מאומת (`require_roles()` ללא ארגומנטים), בניגוד לשני ה-endpoints הקודמים (ERP/GIS) שמוגבלים לתפקידי ניהול-סיכונים/כספים — כי התראת מזג אוויר קיצוני היא אזהרה תפעולית שגם עובד שטח בנכס צריך לראות, לא רק קברניט. נוספה `schemas.WeatherAlertOut` ל-`backend/app/schemas.py`.

  נבדק: השרת עלה נקי (`preview_logs` ללא שגיאות). דרך `javascript_tool` מול שרת preview מקומי: `GET /api/integrations/weather/alerts` (ללא `as_of`, `dana.cohen@company.co.il`) החזיר `200` עם 3 נכסים בעלי התראה פעילה (לדוגמה `PRP-001` — `STORM`/`WATCH`, `PRP-006` — `HEATWAVE`/`ADVISORY`). נבדקה יציבות תוך-יומית — שתי קריאות עוקבות ללא `as_of` החזירו תוכן זהה (למעט `issued_at`, חותמת-זמן `utcnow()` המשתנה בכל קריאה כמצופה — לא באג). נבדק `?as_of=2026-01-01` והחזיר סט נכסים שונה (2 נכסים, לא חופפים ל-3 של היום) — מאמת שהתאריך משפיע על התוצאה בניגוד לשכבת ה-GIS הסטטית. נבדקה גישת `FIELD_WORKER` (`ronit.shimoni@company.co.il`) — `200` כמצופה (אין הגבלת תפקיד). נבדקה גישה ללא אימות — `401`. שורת ה-`logger.info` של "[SIMULATED WEATHER PULL] ..." **לא** הופיעה ב-`preview_logs` — עקביות עם אותו פער תיעודי קיים (INFO מתחת לרמת ברירת המחדל `WARNING` של ה-`root` logger).

- [ ] **מחבר מדדים כלכליים**: מדד אינפלציה ומחירי תשומות בנייה לעדכון שווי כינון.
  📁 קובץ חדש: `backend/app/integrations/economics.py`

- [ ] **שירות שליחת התראות חיצוני** (SMS / Email / Push).
  📁 `backend/app/services/notifications.py` + `backend/app/config.py`

---

## שלב 9 — Compliance & Reporting

- [ ] **דוח תאימות ISO 31000**: מיפוי סיכונים, בעלי אחריות וסטטוס בקרות.
  📁 קובץ חדש: `backend/app/routers/compliance.py` + `frontend/src/pages/Compliance.tsx`

- [ ] **דוחות רגולטוריים** בהתאם להנחיות רשות שוק ההון / Solvency II.
  📁 `backend/app/services/financials.py` + `frontend/src/pages/Reports.tsx`

- [ ] **מסך צפייה ב-Audit Log** למנהלי מערכת בלבד.
  📁 קובץ חדש: `frontend/src/pages/AuditLog.tsx`

- [ ] **הרחבת דוח ההנהלה**: הוספת סימולציית VaR, ROI על מיטיגציה וניתוח מגמות רב-שנתי.
  📁 `frontend/src/components/ExecutiveReportPrintable.tsx`

---

## שלב 10 — Quality & Ops

- [ ] **בדיקות יחידה לשירותי החישוב** (TIV, MFL, Loss Ratio, ROI, VaR).
  📁 תיקייה חדשה: `backend/tests/`

- [ ] **בדיקות אינטגרציה ל-API** (אירועים, תביעות, פוליסות, הרשאות).
  📁 `backend/tests/`

- [ ] **מיגרציות בסיס נתונים מסודרות** (Alembic) במקום עדכון ידני של הסכימה.
  📁 תיקייה חדשה: `backend/alembic/`

- [ ] **עדכון תיעוד**: ERD מעודכן, תיאור Endpoints והוראות הרצה.
  📁 `docs/erd.md`, `docs/README.md`, `README.md`
