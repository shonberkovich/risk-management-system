[x] משימה 1: הקמת סכמת מסד נתונים למערכת המיילים

מהות המשימה: יצירת מודלים ב-SQLAlchemy וטבלאות ב-DB עבור הודעות, נמענים, תיקיות, קבצים מצורפים ותמיכה בשרשורים.
מיקום במערכת: Backend (app/models.py, alembic/versions/)
API בשרת: N/A
שלבי ביצוע (Checklist):
 שלב 1: הוספת מודל `Email` (id, sender_id, subject, body_html, thread_id, created_at, status).
 שלב 2: הוספת מודל `EmailRecipient` (email_id, user_id, recipient_type [TO/CC/BCC], is_read, folder [INBOX/ARCHIVE/TRASH/SPAM]).
 שלב 3: הוספת מודל `EmailAttachment` (id, email_id, file_path, file_name, file_size, content_type).
 שלב 4: יצירת קשרי גומלין (Relationships) בין המודלים למודל ה-User הקיים.
 שלב 5: יצירת מיגרציה בעזרת Alembic (`alembic revision --autogenerate`) והחלתה.
✅ בוצע ב-branch `feature/email-db-schema`: נוספו מודלים `Email`/`EmailRecipient`/`EmailAttachment` ב-`models.py` (טבלאות `Emails`/`Email_Recipients`/`Email_Attachments`), עם `thread_id` כ-FK עצמי המצביע על הודעת השורש של השרשור, `folder` כולל גם `SENT` (לעותק אצל השולח). עודכן `sql/schema.sql` בהתאם, ונוצרה/הוחלה מיגרציית Alembic `f65d3748905b`.

==================================================
[x] משימה 2: פיתוח שכבת הנתונים (Schemas & Pydantic Models)

מהות המשימה: הגדרת הסכמות לאימות קלט והחזרת נתונים (Request/Response) עבור פעולות המייל.
מיקום במערכת: Backend (app/schemas.py)
API בשרת: N/A
שלבי ביצוע (Checklist):
 שלב 1: יצירת סכמת `EmailCreate` הכוללת שדות to, cc, bcc (רשימות מזהים), נושא ותוכן.
 שלב 2: יצירת סכמת `EmailOut` המחזירה את פרטי המייל עם שמות השולח והנמענים (ממבוסס על `UserOut`).
 שלב 3: יצירת סכמת `EmailThreadOut` לתצוגת שרשור הודעות מקובץ.
 שלב 4: הוספת סכמות לניהול סטטוס (כמו `MarkAsRead`, `MoveToFolder`).
✅ בוצע ב-branch `feature/email-schemas`: נוספו `EmailCreate`/`EmailOut`/`EmailThreadOut`/`EmailRecipientOut`/`EmailAttachmentOut`/`MarkAsRead`/`MoveToFolder` ב-`schemas.py`, עם `EmailRecipientType`/`EmailFolder` כ-`Literal` משותפים. `EmailCreate` משתמש ב-`in_reply_to` (email_id של ההודעה שעליה עונים); ה-service (משימה 3) יפתור אותו ל-thread root.

==================================================
[ ] משימה 3: פיתוח שירות (Service) הליבה לשליחה וניהול מיילים

מהות המשימה: מימוש הלוגיקה העסקית לשמירת מייל חדש, חלוקתו לתיקיות הנמענים והשולח, וניהול שרשורים.
מיקום במערכת: Backend (app/services/email.py)
API בשרת: N/A
שלבי ביצוע (Checklist):
 שלב 1: פונקציית יצירת מייל (`send_email`) - שמירת רשומת ה-Email הראשית.
 שלב 2: פונקציית הפצה - יצירת רשומות `EmailRecipient` לכל נמען עם folder=INBOX.
 שלב 3: הוספת עותק של המייל לשולח עצמו עם folder=SENT.
 שלב 4: זיהוי תגובות (Replies) ושיוך לאותו `thread_id`.
 שלב 5: פונקציות עזר - העברה לארכיון, העברה לאשפה, סימון כנקרא.
✅ בוצע ב-branch [שם-הבראנץ']: [תיאור קצר של מה שבוצע יתווסף כאן]

==================================================
[ ] משימה 4: שילוב SSE (Server-Sent Events) להתראות זמן אמת

מהות המשימה: הקמת ערוץ פתוח בשרת כדי לדחוף התראות למשתמשים כאשר מתקבל מייל חדש ללא צורך בריענון (Polling).
מיקום במערכת: Backend (app/routers/sse.py & app/services/sse_manager.py)
API בשרת: GET /api/sse/stream
שלבי ביצוע (Checklist):
 שלב 1: יצירת `ConnectionManager` פנימי בזיכרון לניהול חיבורי לקוחות לפי `user_id`.
 שלב 2: הקמת Endpoint מסוג SSE ב-FastAPI (החזרת `EventSourceResponse` מתאימה).
 שלב 3: הוספת קריאת שידור (Broadcast) מה-`email.py service` ללקוחות הרלוונטיים (הנמענים) בסיום שליחת מייל.
 שלב 4: וידוא הרשאות - חיבור SSE דורש JWT תקין והתחברות מזוהה.
 שלב 5: התמודדות עם ניתוקים פתאומיים וניקוי חיבורים מתים.
✅ בוצע ב-branch [שם-הבראנץ']: [תיאור קצר של מה שבוצע יתווסף כאן]

==================================================
[ ] משימה 5: יצירת ה-API (Routers) של המיילים

מהות המשימה: חשיפת פעולות ה-CRUD של מערכת המיילים כ-REST API מאובטח.
מיקום במערכת: Backend (app/routers/emails.py)
API בשרת: GET /api/emails, POST /api/emails
שלבי ביצוע (Checklist):
 שלב 1: `GET /api/emails` - שליפת רשימת מיילים לפי תיקייה (Inbox/Sent/Trash) עם תמיכה בעמודים (Pagination).
 שלב 2: `GET /api/emails/{id}` - שליפת מייל בודד או שרשור שלם.
 שלב 3: `POST /api/emails` - קבלת `EmailCreate` ושליחתו באמצעות ה-Service.
 שלב 4: `PATCH /api/emails/{id}/read` - שינוי סטטוס קריאה.
 שלב 5: `PATCH /api/emails/{id}/folder` - העברת מייל בין תיקיות (לארכיון, אשפה).
✅ בוצע ב-branch [שם-הבראנץ']: [תיאור קצר של מה שבוצע יתווסף כאן]

==================================================
[ ] משימה 6: ניהול קבצים מצורפים והורדתם (Attachments)

מהות המשימה: יכולת העלאת קבצים יחד עם המייל, שמירתם בשירות אחסון הקיים, והורדתם מאובטחת.
מיקום במערכת: Backend (app/routers/emails.py, app/services/storage.py)
API בשרת: POST /api/emails/{id}/attachments, GET /api/emails/attachments/{id}/signed-url
שלבי ביצוע (Checklist):
 שלב 1: שימוש במודול `storage.py` הקיים לשמירת קבצי המייל תחת `media_storage/emails/`.
 שלב 2: עדכון פונקציית שליחת המייל שתתמוך בקבלת קבצים מרובים (UploadFile).
 שלב 3: יצירת רשומות בטבלת `EmailAttachment` וקישורן למייל.
 שלב 4: חשיפת Endpoint ליצירת Signed URL בדומה למנגנון המסמכים הקיים.
 שלב 5: אכיפת הרשאות - רק נמענים או השולח רשאים לבקש URL להורדת צרופות.
✅ בוצע ב-branch [שם-הבראנץ']: [תיאור קצר של מה שבוצע יתווסף כאן]

==================================================
[ ] משימה 7: פיתוח ממשק משתמש (React) - תיבת דואר וניווט

מהות המשימה: בניית מסך ראשי המציג רשימת מיילים, עם תפריט צדדי לניווט בתיקיות.
מיקום במערכת: Frontend (src/pages/Email/EmailDashboard.tsx, EmailSidebar.tsx)
API בשרת: GET /api/emails
שלבי ביצוע (Checklist):
 שלב 1: הוספת הגדרות הקריאה (API Client) ב-`frontend/src/api/client.ts`.
 שלב 2: יצירת `EmailSidebar` עם רשימת תיקיות (דואר נכנס, נשלח, אשפה) וחיווי מספרי למיילים שלא נקראו.
 שלב 3: פיתוח `EmailList` טבלאי המציג שולח, נושא, תקציר הטקסט ותאריך.
 שלב 4: חיבור הנתונים ל-React Query עם ריענון אוטומטי (Invalidation).
 שלב 5: יישום עיצוב ויזואלי להפרדה בין מיילים שנקראו לכאלה שלא.
✅ בוצע ב-branch [שם-הבראנץ']: [תיאור קצר של מה שבוצע יתווסף כאן]

==================================================
[ ] משימה 8: ממשק כתיבת מייל חדש (Compose)

מהות המשימה: טופס מתקדם לכתיבת הודעה, כולל בחירת נמענים חכמה ועורך עשיר.
מיקום במערכת: Frontend (src/components/EmailComposeModal.tsx)
API בשרת: POST /api/emails
שלבי ביצוע (Checklist):
 שלב 1: הקמת טופס צף (Modal) עם שדות To, CC, BCC, Subject.
 שלב 2: פיתוח קומפוננטת בחירת נמענים (Autocomplete) המושכת את רשימת העובדים מהמערכת (`GET /api/users`).
 שלב 3: שילוב עורך טקסט עשיר (לדוגמה React Quill) לכתיבת תוכן המייל.
 שלב 4: תמיכה בגרירת קבצים (Drag & Drop) לצרופות.
 שלב 5: חיבור טופס השליחה ל-API עם חיווי התקדמות (Loading) וסגירה בהצלחה.
✅ בוצע ב-branch [שם-הבראנץ']: [תיאור קצר של מה שבוצע יתווסף כאן]

==================================================
[ ] משימה 9: חיבור ה-SSE לממשק הלקוח והתראות גלובליות

מהות המשימה: האזנה לערוץ ה-SSE בצד הלקוח והצגת התראות מתפרצות בכל מקום במערכת.
מיקום במערכת: Frontend (src/hooks/useSSE.ts, src/components/MainLayout.tsx)
API בשרת: GET /api/sse/stream
שלבי ביצוע (Checklist):
 שלב 1: כתיבת Hook פנימי (`useSSE`) המאתחל חיבור `EventSource` עם הטוקן הנוכחי.
 שלב 2: האזנה לאירוע `new_email`.
 שלב 3: הקפצת חיווי גלובלי (Toast / Snackbar) ב-`MainLayout` כשיש מייל חדש.
 שלב 4: עדכון (Invalidate) של ה-Query של רשימת המיילים ומונה ההודעות ב-Sidebar אוטומטית מבלי לרענן עמוד.
 שלב 5: ניקוי ה-EventSource כשהמשתמש מתנתק מהמערכת.
✅ בוצע ב-branch [שם-הבראנץ']: [תיאור קצר של מה שבוצע יתווסף כאן]

==================================================
[ ] משימה 10: הגדרות אבטחה, הרשאות ואופטימיזציה של פניות

מהות המשימה: סגירת כל פערי האבטחה (RBAC), מניעת הזרקות קוד (XSS), ואופטימיזציה של ביצועים.
מיקום במערכת: Backend & Frontend
API בשרת: N/A
שלבי ביצוע (Checklist):
 שלב 1: אכיפת הרשאות יסודית - בדיקה ברמת ה-API שכל משתמש רשאי לקרוא/לשנות רק מיילים המשויכים אליו.
 שלב 2: ניקוי קלט - שימוש בספרייה כמו `bleach` בצד השרת כדי לנקות את ה-HTML של המייל ולמנוע XSS.
 שלב 3: (אופציונלי) הצפנת תוכן ההודעה במסד הנתונים בעזרת מנגנון ה-`EncryptedText` הקיים במערכת לפרטיות מרבית.
 שלב 4: הוספת תמיכה בחיפוש/סינון במיילים.
 שלב 5: יצירת דאטה התחלתי (Seed Data) של תכתובות בין עובדים לבדיקות ופיתוח בסביבה המקומית.
✅ בוצע ב-branch [שם-הבראנץ']: [תיאור קצר של מה שבוצע יתווסף כאן]

[ ] משימה 11: קישור אוטומטי וידני של שרשורי מיילים לישויות במערכת (Contextual Linking)

מהות המשימה: פיתוח מנגנון המאפשר לקשר שרשור מיילים (Thread) ישירות לאירוע (Incident), תביעה (Claim) או נכס (Property), כך שכל התקשורת מתועדת בתוך תיק הישות.
מיקום במערכת: Backend (models.py, routers/emails.py) & Frontend (Entity details)
API בשרת: POST /api/emails/{id}/link, GET /api/{entity}/{id}/emails
שלבי ביצוע (Checklist):
 שלב 1: הוספת טבלת גישור (Entity_Emails) המקשרת בין email_id ל-entity_type ו-entity_id.
 שלב 2: הוספת Endpoint ליצירת קשר בין מייל/שרשור לישות ספציפית.
 שלב 3: ממשק משתמש בתוך המייל המאפשר לבחור ישות לקשר אליה (למשל Dropdown חכם 'קשר לתביעה...').
 שלב 4: הוספת כרטיסיית 'תקשורת' (Emails Tab) בעמודי תיק נכס/תביעה המציגה את המיילים המקושרים.
 שלב 5: (אופציונלי) זיהוי אוטומטי - אם נושא המייל מכיל מזהה אירוע (למשל INC-2026-001), המערכת תקשר אותו אוטומטית.
✅ בוצע ב-branch [שם-הבראנץ']: [תיאור הפיתוח יכתב כאן לאחר הביצוע]

==================================================

[ ] משימה 12: תמיכה בתבניות מייל מוגדרות מראש (Email Templates)

מהות המשימה: יצירת מערכת לניהול ושמירת תבניות למיילים נפוצים (כמו דרישת מסמכים משמאי, עדכון סטטוס תביעה), כולל משתנים דינמיים (Placeholders).
מיקום במערכת: Backend (models.py, routers/email_templates.py) & Frontend (Compose Modal)
API בשרת: GET /api/email-templates, POST /api/email-templates
שלבי ביצוע (Checklist):
 שלב 1: הקמת טבלת Email_Templates (id, name, subject_template, body_template).
 שלב 2: חשיפת API לניהול תבניות (CRUD) - מוגבל למנהלים.
 שלב 3: בממשק כתיבת המייל (Compose), הוספת כפתור 'השתמש בתבנית' שפותח רשימה לבחירה.
 שלב 4: פיתוח מנגנון החלפת משתנים (למשל {{claim_number}} או {{client_name}}) בזמן טעינת התבנית.
 שלב 5: מתן אפשרות למשתמש לערוך את הטקסט לאחר טעינת התבנית ולפני השליחה.
✅ בוצע ב-branch [שם-הבראנץ']: [תיאור הפיתוח יכתב כאן לאחר הביצוע]

==================================================

[ ] משימה 13: השהיית שליחה וביטול שליחה (Undo Send / Scheduled Send)

מהות המשימה: מתן זמן חסד (למשל 10 שניות) לביטול המייל לאחר הלחיצה על 'שלח', ומתן אפשרות לתזמן שליחת מייל למועד עתידי.
מיקום במערכת: Backend (Task Queue) & Frontend (Compose, Timeout logic)
API בשרת: POST /api/emails/schedule, DELETE /api/emails/cancel-schedule
שלבי ביצוע (Checklist):
 שלב 1: Frontend: בעת שליחה רגילה, החזקת הבקשה המקומית ב-Timeout של 10 שניות והצגת כפתור 'בטל שליחה' ב-Toast.
 שלב 2: Backend: הוספת סטטוס SCHEDULED לטבלת המיילים, והוספת שדה scheduled_for.
 שלב 3: הקמת תור משימות פשוט (BackgroundTasks או Celery) שיבדוק וישלח מיילים שזמנם הגיע.
 שלב 4: הוספת אופציה ב-UI 'שלח במועד אחר' (בחירת תאריך ושעה).
 שלב 5: Endpoint לביטול השליחה (מחיקה או העברה חזרה לטיוטות) כל עוד לא הגיע המועד.
✅ בוצע ב-branch [שם-הבראנץ']: [תיאור הפיתוח יכתב כאן לאחר הביצוע]

==================================================

[ ] משימה 14: חתימות אישיות וארגוניות למשתמשים (Email Signatures)

מהות המשימה: מתן אפשרות לכל משתמש להגדיר חתימת HTML קבועה שתצורף אוטומטית לכל הודעה, וכן ניהול חתימה ארגונית אחידה מטעם האדמין.
מיקום במערכת: Backend (Users table, routers/users.py) & Frontend (Settings, Compose)
API בשרת: PATCH /api/users/{id}/signature
שלבי ביצוע (Checklist):
 שלב 1: הוספת עמודת signature (HTML) למודל ה-User במסד הנתונים.
 שלב 2: יצירת מסך הגדרות אישי (Profile Settings) ב-Frontend בו המשתמש יכול לעצב את חתימתו בעורך טקסט.
 שלב 3: בעת פתיחת חלון Compose, הזרקת החתימה באופן אוטומטי לתחתית העורך.
 שלב 4: מניעת הכפלת החתימה בשרשורי 'השב' (הכנסת החתימה רק להודעה הנוכחית לפני הציטוט).
 שלב 5: (אופציונלי) פיתוח חוק שמייצר חתימה אוטומטית לעובדים חדשים (שם + תפקיד).
✅ בוצע ב-branch [שם-הבראנץ']: [תיאור הפיתוח יכתב כאן לאחר הביצוע]

==================================================

[ ] משימה 15: סיכום מיילים ארוכים וסיווג באמצעות Claude AI

מהות המשימה: שימוש ביכולות ה-LLM הקיימות (Claude) כדי לספק 'תקציר מנהלים' לשרשורי מיילים ארוכים (TL;DR) ולהציע מענה אוטומטי.
מיקום במערכת: Backend (routers/ai.py, services/llm.py) & Frontend (EmailDetail)
API בשרת: POST /api/emails/{id}/summarize, POST /api/emails/{id}/suggest-reply
שלבי ביצוע (Checklist):
 שלב 1: הוספת Endpoint שקורא את כל הודעות השרשור ושולח ל-Claude בקשה לסיכום של עד 3 משפטים.
 שלב 2: הצגת כפתור ✨ (קריאת AI) בראש שרשור בממשק המשתמש.
 שלב 3: הוספת Endpoint המבקש מ-Claude לנסח טיוטת 'תשובה מקצועית' בהתבסס על ההודעה האחרונה.
 שלב 4: הזרקת התשובה המוצעת ישירות לתוך חלון ה-Compose כדי שהמשתמש יוכל לערוך לפני שישלח.
 שלב 5: שמירה על פרטיות - וידוא שרק נתוני השרשור הספציפי מועברים לפרומפט, ללא מידע צד ג' עודף.
✅ בוצע ב-branch [שם-הבראנץ']: [תיאור הפיתוח יכתב כאן לאחר הביצוע]

==================================================

[ ] משימה 16: תמיכה בריבוי תיבות ויצירת תיקיות מותאמות אישית (Custom Folders/Labels)

מהות המשימה: מעבר לתיקיות הבסיסיות (Inbox, Sent), המשתמש יוכל ליצור תגיות (Labels) או תיקיות משלו ולתייג מיילים (למשל: 'תביעות דחופות', 'אישור מנהל').
מיקום במערכת: Backend (models.py, routers/folders.py) & Frontend (Sidebar, Tagging UI)
API בשרת: POST /api/folders, POST /api/emails/{id}/tags
שלבי ביצוע (Checklist):
 שלב 1: יצירת טבלת Labels/Custom_Folders המקושרת ל-user_id (כדי שלכל משתמש יהיו תגיות משלו).
 שלב 2: יצירת טבלת גישור Email_Labels כדי לאפשר למייל אחד להיות מתויג במספר תגיות במקביל.
 שלב 3: הוספת אופציה ב-Sidebar ('צור תיקייה/תגית חדשה') עם בחירת צבע.
 שלב 4: ממשק גרירה (Drag & Drop) או תפריט פעולות להוספת תגית למייל מתוך רשימת המיילים.
 שלב 5: עדכון חיפוש וסינון כדי לתמוך בצפייה רק במיילים תחת תגית מסוימת.
✅ בוצע ב-branch [שם-הבראנץ']: [תיאור הפיתוח יכתב כאן לאחר הביצוע]

==================================================

[ ] משימה 17: כללי סינון אוטומטיים (Email Rules/Filters)

מהות המשימה: מנגנון המאפשר למשתמש להגדיר חוקים. לדוגמה: 'אם נושא המייל מכיל המילה 'דחוף', סמן בכוכב והוסף תגית אדומה'.
מיקום במערכת: Backend (services/email_rules.py, models.py) & Frontend (Settings)
API בשרת: POST /api/rules
שלבי ביצוע (Checklist):
 שלב 1: הוספת טבלת Email_Rules המכילה conditions (תנאים, למשל: sender==X) ו-actions (פעולות, למשל: add_label_Y).
 שלב 2: עדכון הלוגיקה של email_service.py המקבלת מייל חדש כדי שתעביר אותו דרך ה-Rules Engine לפני השמירה ל-Inbox.
 שלב 3: פיתוח ממשק הגדרות המאפשר להרכיב חוק באמצעות תפריטים קופצים (If [Condition] Then [Action]).
 שלב 4: הוספת פעולות כמו 'העבר לאשפה מיד', 'סמן כנקרא', 'העבר לתיקייה X'.
 שלב 5: וידוא ביצועים למניעת האטה בזמן חלוקת מייל בעקבות ריבוי חוקים.
✅ בוצע ב-branch [שם-הבראנץ']: [תיאור הפיתוח יכתב כאן לאחר הביצוע]

==================================================

[ ] משימה 18: התראות מחוץ למשרד ומענה אוטומטי (Out of Office / Auto-Responder)

מהות המשימה: אפשרות למשתמש להגדיר הודעת חופשה שתשלח אוטומטית חזרה לשולחים בתקופה מוגדרת.
מיקום במערכת: Backend (Users/Settings table, email_service.py) & Frontend (Settings)
API בשרת: PUT /api/users/auto-responder
שלבי ביצוע (Checklist):
 שלב 1: הוספת שדות ל-User (או טבלת הגדרות נפרדת): auto_reply_enabled, auto_reply_start, auto_reply_end, auto_reply_body.
 שלב 2: ממשק בהגדרות המשתמש (Toggle להדלקה, בחירת תאריכים ביומן, עריכת טקסט ההודעה).
 שלב 3: עדכון שירות קבלת המיילים: במידה ונמען מסומן ב'חופשה', המערכת מייצרת ומכניסה לתור מייל 'השב' אוטומטי לשולח.
 שלב 4: מניעת לולאות אינסופיות (Loop Prevention) - וידוא שמענה אוטומטי נשלח לאותו שולח פעם אחת ביום בלבד.
 שלב 5: תצוגת באנר אזהרה צהוב קטן בממשק המייל של המשתמש: 'מענה אוטומטי מופעל כעת'.
✅ בוצע ב-branch [שם-הבראנץ']: [תיאור הפיתוח יכתב כאן לאחר הביצוע]

==================================================

[ ] משימה 19: אישורי קריאה ומעקב אחרי פתיחות (Read Receipts & Tracking)

מהות המשימה: יכולת של השולח לדעת האם ומתי הנמען פתח את המייל שנשלח אליו.
מיקום במערכת: Backend (Email_Recipients table) & Frontend (Sent Folder UI)
API בשרת: GET /api/emails/{id}/tracking
שלבי ביצוע (Checklist):
 שלב 1: הוספת שדה read_at (Timestamp) לטבלת Email_Recipients המתעדכן בעת הקריאה הראשונה.
 שלב 2: בעת כתיבת מייל, אפשרות סימון וי (Checkbox) 'בקש אישור קריאה'.
 שלב 3: עדכון ממשק התיקייה 'נשלח' (Sent) שיציג צלמית של 'וי כפול' (כמו בוואטסאפ) אם המייל נקרא.
 שלב 4: ממשק מידע (Tooltip) בלחיצה על הצלמית שמציג בדיוק באיזו שעה פתח הנמען את המייל.
 שלב 5: במקרה של מספר נמענים, הצגת רשימה (מי קרא ומתי, ומי טרם פתח).
✅ בוצע ב-branch [שם-הבראנץ']: [תיאור הפיתוח יכתב כאן לאחר הביצוע]

==================================================

[ ] משימה 20: שילוב עם מערכת ה-Notifications הכללית וה-Audit Log

מהות המשימה: סנכרון מערכת המיילים עם הלוג הארגוני (Audit Log) למטרות בקרה, והמרת התראות מערכת למיילים פנימיים.
מיקום במערכת: Backend (services/notifications.py, middleware/audit.py)
API בשרת: N/A
שלבי ביצוע (Checklist):
 שלב 1: עדכון מערכת ה-Audit הכללית כך שתתעד פעולות קריטיות: 'מחיקת מייל לצמיתות', 'הורדת צרופה רגישה'.
 שלב 2: שינוי מנגנון ה-Notifications של המערכת (שכעת מדמה שליחת דוא'ל) כך שאם הנמען הוא משתמש מערכת, ה-Notification ייווצר כ-Email אמיתי בתיבת הדואר הפנימית שלו.
 שלב 3: עיצוב תבניות 'מייל מערכת' מיוחדות (למשל התראת פיקוד העורף או אזהרת מזג אוויר) שייראו שונה ממייל רגיל.
 שלב 4: ניהול רזולוציית התראות - מתן אפשרות למשתמש לבחור אם התראות מערכת יגיעו ל-Inbox הראשי או לתיקייה ייעודית (System Alerts).
 שלב 5: וידוא עמידה בהרשאות גישה (GDPR/פרטיות) כך שמנהל רשת לא יוכל לקרוא מיילים אישיים של עובד דרך ה-DB ללא הרשאה מוצהרת ב-Audit.
✅ בוצע ב-branch [שם-הבראנץ']: [תיאור הפיתוח יכתב כאן לאחר הביצוע]
